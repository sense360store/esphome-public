# ============================================================================
# Sense360 WebFlash wrapper — Kitchen PoE AirIQ + RoomIQ (STABLE, owner waiver).
# ============================================================================
# This file is the WebFlash-namespaced wrapper for the Kitchen PoE AirIQ +
# RoomIQ bundle. It is a thin wrapper that re-exports the canonical product
# YAML so the WebFlash build matrix (config/webflash-builds.json) can address
# it via the products/webflash/ namespace without duplicating package wiring.
#
# Canonical YAML (full substitutions, packages, board config):
#   products/sense360-ceiling-poe-airiq-roomiq.yaml
#   (a compat shim over products/bundles/ceiling-poe-airiq-roomiq.yaml)
#
# WebFlash config string : Ceiling-POE-AirIQ-RoomIQ
# Channel / version      : stable; the live version + artifact name are
#                          declared in config/webflash-builds.json (the sole
#                          release-eligibility source of truth).
#
# ============================================================================
# STATUS
# ============================================================================
#   * STABLE under an owner risk-acceptance waiver (HW-AIRIQ-WAIVER-2026-06),
#     promoted from preview at v1.0.6 (2026-06-09). The waiver records risk
#     acceptance, NOT hardware verification: AirIQ sensor-stack bench accuracy
#     (SPS30, SGP41, SCD41, BMP390) is not verified, and the S360-410 PoE PSU
#     is covered by HW-S360-410-WAIVER-2026-06.
#   * NOT recommended, NOT a customer default, NOT buyable. The Release-One
#     customer baseline stays Ceiling-POE-VentIQ-RoomIQ; the S360-KIT-KITCHEN-P
#     bundle stays hidden / not buyable.
#   * This wrapper declares NO version, NO channel, and NO artifact_name —
#     publication metadata lives only in config/webflash-builds.json and
#     config/product-catalog.json.
#
# History: authored as a preview candidate by
# RELEASE-PREVIEW-WEBFLASH-WRAPPERS-001 (hosted compile proof: run
# 26821900127); the reviewed preview build row was added by
# RELEASE-PREVIEW-WEBFLASH-BUILD-ROWS-001; promoted to stable v1.0.6 under the
# owner waiver (see config/product-catalog.json stable_blocker text).
# See docs/webflash-contract.md for the WebFlash artifact / grammar / token
# contract this wrapper satisfies.
# ============================================================================

packages:
  airiq_roomiq_preview_candidate: !include ../sense360-ceiling-poe-airiq-roomiq.yaml

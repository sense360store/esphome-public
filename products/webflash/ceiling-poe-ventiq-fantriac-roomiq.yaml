# ============================================================================
# Sense360 WebFlash wrapper — EXPERIMENTAL SELF-BUILD MAINS lane (FanTRIAC).
# ============================================================================
# TRIAC-COMMISSIONING-001. This is the WebFlash-namespaced release-gate wrapper
# for the experimental self-build mains build of the S360-320 Sense360 TRIAC.
# It re-exports the canonical product YAML so the declaration-driven release
# matrix (config/webflash-builds.json, ESP-007) can address the build via the
# products/webflash/ namespace; the release workflow compiles the canonical
# products/sense360-ceiling-poe-ventiq-fantriac-roomiq.yaml, not this wrapper.
#
# Canonical YAML (full substitutions, packages, board config — UNCHANGED by
# this commissioning; no YAML functional change):
#   products/sense360-ceiling-poe-ventiq-fantriac-roomiq.yaml
#
#   WebFlash config string : Ceiling-POE-VentIQ-FanTRIAC-RoomIQ
#   Build channel          : experimental
#   Artifact name          : Sense360-Ceiling-POE-VentIQ-FanTRIAC-RoomIQ-v1.0.0-experimental.bin
#   Governing decision     : docs/decisions/COMPLIANCE-001-RESOLUTION-001.md (experimental_lane)
#   Bench proof (attested) : docs/package-triac-001-operator-bench-proof.md
#
# ============================================================================
# EXPERIMENTAL SELF-BUILD MAINS — what this wrapper is, and is NOT
# ============================================================================
#   * EXPERIMENTAL self-build mains firmware. The S360-320 is an open-source
#     CERN-OHL-P board that Sense360 never sells, supplies, or places on the
#     market. Builders self-source and self-assemble the board, as
#     self-builders of their own devices, ENTIRELY AT THEIR OWN RISK. Mains
#     wiring must only be performed by a competent person. Incorrect
#     installation can cause fire, electric shock, or death.
#   * NEVER stable, NEVER recommended, NEVER a customer default, NEVER buyable,
#     NEVER in any kit or kit picker, NEVER in release_one_required_configs.
#     The stable Release-One target stays Ceiling-POE-VentIQ-RoomIQ; that
#     wrapper (products/webflash/ceiling-poe-ventiq-roomiq.yaml) is unchanged.
#   * NOT a safety / EMC / compliance certification. The PACKAGE-TRIAC-001
#     bench proof is firmware-build + functional bench evidence (operator-
#     attested) ONLY. Electrical safety, isolation, creepage, clearance, EMC,
#     and CE / UKCA conformity sit outside that bench and become assessable
#     obligations only via the COMPLIANCE-001 reopen trigger (a market-
#     placement act, which Sense360 never performs for this board).
#   * Downstream WebFlash one-click import stays gated by WF-IMPORT-TRIAC-001;
#     it is unblocked only once the experimental release is cut, and is admitted
#     in the WebFlash repo together with its advanced acknowledgement / warning
#     UI in one reviewed PR.
#
# ============================================================================
# WHAT IS PROVEN
# ============================================================================
#   * PACKAGE-TRIAC-001 operator bench protocol COMPLETE and operator-attested
#     (docs/package-triac-001-operator-bench-proof.md): zero-cross lock, gate
#     timing, leading-edge load waveform on the real Manrose inductive fan
#     motor, thermal soak, boot/stability, and the full-composition re-confirm
#     (closure path (a)) all PASS; the six-cell operator attestation is
#     recorded. Gate IO14 / zero-cross IO13 are the schematic-verified pins
#     (TRIAC-PINMAP-CORRECT-001); HW-005 buildability is resolved.
#   * This is functional bench + firmware-build proof only — NOT a safety, EMC,
#     or compliance claim of any kind.
# See docs/hardware/s360-320-r4-triac.md and docs/hardware/s360-100-r4-core.md.
# ============================================================================

packages:
  fantriac_experimental_self_build: !include ../sense360-ceiling-poe-ventiq-fantriac-roomiq.yaml

# ============================================================================
# Sense360 WebFlash wrapper — Bedroom PoE RoomIQ-only (STABLE, owner waiver).
# ============================================================================
# This file is the WebFlash-namespaced wrapper for the Bedroom PoE RoomIQ-only
# bundle (the smallest SELV RoomIQ-only PoE stack). It is a thin wrapper that
# re-exports the canonical product YAML so the WebFlash build matrix
# (config/webflash-builds.json) can address it via the products/webflash/
# namespace without duplicating package wiring.
#
# Canonical YAML (full substitutions, packages, board config):
#   products/sense360-ceiling-poe-roomiq.yaml
#   (a compat shim over products/bundles/ceiling-poe-roomiq.yaml)
#
# WebFlash config string : Ceiling-POE-RoomIQ
# Channel / version      : stable; the live version + artifact name are
#                          declared in config/webflash-builds.json (the sole
#                          release-eligibility source of truth).
#
# ============================================================================
# STATUS
# ============================================================================
#   * STABLE under an owner risk-acceptance waiver (HW-S360-410-WAIVER-2026-06),
#     promoted from preview at v1.0.5 (2026-06-08). The waiver records risk
#     acceptance, NOT hardware verification: the S360-410 PoE PSU stays
#     cataloged_unverified and the RoomIQ on-device flash test is not yet done.
#   * NOT recommended, NOT a customer default, NOT buyable. The Release-One
#     customer baseline stays Ceiling-POE-VentIQ-RoomIQ; the S360-KIT-BEDROOM-P
#     bundle stays hidden / not buyable.
#   * This wrapper declares NO version, NO channel, and NO artifact_name —
#     publication metadata lives only in config/webflash-builds.json and
#     config/product-catalog.json.
#
# History: authored as a preview candidate by
# RELEASE-PREVIEW-WEBFLASH-WRAPPERS-001 (hosted compile proof: run
# 26821900127); the reviewed preview build row was added by
# RELEASE-PREVIEW-WEBFLASH-BUILD-ROWS-001; promoted to stable v1.0.5 under the
# owner waiver (see config/product-catalog.json stable_blocker text).
# See docs/webflash-contract.md for the WebFlash artifact / grammar / token
# contract this wrapper satisfies.
# ============================================================================

packages:
  roomiq_preview_candidate: !include ../sense360-ceiling-poe-roomiq.yaml

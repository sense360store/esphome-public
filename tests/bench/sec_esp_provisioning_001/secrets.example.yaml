# ============================================================================
# Bench secrets template — SPIKE-PROVISIONING-BENCH-001 harness
# ============================================================================
# TEST-ONLY — NOT FOR RELEASE OR CUSTOMER USE
#
# Copy this file to secrets.yaml IN THIS DIRECTORY for the bench build:
#
#     cp tests/bench/sec_esp_provisioning_001/secrets.example.yaml \
#        tests/bench/sec_esp_provisioning_001/secrets.yaml
#
# secrets.yaml is gitignored at every depth (scripts/check-no-tracked-secrets.py
# enforces this). Fill in ISOLATED BENCH-NETWORK values only — never a
# production or home credential. Delete secrets.yaml when the bench is done
# (see README.md clean-up steps).
#
# Note there is deliberately NO api key, NO ota password, and NO fallback-AP
# password here: the empty-key / empty-password / open-AP compiled state IS
# the substrate under test. Those throwaway values are supplied at runtime
# via bench_host.py.
# ============================================================================

bench_wifi_ssid: "REPLACE_WITH_ISOLATED_BENCH_SSID"
bench_wifi_password: "REPLACE_WITH_ISOLATED_BENCH_WIFI_PASSWORD"

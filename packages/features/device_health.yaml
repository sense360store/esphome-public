---
# Device health monitoring and information
# Note: Uptime, WiFi Signal, Restart button, and Safe Mode button are provided by hardware configs
# This file provides additional diagnostic information. WiFi details are provided by hardware
# packages, so they are omitted here to avoid duplicate wifi_info sensors.

text_sensor:
  - platform: version
    name: "${friendly_name} ESPHome"

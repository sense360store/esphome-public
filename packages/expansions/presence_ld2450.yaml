# ============================================================================
# SENSE360 PRESENCE EXPANSION MODULE - LD2450 (Multi-Target)
# ============================================================================
# HLK-LD2450 24GHz mmWave Radar Sensor for presence detection
# - Multi-target tracking (up to 3 simultaneous targets)
# - Zone configuration support
# - Static vs moving target differentiation
# - UART interface at 256000 baud
#
# This file provides the base hardware setup. User-facing entities are defined
# in the feature profile files (basic or advanced).
#
# Expansion Bus Connection (RoomIQ J10 Hi-Link radar UART, ESP-perspective):
#   Pin 6 (Hi-Link_RX) → GPIO1 (ESP rx_pin, ESP receives from radar)
#   Pin 7 (Hi-Link_TX) → GPIO2 (ESP tx_pin, ESP transmits to radar)
#   Pin 3 (5V) → Power
#   Pin 1 (GND) → Ground
#
# Power Requirements: 5V / 150mA max
#
# NOTE: Uses native ESPHome LD2450 support (no external component required)
# ============================================================================

substitutions:
  # UART Configuration — RoomIQ J10 Hi-Link radar UART.
  # CORE-ABSTRACT-BUS-001C: was `uart_bus` (single, schematic-conflicting); now
  # uses the named Hi-Link UART defined by the Core abstract package. The
  # Hi-Link LD2450 radar runs at 256000 baud and the UART pin / direction /
  # baud_rate are owned by the Core package's `roomiq_hi_link_uart` block.
  ld2450_uart_id: roomiq_hi_link_uart
  ld2450_baud: "256000"

  # Presence detection settings
  presence_timeout: 30s

# ============================================================================
# LD2450 Radar Component (Native ESPHome)
# ============================================================================
ld2450:
  id: ld2450_radar
  uart_id: ${ld2450_uart_id}

# ============================================================================
# Binary Sensors - Presence Detection
# ============================================================================
binary_sensor:
  - platform: ld2450
    ld2450_id: ld2450_radar
    has_target:
      id: ld2450_occupancy
      name: "${friendly_name} Radar Occupancy"
      internal: true
    has_moving_target:
      id: ld2450_has_moving
      name: "${friendly_name} Moving Target"
      internal: true
    has_still_target:
      id: ld2450_has_still
      name: "${friendly_name} Still Target"
      internal: true

# ============================================================================
# Sensors - Target Tracking
# ============================================================================
sensor:
  - platform: ld2450
    ld2450_id: ld2450_radar
    target_count:
      id: ld2450_target_count
      name: "${friendly_name} Target Count"
      internal: true
    still_target_count:
      id: ld2450_still_target_count
      name: "${friendly_name} Still Target Count"
      internal: true
    moving_target_count:
      id: ld2450_moving_target_count
      name: "${friendly_name} Moving Target Count"
      internal: true
    # Target 1
    target_1:
      x:
        id: ld2450_t1_x
        name: "${friendly_name} Target 1 X"
        internal: true
      y:
        id: ld2450_t1_y
        name: "${friendly_name} Target 1 Y"
        internal: true
      speed:
        id: ld2450_t1_speed
        name: "${friendly_name} Target 1 Speed"
        internal: true
      distance:
        id: ld2450_t1_distance
        name: "${friendly_name} Target 1 Distance"
        internal: true
      angle:
        id: ld2450_t1_angle
        name: "${friendly_name} Target 1 Angle"
        internal: true
    # Target 2
    target_2:
      x:
        id: ld2450_t2_x
        name: "${friendly_name} Target 2 X"
        internal: true
      y:
        id: ld2450_t2_y
        name: "${friendly_name} Target 2 Y"
        internal: true
      speed:
        id: ld2450_t2_speed
        name: "${friendly_name} Target 2 Speed"
        internal: true
      distance:
        id: ld2450_t2_distance
        name: "${friendly_name} Target 2 Distance"
        internal: true
      angle:
        id: ld2450_t2_angle
        name: "${friendly_name} Target 2 Angle"
        internal: true
    # Target 3
    target_3:
      x:
        id: ld2450_t3_x
        name: "${friendly_name} Target 3 X"
        internal: true
      y:
        id: ld2450_t3_y
        name: "${friendly_name} Target 3 Y"
        internal: true
      speed:
        id: ld2450_t3_speed
        name: "${friendly_name} Target 3 Speed"
        internal: true
      distance:
        id: ld2450_t3_distance
        name: "${friendly_name} Target 3 Distance"
        internal: true
      angle:
        id: ld2450_t3_angle
        name: "${friendly_name} Target 3 Angle"
        internal: true

# ============================================================================
# Globals for Presence Detection
# ============================================================================
globals:
  - id: presence_confidence
    type: float
    restore_value: false
    initial_value: '0.0'

  # Closest target distance (for zone-based logic)
  - id: presence_closest_distance
    type: float
    restore_value: false
    initial_value: '0.0'

  # Number of people detected
  - id: presence_people_count
    type: int
    restore_value: false
    initial_value: '0'

  # Moving/still counts
  - id: ld2450_still_count
    type: int
    restore_value: false
    initial_value: '0'

  - id: ld2450_moving_count
    type: int
    restore_value: false
    initial_value: '0'

# Update presence confidence based on sensor data
interval:
  - interval: 1s
    then:
      - lambda: |-
          // LD2450 presence detection logic
          int targets = id(ld2450_target_count).state;
          int moving = id(ld2450_moving_target_count).state;
          int still = id(ld2450_still_target_count).state;

          id(ld2450_still_count) = still;
          id(ld2450_moving_count) = moving;

          // Update people count
          id(presence_people_count) = targets;

          if (targets > 0) {
            // Someone is present
            if (moving > 0) {
              // Movement detected - high confidence
              id(presence_confidence) = 0.95f;
            } else if (still > 0) {
              // Still presence - medium-high confidence
              id(presence_confidence) = 0.7f;
            } else {
              // Target detected but not classified
              id(presence_confidence) = 0.6f;
            }

            // Calculate closest target distance
            float min_dist = 9999.0f;
            float d1 = id(ld2450_t1_distance).state;
            float d2 = id(ld2450_t2_distance).state;
            float d3 = id(ld2450_t3_distance).state;

            if (!std::isnan(d1) && d1 > 0 && d1 < min_dist) min_dist = d1;
            if (!std::isnan(d2) && d2 > 0 && d2 < min_dist) min_dist = d2;
            if (!std::isnan(d3) && d3 > 0 && d3 < min_dist) min_dist = d3;

            if (min_dist < 9999.0f) {
              id(presence_closest_distance) = min_dist;
            }
          } else {
            // No one detected
            id(presence_confidence) = 0.1f;
            id(presence_closest_distance) = 0.0f;
          }

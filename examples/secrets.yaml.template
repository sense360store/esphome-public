# This template has moved.
#
# Use the root-level secrets.example.yaml instead:
#
#   cp secrets.example.yaml secrets.yaml
#
# Then edit secrets.yaml with your local credentials.
# Never commit secrets.yaml.

# S360-311-R4 PWM Fan Driver Pin / Package Mapping Audit (HW-PINMAP-311)

## Status

**Status: partial — schematic evidence available; package reconciliation pending.**

This document is the **HW-PINMAP-311** audit record for the
`Sense360 PWM` board, revision **R4** (`S360-311-R4`). It exists to
close the documentation gap recorded at
[`docs/hardware/hardware-artifact-policy.md` Follow-up PR sequence](hardware-artifact-policy.md#follow-up-pr-sequence)
and at
[`docs/hardware/board-readiness-matrix.md` Follow-up PR sequence #2](board-readiness-matrix.md#follow-up-pr-sequence),
which together call for a per-board pin/package mapping audit
**before** any `FanPWM`-bearing package, product, build, or
WebFlash work can proceed.

Unlike HW-PINMAP-310 (which is `pending — schematic/design evidence
required`), HW-PINMAP-311 has a committed module-side schematic to
work from: the curated artifact index
[`docs/hardware/artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md)
landed under HW-ASSETS-003 with the schematic PDF at
[`docs/hardware/schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf).
This audit consumes that evidence to record a schematic-backed
reconciliation view. It does **not** resolve the firmware-side
disagreements it surfaces — those are owed to separate,
evidence-bearing follow-up PRs.

## Purpose and scope

This document is **documentation only**. It does **not**:

- change any value in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json),
  [`config/product-catalog.json`](../../config/product-catalog.json),
  [`config/webflash-builds.json`](../../config/webflash-builds.json),
  or [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json),
- change any product YAML under [`products/`](../../products/),
  any WebFlash wrapper under
  [`products/webflash/`](../../products/webflash/), or any package
  YAML under [`packages/`](../../packages/),
- change any workflow under `.github/workflows/`, any script under
  [`scripts/`](../../scripts/), any test under
  [`tests/`](../../tests/), any component under `components/`, or any
  header under `include/`,
- mark `S360-311` `verified` — `schematic_status` stays
  `cataloged_unverified`,
- add or change `schematic_file` for `S360-311` in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json),
- mark the pin map confirmed, or mark
  [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
  as `confirmed-ok` — its status stays `package-yaml-pending`,
- resolve the package-vs-schematic mapping disagreement (the
  ceiling-Core abstract package binds `fan_pwm_pin` / `fan_tach_pin`
  to direct ESP32 expansion GPIOs while the Core schematic routes
  the corresponding `TachPMW*` / `Pul_Cou*` nets via the SX1509
  expander) — that resolution belongs to the systemic Core
  abstract-bus rebind owned by
  [`docs/release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)
  and to a future evidence-bearing HW-PINMAP-311 follow-up,
- promote `S360-311` to `preview` / `stable` / `production`,
- add a `FanPWM` token to any current or future Release-One config
  string,
- add a `FanPWM`-bearing entry to
  [`config/webflash-builds.json`](../../config/webflash-builds.json)
  or to any product / WebFlash wrapper,
- regenerate firmware, create a release, or change any WebFlash
  manifest / import,
- unblock FanTRIAC (`Ceiling-POE-VentIQ-FanTRIAC-RoomIQ` stays
  `status: blocked`, `blocker: HW-005`,
  `webflash_build_matrix: false`),
- change the mains-voltage compliance status of `S360-320` or
  `S360-400` (owned by COMPLIANCE-001;
  [`docs/compliance/mains-voltage-uk-eu-assessment.md`](../compliance/mains-voltage-uk-eu-assessment.md)),
- change the Release-One configuration
  `Ceiling-POE-VentIQ-RoomIQ` (`status: production`,
  `channel: stable`, artifact
  `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`, tag
  `v1.0.0`),
- change the LED preview entry `Ceiling-POE-VentIQ-RoomIQ-LED`
  (stays `status: preview`, `channel: preview`).

If this audit and any source-of-truth document drift, **the
source-of-truth document wins** and this audit must be updated. The
authoritative artifact-side record for this board is
[`docs/hardware/artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md);
the authoritative Core-side capture lives in
[`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md).

## Board identity

Mirrored from
[`config/hardware-catalog.json`](../../config/hardware-catalog.json)
lines 72–81 without modification.

| Field | Value |
|---|---|
| `group` | `Inline` |
| `type` | `Driver` |
| `friendly_name` | `Sense360 PWM` |
| `sku` | `S360-311` |
| `rev` | `R4` |
| `old_name` | `12vFan_PWM_PulseCounter` |
| `description` | `12V PWM fan driver, up to 4 fans with tach feedback.` |
| `schematic_status` | `cataloged_unverified` (unchanged by this PR) |
| `schematic_file` | _(not set; unchanged by this PR)_ |
| Curated artifact index | [`docs/hardware/artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md) (HW-ASSETS-003; landed) |
| Committed schematic PDF | [`docs/hardware/schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf) (HW-ASSETS-003; landed) |

## Repository evidence

### What IS in the repo today

- **Module-side schematic PDF.** Committed at
  [`docs/hardware/schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf)
  under HW-ASSETS-003 (byte-identical to the upload; SHA256 and
  upload inventory recorded in
  [`docs/hardware/artifacts/S360-311-R4.md` Checksums / file sizes](artifacts/S360-311-R4.md#checksums--file-sizes)).
- **Curated artifact index.**
  [`docs/hardware/artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md)
  records the schematic content (MT3608 `+5V`→`+12V` boost; four
  4-pin fan output connectors `J1` / `J2` / `J4` / `J5`; 13-pin
  module-side `J3` "From Core"; 4-pin Nextion display connector `J6`;
  mounting holes `H1..H4`) and the artifact-side opens
  ([§Open questions / verification needed](artifacts/S360-311-R4.md#open-questions--verification-needed)).
- **Core-side `J6` 13-pin connector capture.**
  [`docs/hardware/s360-100-r4-core.md` §J6](s360-100-r4-core.md#j6--12-v-pwm-fan-connector-13-pin)
  records the Core-side mate as `J6` (12 V PWM fan connector,
  13-pin) with the net list `+5V`, `GND`, `TachIO`, `TachPMW1..4`,
  `Pul_Cou1..4` and the **explicit `verify` flag** on the 1-to-13
  pin order (see
  [`s360-100-r4-core.md` Open Questions #9](s360-100-r4-core.md#open-questions--verification-needed)).
- **Core-side fan / driver outputs capture.**
  [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs)
  records the source pins: `TachPMW1..4` and `Pul_Cou1..4` originate
  at the **SX1509 (U3) I/O bank**, not directly at the ESP32;
  `TachIO` originates at ESP32 `IO16` and is a direct passthrough.
- **SX1509 channel map.**
  [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml)
  reserves SX1509 channels 0–3 for fan PWM outputs and channels
  4–7 for tachometer inputs (header comment, lines 1–10; output
  declarations at lines 25–44; binary-sensor declarations at lines
  97–140).
- **Module-side firmware package.**
  [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
  exists and binds `fan_pwm_pin: ${fan_pwm_pin}` and
  `fan_tach_pin: ${fan_tach_pin}` through the parent Core package
  (see
  [§Existing package abstraction](#existing-package-abstraction)).
- **Legacy 4-channel firmware package.**
  [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml)
  exists, used only by the legacy product YAML
  [`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml)
  (classified `legacy-compatible`; not WebFlash-shippable).
- **WebFlash compatibility token reservation.**
  [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json)
  line 12 reserves `FanPWM` as a `canonical_modules` token, subject
  to the fan-driver `max-one-of` rule enforced by
  `FAN_DRIVER_TOKENS` in
  [`tests/validate_webflash_builds.py`](../../tests/validate_webflash_builds.py).
- **Existing classifications.**
  [`docs/hardware/board-readiness-matrix.md` `S360-311` row](board-readiness-matrix.md#s360-311-sense360-pwm)
  — `partially-documented`, `not-needed-for-release-one`;
  `package-yaml-pending`.
  [`docs/hardware/remaining-board-documentation-audit.md` Sense360 PWM (`S360-311`)](remaining-board-documentation-audit.md#sense360-pwm-s360-311)
  — same classification.
  [`docs/product-availability-taxonomy.md` Current board / product snapshot](../product-availability-taxonomy.md#current-board--product-snapshot)
  line 587 — `partially-documented` + `design-pending`.

### What is NOT in the repo today

- **No standalone schematic-backed reference doc** in the
  `docs/hardware/sXXX-r4-<role>.md` pattern before this PR.
  (Comparable docs at
  [`s360-100-r4-core.md`](s360-100-r4-core.md),
  [`s360-200-r4-roomiq.md`](s360-200-r4-roomiq.md),
  [`s360-210-r4-airiq.md`](s360-210-r4-airiq.md),
  [`s360-211-r4-ventiq.md`](s360-211-r4-ventiq.md),
  [`s360-300-r4-led.md`](s360-300-r4-led.md) exist for verified
  boards.) This PR is an **audit / reconciliation record**, not a
  schematic-backed reference doc rewrite — the rewrite belongs to
  the HW-PINMAP-311 follow-up.
- **No KiCad source / KiCad PCB / project metadata / BOM / CPL /
  gerbers / drill files / STEP / board images** for
  `S360-311-R4`; see
  [`docs/hardware/artifacts/S360-311-R4.md` Files NOT provided in this upload](artifacts/S360-311-R4.md#files-not-provided-in-this-upload).
- **No silkscreen / bench evidence** for either the Core-side `J6`
  or the module-side `J3` pin order.
- **No active product YAML** consumes `S360-311`. The legacy
  [`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml)
  is `legacy-compatible` (pre-WebFlash standalone fan board; uses
  the 4-channel
  [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml),
  not the single-channel
  [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)).
- **No WebFlash wrapper** under
  [`products/webflash/`](../../products/webflash/) targets FanPWM.
- **No entry in
  [`config/webflash-builds.json`](../../config/webflash-builds.json)**
  consumes the `FanPWM` token. The two committed builds are
  `Ceiling-POE-VentIQ-RoomIQ` (`stable`) and
  `Ceiling-POE-VentIQ-RoomIQ-LED` (`preview`).
- **No release artifact** for any `FanPWM`-bearing config.
- **No WebFlash import** for any `FanPWM`-bearing config.

## Schematic summary

This section restates only what is directly visible in the
committed schematic PDF
[`docs/hardware/schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf)
and inventoried in
[`docs/hardware/artifacts/S360-311-R4.md` What the schematic appears to contain](artifacts/S360-311-R4.md#what-the-schematic-appears-to-contain).
It does **not** invent electrical characteristics, transistor
topology, timing guarantees, MOSFET specs, or pull-up assumptions
beyond what is labelled on the sheet.

- **Voltage Boost** section. `MT3608` boost converter (`U1`) with
  input inductor `L1 22 µH`, Schottky `D1 SS34`, feedback divider
  `R3 38 kΩ` / `R5 2 kΩ`, input cap `C1 22 µF / 10 V`, output cap
  `C2 22 µF / 10 V`, output indicator `R4 330 Ω` + blue LED `D2`.
  Boosts the Core-supplied `+5V` rail up to `+12V` for the fan
  outputs.
- **Fan outputs section** (labelled `"NINE 4pin FANs"` in the sheet
  title; see [§Open documentation questions](#open-documentation-questions)).
  **Four** 4-pin fan output connectors are present: `J1`, `J2`,
  `J4`, `J5`. Each fan connector has pins `+12V`, `GND`,
  `Pul_Cou*`, `TachPMW*` and is paired with one low-side N-channel
  MOSFET (`Q_NMOS_GSD` — `Q1..Q4`) on the `TachPMW*` gate path,
  with a 1 kΩ gate resistor (`R1`, `R2`, `R6`, `R7`); drain wired
  to the shared `TachIO` net; source to `GND`.
- **"From Core"** module-side connector `J3` — 13-pin. Pin / net
  table as labelled in the PDF:

  | Module-side `J3` pin | Net |
  |---:|---|
  | 1 | `+5V` |
  | 2 | `TachIO` |
  | 3 | `TachPMW1` |
  | 4 | `Pul_Cou1` |
  | 5 | `TachPMW2` |
  | 6 | `Pul_Cou2` |
  | 7 | `TachPMW3` |
  | 8 | `Pul_Cou3` |
  | 9 | `TachPMW4` |
  | 10 | `Pul_Cou4` |
  | 11 | `UART_RX` |
  | 12 | `UART_TX` |
  | 13 | `GND` |

- **"NEXTION DISPLAY"** connector `J6` (module-side) — 4-pin:
  `+5V`, `ESP32_TXD` / `UART_TX` (to display RX), `ESP32_RXD` /
  `UART_RX` (to display TX), `GND`. On-board route from module
  `J3` pins 11 / 12 to module `J6` UART pair.
- **Mounting holes** `H1`, `H2`, `H3`, `H4`.

Everything beyond the list above — module dimensions, exact
component values not printed on the visible sheet, on-board PCB
silkscreen, layer stackup, fab notes, harness identity, or any
electrical timing claim — is **unknown** and would have to come
from the retained-but-not-committed KiCad source / KiCad PCB / BOM
/ CPL / gerbers / drill / STEP / board images per
[`hardware-artifact-policy.md`](hardware-artifact-policy.md).

## Connector / pin-map findings

### Module-side `J3` ↔ Core-side `J6` (13-pin harness)

The two connectors mate via a 13-pin Core ↔ module harness. The
module-side capture (above) is **schematic-confirmed** against the
committed S360-311-R4 PDF. The Core-side capture in
[`s360-100-r4-core.md` §J6](s360-100-r4-core.md#j6--12-v-pwm-fan-connector-13-pin)
records the net list but explicitly marks the 1-to-13 pin order as
**verify** against the silkscreen (
[`s360-100-r4-core.md` Open Questions #9](s360-100-r4-core.md#open-questions--verification-needed)).

| Element | Core side (`J6`) | Module side (`J3`) | Reconciliation status |
|---|---|---|---|
| Connector identity | `J6` — 12 V PWM fan connector (13-pin) — [§J6](s360-100-r4-core.md#j6--12-v-pwm-fan-connector-13-pin) | `J3` — "From Core" (13-pin) — schematic-confirmed | Naming mismatch is expected (each board labels its own connectors); 13-pin pin count matches. |
| Net list (signal nets only) | `+5V`, `GND`, `TachIO`, `TachPMW1..4`, `Pul_Cou1..4` | `+5V`, `GND`, `TachIO`, `TachPMW1..4`, `Pul_Cou1..4`, **`UART_RX`**, **`UART_TX`** | **Module side carries two additional nets (`UART_RX` / `UART_TX`) that the Core-side capture does not currently document.** See [§UART pins on `J3` pins 11–12](#uart-pins-on-j3-pins-11-12). |
| 1-to-13 pin order | **`verify`** against silkscreen — [`s360-100-r4-core.md` Open Questions #9](s360-100-r4-core.md#open-questions--verification-needed) | Schematic-only — order as labelled in the PDF (`+5V` → `TachIO` → `TachPMW1` → `Pul_Cou1` → … → `Pul_Cou4` → `UART_RX` → `UART_TX` → `GND`) | Schematic-only on the module side; **requires silkscreen verification** to confirm the harness wires `+5V` (Core J6) to `+5V` (Module J3 pin 1), and so on through to `GND` (Module J3 pin 13). Resolution belongs to the HW-PINMAP-311 follow-up + bench evidence. |

### UART pins on `J3` pins 11–12

The module-side `J3` labels pins 11 / 12 as `UART_RX` / `UART_TX`
and routes them on-board to the 4-pin Nextion display connector
`J6` (module side). The Core-side `J6` capture in
[`s360-100-r4-core.md` §J6](s360-100-r4-core.md#j6--12-v-pwm-fan-connector-13-pin)
does **not** currently list any UART pins on the 13-pin connector
— only `+5V`, `GND`, `TachIO`, and the eight `TachPMW*` /
`Pul_Cou*` signals (11 nets total) are recorded. Two
schematic-internally-consistent possibilities remain, **both in
scope for the HW-PINMAP-311 follow-up**:

1. **The Core-side `J6` capture is incomplete.** Pins 11 / 12 on
   the Core-side `J6` connector physically carry the UART pair
   (e.g. from `TXD0` / `RXD0` or a different ESP32 UART), and the
   capture in
   [`s360-100-r4-core.md` §J6](s360-100-r4-core.md#j6--12-v-pwm-fan-connector-13-pin)
   must be extended to record those two nets.
2. **The Nextion UART pair is delivered to the module via a
   separate harness, not via the 13-pin Core `J6` cable.** Pins
   11 / 12 on module-side `J3` would then be expected to be left
   unconnected (or to host different signals) on the actual
   13-pin cable. In this case the module-side schematic labels
   are misleading and the on-board Nextion route depends on a
   second harness that is not yet documented.

This audit records the discrepancy. It does **not** edit
[`s360-100-r4-core.md`](s360-100-r4-core.md) and does **not**
choose between the two possibilities.

### Per-fan-output drive topology (module side)

The module-side schematic places one low-side N-channel MOSFET
(`Q_NMOS_GSD` — `Q1..Q4`) per fan output, with a 1 kΩ gate
resistor (`R1`, `R2`, `R6`, `R7`) on the `TachPMW*` gate path.
Drain is tied to the shared `TachIO` net; source to `GND`. The
4-pin fan-output connectors (`J1`, `J2`, `J4`, `J5`) each carry
`+12V`, `GND`, `Pul_Cou*`, `TachPMW*`.

This is recorded as **observed schematic topology**, not as a
verified design intent. The schematic does not state:

- whether `TachPMW*` is the PWM drive signal or only the
  open-drain pull-down for a tach-share scheme (the net name is
  ambiguous on its own),
- whether the per-fan `Pul_Cou*` pin in the connector is a direct
  tach feedback from the fan that the module passes through to the
  Core, or whether some additional buffering / level shifting is
  present on the module side,
- the per-fan rated current, the MOSFET part number's actual
  ratings (the symbol is `Q_NMOS_GSD`, not a populated part
  number), or any thermal characterisation.

Bench / harness / waveform evidence is **owed** to the
HW-PINMAP-311 follow-up.

### Open documentation questions

- **`"NINE 4pin FANs"` section title.** The schematic sheet's
  fan-section title reads `"NINE 4pin FANs"`, but only **four**
  4-pin fan output connectors are visible on the sheet (`J1`,
  `J2`, `J4`, `J5`) — matching the hardware-catalog description
  `12V PWM fan driver, up to 4 fans with tach feedback.`. Whether
  this title is a stale label copied from a different design, a
  marker / project code, or an undelivered intent is **unresolved**
  and is not a wiring change.
- **Date / Title / Rev fields blank.** The KiCad title-block date,
  title, and revision fields are blank in the committed PDF — see
  [`docs/hardware/artifacts/S360-311-R4.md` Uploaded artifact inventory](artifacts/S360-311-R4.md#uploaded-artifact-inventory).
  Provenance is `S360-311-R4.kicad_sch` (per the title-block file
  field), but no commit / revision metadata is recorded on the
  sheet.

## Existing package abstraction

This section audits how
[`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
binds its PWM and tachometer signals, and what those bindings
resolve to under the current Core abstract-bus packages.

### `packages/expansions/fan_pwm.yaml`

Found at
[`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml).
Single-channel PWM fan package; not used by any current product
YAML.

| Field | Value as found |
|---|---|
| Logical role | `FanPWM` (single-channel 25 kHz PWM fan with one tachometer feedback line) |
| Schematic-backed | **Partially.** The module-side schematic is committed under HW-ASSETS-003; the package is **not** reconciled against it. |
| PWM output pin | `output.platform: ledc`, `pin: ${fan_pwm_pin}` — file line 45. |
| Tachometer input pin | `sensor.platform: pulse_counter`, `pin.number: ${fan_tach_pin}` — file line 67. |
| Source of `fan_pwm_pin` / `fan_tach_pin` | Parent Core package (see [§Parent Core packages](#parent-core-packages-that-resolve-fan_pwm_pin--fan_tach_pin)). Comment block at lines 22–29 states the substitutions MUST be provided by the parent board file. |
| PWM frequency | `fan_pwm_frequency: "25000"` — file line 32 (25 kHz standard PC fan PWM). |
| Channel count | **Single.** One `fan` entity, one tach sensor. Does not bind any of the per-fan `TachPMW1..4` / `Pul_Cou1..4` nets shown on the module schematic. |
| Reconciliation against module-side schematic | **Pending.** See [§Reconciliation findings](#reconciliation-findings). |
| Current status | `package-yaml-pending`. **Not** `confirmed-ok`. |

This file is **not edited** by this PR.

### `packages/expansions/sense360_fan_pwm.yaml` (legacy 4-channel)

Found at
[`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml).
Four-channel variant used only by the legacy
`legacy-compatible` product YAML
[`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml).

| Field | Value as found |
|---|---|
| Logical role | `FanPWM` (legacy four-channel) — four `fan` entities |
| PWM output pins | `fan1_pwm_pin: GPIO7`, `fan2_pwm_pin: GPIO11`, `fan3_pwm_pin: GPIO13`, `fan4_pwm_pin: GPIO15` — file lines 40–43. |
| Tachometer input pins | `fan1_tach_pin: GPIO8`, `fan2_tach_pin: GPIO12`, `fan3_tach_pin: GPIO14`, `fan4_tach_pin: GPIO16` — file lines 46–49. |
| Shared tach / control pin | `tach_io_pin: GPIO6` — file line 52. |
| Nextion UART pins | `nextion_tx_pin: GPIO43`, `nextion_rx_pin: GPIO44` — file lines 55–56. |
| Reconciliation against module-side schematic | **Pending.** Direct ESP32 GPIO bindings do not match the module-side schematic, which routes per-fan PWM / tach through the Core-side `TachPMW*` / `Pul_Cou*` nets (which themselves originate on the SX1509 expander per [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs)). |
| Current status | Consumed only by `legacy-compatible` product YAML; **not WebFlash-shippable**; subject to the legacy-compatible policy in [`docs/cleanup-audit.md`](../cleanup-audit.md) and [`docs/webflash-contract.md`](../webflash-contract.md). |

This file is **not edited** by this PR.

### Parent Core packages that resolve `fan_pwm_pin` / `fan_tach_pin`

The `fan_pwm.yaml` package inherits `${fan_pwm_pin}` and
`${fan_tach_pin}` from whichever Core package is included by the
parent product YAML. The two Core abstract packages in this repo
bind these substitutions to **direct ESP32 expansion GPIOs**, not
to the SX1509-routed `TachPMW*` / `Pul_Cou*` paths the schematic
describes.

| Source | File | Line | Substitution | Resolved value | Resolved to | Status against the Core schematic |
|---|---|---|---|---|---|---|
| Ceiling Core abstract | [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) | 65 | `expansion_gpio1` | `GPIO5` | direct ESP32 GPIO | `IO5 = SEN0609_TX` (RoomIQ radar UART) per [`s360-100-r4-core.md` §Pin assignments](s360-100-r4-core.md#pin-assignments). |
| Ceiling Core abstract | [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) | 66 | `expansion_gpio2` | `GPIO6` | direct ESP32 GPIO | `IO6 = out(gpio6)` per [`s360-100-r4-core.md` §Pin assignments](s360-100-r4-core.md#pin-assignments). |
| Ceiling Core abstract | [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) | 72 | `fan_pwm_pin` | `${expansion_gpio1}` (i.e. `GPIO5`) | direct ESP32 GPIO | **Disagrees with the Core schematic.** The schematic's per-fan PWM-style signals `TachPMW1..4` originate at the **SX1509 (U3) I/O bank**, not directly at the ESP32 — [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs). The direct-ESP32 `TachIO` net is on ESP32 `IO16` (a single net), not `GPIO5`. |
| Ceiling Core abstract | [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) | 73 | `fan_tach_pin` | `${expansion_gpio2}` (i.e. `GPIO6`) | direct ESP32 GPIO | **Disagrees with the Core schematic.** The schematic's per-fan tach signals `Pul_Cou1..4` originate at the **SX1509 (U3) I/O bank**, not directly at the ESP32. |
| Generic Core abstract | [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) | 57–58 | `expansion_gpio1` / `expansion_gpio2` | `GPIO4` / `GPIO5` | direct ESP32 GPIO | `IO4 = SEN0609_RX` and `IO5 = SEN0609_TX` per Core schematic. **Disagrees with the schematic** for any fan-PWM use. |
| Core schematic / HW-009 | [`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md) | 95, 277–279 | `TachPMW1..4` / `Pul_Cou1..4` / `TachIO` | SX1509 channels 0–3 (PWM) / 4–7 (tach) / ESP32 `IO16` | SX1509 expander + one direct ESP32 pin | This is the only schematic-backed evidence in the repo for the per-fan PWM / tach paths and for the shared `TachIO` passthrough. |
| SX1509 channel map | [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml) | 1–10, 25–44, 97–140 | `sx1509_pwm_fan_0..3` on SX1509 pins 0–3; `sx1509_tach_0..3` on SX1509 pins 4–7 | SX1509 channels | SX1509 expander | **Matches the Core schematic** for the `TachPMW*` / `Pul_Cou*` source. Note: this package declares four named `fan` entities driven from the SX1509, but `fan_pwm.yaml` declares an independent single-channel `fan` entity driven from `${fan_pwm_pin}`. The two paths exist side by side; no current product YAML wires them together against the FanPWM board. |

**HW-PINMAP-311 does not resolve this disagreement.** Both Core
abstract packages bind FanPWM to direct ESP32 GPIOs while the
verified Core schematic routes the corresponding per-fan signals
through the SX1509 expander. Resolution requires:

1. The HW-PINMAP-311 follow-up (a standalone schematic-backed
   reference doc that ties the module-side `J3`, the Core-side
   `J6`, the SX1509 channel map, and the FanPWM package bindings
   into one explicit pin map).
2. The systemic Core abstract-bus rebind PR(s) that re-anchor
   `sense360_core_ceiling.yaml` and `sense360_core.yaml` against
   the verified Core schematic, per
   [`docs/release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups).
3. A PACKAGE-GAP-001 FanPWM-slice decision that chooses whether
   `packages/expansions/fan_pwm.yaml` continues to bind a single
   direct-ESP32 pin pair (treating FanPWM as a single-channel
   abstraction) or moves to bind the per-fan SX1509 channels (and
   the direct-ESP32 `TachIO` passthrough) explicitly.

This PR records the disagreement and points to the future PRs
that must close it; it does not edit either Core abstract package,
does not edit `fan_pwm.yaml`, and does not choose `GPIO5` / `GPIO6`
vs `GPIO4` / `GPIO5` vs SX1509 channels 0–7 + ESP32 `IO16`.

## Reconciliation findings

This is the heart of HW-PINMAP-311: a per-element classification
of where the schematic, the Core-side capture, the Core abstract
packages, the FanPWM package, and the SX1509 channel map are
consistent versus where they require future reconciliation.

Every "requires future package reconciliation" entry below names
the future PR that owns the resolution. **No entry says "should be
changed now."**

| Element | Source(s) | Status | Owner if reconciliation is needed |
|---|---|---|---|
| Module-side `J3` 13-pin pinout (schematic capture) | `S360-311-R4.pdf` + [`artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md) | **Schematic-confirmed** on the module side. Pin order is schematic-only until silkscreen verification. | HW-PINMAP-311 follow-up + bench evidence (silkscreen). |
| Core-side `J6` 13-pin pinout (net list) | [`s360-100-r4-core.md` §J6](s360-100-r4-core.md#j6--12-v-pwm-fan-connector-13-pin) | **Schematic-confirmed** on the Core side for the 11 fan / power nets; **does not currently list** the `UART_RX` / `UART_TX` pair that the module-side `J3` labels on pins 11 / 12. | HW-PINMAP-311 follow-up (decide whether to extend the Core capture or document the second-harness alternative). |
| Core-side `J6` 1-to-13 pin order | [`s360-100-r4-core.md` Open Questions #9](s360-100-r4-core.md#open-questions--verification-needed) | **`verify` against silkscreen.** Pre-existing flag, not introduced here. | HW-PINMAP-311 follow-up + S360-100-BENCH-001 per [`s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status). |
| Module-side `J6` Nextion 4-pin connector | `S360-311-R4.pdf` + [`artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md) | **Schematic-confirmed** on the module side; on-board route from module `J3` pins 11 / 12 → module `J6` UART pair. | No firmware-side package currently consumes a Nextion display through a FanPWM module; if one is added, package reconciliation would be required. |
| `TachIO` net source | [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs) line 279; module-side schematic ties `TachIO` to the shared MOSFET drain rail | ESP32 `IO16` (direct passthrough) on the Core side; aggregated MOSFET drain on the module side. **Schematic-confirmed** on both sides. | No firmware-side reconciliation owed at this layer. (`fan_pwm.yaml` does not currently bind a separate `tach_io_pin` substitution.) |
| `TachPMW1..4` net source | [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs) line 277 — SX1509 (U3) I/O bank | SX1509 channels 0–3 per [`gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml). **Schematic-confirmed** on the Core side. | **Requires future package reconciliation.** [`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) binds a single `${fan_pwm_pin}` to a direct ESP32 GPIO, not to the SX1509 channels. Owner: PACKAGE-GAP-001 FanPWM slice. |
| `Pul_Cou1..4` net source | [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs) line 278 — SX1509 (U3) I/O bank | SX1509 channels 4–7 per [`gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml). **Schematic-confirmed** on the Core side. | **Requires future package reconciliation.** [`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) binds a single `${fan_tach_pin}` to a direct ESP32 GPIO, not to the SX1509 channels. Owner: PACKAGE-GAP-001 FanPWM slice. |
| Ceiling-Core `expansion_gpio1` / `expansion_gpio2` resolution | [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) lines 65–66 | `GPIO5` / `GPIO6`. **Disagrees** with the Core schematic (`IO5 = SEN0609_TX`, `IO6 = out(gpio6)`). Belongs to the systemic Core abstract-bus mismatch. | Systemic Core abstract-bus rebind per [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups). Not owned by HW-PINMAP-311. |
| Generic-Core `expansion_gpio1` / `expansion_gpio2` resolution | [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) lines 57–58 | `GPIO4` / `GPIO5`. **Disagrees** with the Core schematic. Same systemic mismatch. | Systemic Core abstract-bus rebind. Not owned by HW-PINMAP-311. |
| `fan_pwm.yaml` PWM polarity | [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) lines 42–46 | `ledc` output at 25 kHz. **Schematic-side polarity is not verified** — the module-side N-FET gate path with 1 kΩ series resistor on the `TachPMW*` net suggests low-side switching, but no schematic annotation states whether the PWM source must be active-high or active-low to turn the fan on. | **Requires future package reconciliation** (bench / waveform evidence). Owner: HW-PINMAP-311 follow-up. |
| `fan_pwm.yaml` tachometer polarity / pull-up | [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) lines 64–80 | `pulse_counter` with `mode: input: true, pullup: true`. The schematic does not show a discrete pull-up on the `Pul_Cou*` nets at the module side — the resistor would be either inside the SX1509 (channel pull-ups), on the fan, or unstated. | **Requires future package reconciliation**. Owner: HW-PINMAP-311 follow-up + bench evidence. |
| `fan_pwm.yaml` per-fan vs single-channel abstraction | [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) (single `fan` entity) vs `S360-311-R4.pdf` (four 4-pin fan output connectors `J1` / `J2` / `J4` / `J5`) | **Mismatch in cardinality.** The board exposes up to four fans; `fan_pwm.yaml` binds one. The legacy [`sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml) binds four (but with direct ESP32 GPIOs that also disagree with the schematic). | **Requires future package reconciliation.** Owner: PACKAGE-GAP-001 FanPWM slice — decide whether the canonical `FanPWM` token implies single-channel or four-channel firmware. |
| MT3608 boost converter | `S360-311-R4.pdf` | Schematic-confirmed `+5V` → `+12V` boost on the module side. The Core-side `J6` capture lists `+5V` (not `+12V`) at the connector — so the boost is local to the FanPWM module, not on the Core. | No firmware-side reconciliation owed. |
| `"NINE 4pin FANs"` section title | `S360-311-R4.pdf` title block on the fan-section grouping | **Inconsistent** with the four visible fan-output connectors and with the hardware-catalog description (`up to 4 fans`). Recorded as an open documentation question. | Owner: HW-PINMAP-311 follow-up (documentation-only). |
| Date / Title / Rev fields on the schematic | `S360-311-R4.pdf` title block | **Blank.** Provenance is the title-block file field (`S360-311-R4.kicad_sch`); no commit / revision metadata is on the sheet. | Owner: HW-ASSETS-003-equivalent re-ingest if KiCad source / project metadata is ever supplied. |

The classification above does **not** change any package, product,
WebFlash wrapper, build, release, or catalog status.

## Known unresolved issues

The list below is the full evidence-gap inventory for HW-PINMAP-311
after this PR. Resolving any of these items belongs to a separate,
evidence-bearing follow-up — **not** to this PR.

- **Systemic Core abstract-bus mismatch.** Both Core abstract
  packages
  ([`sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml),
  [`sense360_core.yaml`](../../packages/hardware/sense360_core.yaml))
  bind `expansion_gpio1` / `expansion_gpio2` to direct ESP32 GPIOs
  whose schematic identity ([`s360-100-r4-core.md` §Pin assignments](s360-100-r4-core.md#pin-assignments))
  is `SEN0609_TX` / `SEN0609_RX` / `out(gpio6)` / radar-UART pins,
  not fan-driver pins. Resolution is owed to
  [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups).
- **No bench verification.** No bench session has exercised a
  populated `S360-311-R4` board, no operator / reviewer is on file,
  no measurement of the fan-PWM waveform, the tach-input waveform,
  or the MOSFET drive path has been recorded.
- **No silkscreen verification.** Neither the Core-side `J6`
  1-to-13 silkscreen pin order ([`s360-100-r4-core.md` Open
  Questions #9](s360-100-r4-core.md#open-questions--verification-needed))
  nor the module-side `J3` 1-to-13 silkscreen pin order has been
  verified against a physical board.
- **No harness verification.** No physical Core-to-module 13-pin
  cable has been inspected; the conductor-by-conductor mapping
  from Core `J6` pin N to module `J3` pin N is schematic-only.
- **No KiCad source / KiCad PCB / project metadata.** None of
  `S360-311-R4.kicad_sch` / `S360-311-R4.kicad_pcb` /
  `S360-311-R4.kicad_pro` / `S360-311-R4.kicad_prl` /
  `fp-lib-table` / `sym-lib-table` was supplied to HW-ASSETS-003.
  See
  [`docs/hardware/artifacts/S360-311-R4.md` Files NOT provided in this upload](artifacts/S360-311-R4.md#files-not-provided-in-this-upload).
- **No BOM.** No procurement / part-identification evidence for
  the MT3608, the four `Q_NMOS_GSD` MOSFETs (no manufacturer / part
  number / V<sub>DSS</sub> / R<sub>DS(on)</sub> / I<sub>D</sub> /
  thermal data), the SS34, the inductor / capacitors, the Nextion
  display variant.
- **No CPL / pick-and-place.** No assembly placement evidence.
- **No board photography.** No top-side or bottom-side photo /
  render; no annotated silkscreen capture.
- **No tach waveform verification.** No oscilloscope capture of
  the `Pul_Cou*` or `TachPMW*` signals on either side of the
  connector; no measurement of duty / frequency / level shifting.
- **No operator validation.** No customer-facing or operator
  validation of any FanPWM-bearing config string (none exists; see
  [§Product / WebFlash status](#product--webflash-status)).
- **No standalone schematic-backed reference doc.** No
  `docs/hardware/s360-311-r4-fanpwm.md` or equivalent in the
  per-board reference pattern of
  [`s360-200-r4-roomiq.md`](s360-200-r4-roomiq.md) /
  [`s360-211-r4-ventiq.md`](s360-211-r4-ventiq.md) /
  [`s360-300-r4-led.md`](s360-300-r4-led.md). The HW-PINMAP-311
  follow-up owes this doc.

## Package YAML status

**Status: `needs-package-reconciliation`.** Not `confirmed-ok`.
Not `needs-package-change` (a definitive change requires
schematic-backed reconciliation we have not yet performed — the
schematic exists but the cross-side pin map and the
single-channel-vs-four-channel decision have not been made). Not
`blocked` (no named blocker prevents the work — it just has not
been done).

[`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
stays as-is. This PR does not edit it.

The package's status will move only after:

1. The HW-PINMAP-311 follow-up produces a standalone
   schematic-backed reference doc that ties the module-side `J3`,
   the Core-side `J6`, the SX1509 channel map, and the FanPWM
   package bindings into one explicit pin map.
2. The Core-side `J6` 1-to-13 silkscreen verification (Open
   Question #9) is closed.
3. The Core-side `J6` ↔ module-side `J3` UART-on-pins-11/12
   question is closed.
4. PACKAGE-GAP-001 (per
   [`board-readiness-matrix.md` Follow-up PR sequence #7](board-readiness-matrix.md#follow-up-pr-sequence))
   chooses an action — confirm, change, or defer — and that
   decision is recorded in
   [`firmware-package-mapping-audit.md`](firmware-package-mapping-audit.md)
   under the existing six-label taxonomy.

## Product / WebFlash status

**Unchanged.** This PR does not promote, demote, add, or remove any
product, WebFlash wrapper, build, release, or import.

Availability snapshot for `S360-311` after this PR (same as before
this PR):

- Hardware-evidence axis: `cataloged-unverified` JSON +
  `partially-documented` audit (Core-side `J6` 13-pin connector
  captured; module-side schematic now committed under HW-ASSETS-003;
  pin-order `verify` flag open).
- Pin-map axis: `pin-map-pending` (HW-PINMAP-311 follow-up owes the
  standalone reference doc).
- Package-YAML axis: `package-yaml-pending` /
  `needs-package-reconciliation`.
- Product-YAML axis: no active product consumes the board. The
  legacy [`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml)
  is `legacy-compatible` (pre-WebFlash standalone fan board; uses
  the legacy four-channel
  [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml),
  not the single-channel
  [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)).
  `firmware-missing` for any current FanPWM-bearing config.
- WebFlash-availability axis: `firmware-build-pending` /
  `design-pending` for any FanPWM-bearing config.
- WebFlash wizard treatment: must show any FanPWM-bearing selection
  as `design-pending — not buildable today`, per
  [`docs/product-availability-taxonomy.md` How WebFlash should consume this taxonomy](../product-availability-taxonomy.md#how-webflash-should-consume-this-taxonomy).
  This audit does not edit any WebFlash-side code.

Release-One stays `Ceiling-POE-VentIQ-RoomIQ` on `stable` with
artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`
and tag `v1.0.0`. The LED preview entry
`Ceiling-POE-VentIQ-RoomIQ-LED` stays `status: preview`,
`channel: preview`. FanTRIAC
(`Ceiling-POE-VentIQ-FanTRIAC-RoomIQ`) stays `status: blocked`,
`blocker: HW-005`, `webflash_build_matrix: false`. Mains-voltage
compliance for `S360-320` and `S360-400` (COMPLIANCE-001) is
unchanged. The fan-driver `max-one-of` rule in
[`config/webflash-compatibility.json`](../../config/webflash-compatibility.json)
continues to apply: any future `FanPWM`-bearing config is mutually
exclusive with `FanRelay` / `FanDAC` / `FanTRIAC`.

## Required evidence before promotion

A future PR that wants to close HW-PINMAP-311 and unlock any
downstream package / product / WebFlash work must supply, at
minimum:

1. **Bench verification.** Populated `S360-311-R4` board on a
   `S360-100-R4` Core, with operator / reviewer / serial recorded,
   and oscilloscope captures of:
   - the per-fan PWM drive waveform at the module-side fan-output
     connector pins (`TachPMW*` on `J1` / `J2` / `J4` / `J5`),
   - the per-fan tach waveform at the module-side fan-output
     connector pins (`Pul_Cou*`),
   - the shared `TachIO` net (open-drain behaviour, idle level,
     transition timing).
2. **Connector / harness validation.** Silkscreen reading of the
   Core-side `J6` 1-to-13 pin order (resolves
   [`s360-100-r4-core.md` Open Questions #9](s360-100-r4-core.md#open-questions--verification-needed)),
   silkscreen reading of the module-side `J3` 1-to-13 pin order,
   and an inspection of the physical 13-pin Core ↔ module harness
   (conductor-by-conductor pin mapping).
3. **Tach verification.** Confirmation of the pulses-per-revolution
   factor used by [`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
   lines 76–80 (`multiply: 0.5`, "Standard fans output 2 pulses per
   revolution") against a real fan via the SX1509 path and / or the
   direct-ESP32 `TachIO` path.
4. **UART-on-`J3`-pins-11/12 resolution.** Choose between (a)
   extending the Core-side `J6` capture to record `UART_RX` /
   `UART_TX` at pins 11 / 12, or (b) documenting the separate
   harness that actually delivers the Nextion UART pair to the
   module.
5. **Package reconciliation.** Standalone schematic-backed
   reference doc (HW-PINMAP-311 follow-up) that ties together the
   module-side `J3`, the Core-side `J6`, the SX1509 channel map
   ([`gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml)),
   the direct-ESP32 `TachIO` passthrough, and the FanPWM package
   bindings, with one explicit pin map per fan output.
6. **Schematic-status promotion PR (separate, JSON-only).** Flips
   `schematic_status` for `S360-311` from `cataloged_unverified`
   to `verified` and sets `schematic_file` to
   `docs/hardware/schematics/S360-311-R4.pdf` in
   [`config/hardware-catalog.json`](../../config/hardware-catalog.json).
7. **Package implementation PR (PACKAGE-GAP-001 FanPWM slice).**
   Reconciles
   [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
   (and the legacy
   [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml),
   if retained) against the reconciled pin map. Depends on the
   systemic Core abstract-bus rebind landing first (or in the same
   PR, scoped carefully).
8. **Product onboarding PR (PRODUCT-GAP-001 FanPWM slice).** Adds a
   FanPWM-bearing product YAML (e.g. a future
   `Ceiling-POE-VentIQ-RoomIQ-FanPWM` config) only if and when a
   customer-facing surface is decided. Goes through
   [`docs/product-onboarding.md`](../product-onboarding.md) gates.
9. **WebFlash onboarding PRs.** WebFlash wrapper under
   [`products/webflash/`](../../products/webflash/), build-matrix
   entry in [`config/webflash-builds.json`](../../config/webflash-builds.json),
   GitHub Release artifact, and WebFlash-side import per
   [`docs/webflash-release-handoff.md`](../webflash-release-handoff.md).

Curating S360-311 artifacts and committing a module-side schematic
PDF (HW-ASSETS-003) does **not** by itself upgrade the JSON status
of the board. Promotion requires both the pin-map reconciliation
above and a separate JSON PR — same rule applied to
[S360-100](artifacts/S360-100-R4.md), [S360-312](artifacts/S360-312-R4.md),
and [S360-320](artifacts/S360-320-R4.md).

## Follow-up PR sequence

Each entry is a separate PR with its own scope, review, and gate
evidence. None is committed to a calendar by this PR. Names follow
the existing style in
[`board-readiness-matrix.md` Follow-up PR sequence](board-readiness-matrix.md#follow-up-pr-sequence)
and
[`hardware-artifact-policy.md` Follow-up PR sequence](hardware-artifact-policy.md#follow-up-pr-sequence).

| PR | Purpose | Gated on |
|---|---|---|
| **HW-PINMAP-311-FOLLOWUP** | Produce the schematic-backed standalone reference doc (`docs/hardware/s360-311-r4-fanpwm.md` or equivalent) and the pin-map reconciliation against the module-side `J3` capture, the Core-side `J6` capture, the SX1509 channel map, and the FanPWM package bindings. Resolves the Core-side `J6` 1-to-13 verify flag against silkscreen evidence (paired with S360-100-BENCH-001). Resolves the UART-on-`J3`-pins-11/12 question. Does **not** change package YAMLs. | Bench / silkscreen evidence for `S360-100-R4` `J6` and `S360-311-R4` `J3`; harness inspection; resolution of the `UART_RX` / `UART_TX` routing alternative. |
| **`S360-311` `schematic_status` promotion** | Separate JSON-only PR that flips `schematic_status` for `S360-311` from `cataloged_unverified` to `verified` and sets `schematic_file: docs/hardware/schematics/S360-311-R4.pdf`. | HW-PINMAP-311-FOLLOWUP resolved (no outstanding flags). |
| **PACKAGE-GAP-001 (FanPWM slice)** | Reconcile [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) (and decide the fate of the legacy four-channel [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml)) against the now-verified schematic evidence: single-channel vs four-channel canonical abstraction; SX1509-channel vs direct-ESP32 binding decision; tach polarity / pull-up / pulses-per-revolution decision; PWM polarity / frequency decision. **Independently** drives the systemic Core abstract-bus rebind referenced by [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups). | `S360-311` `verified`; pin-map reconciliation; Core abstract-bus rebind decisions. |
| **PRODUCT-GAP-001 (FanPWM slice)** | Add a FanPWM-bearing product YAML (e.g. a future `Ceiling-POE-VentIQ-RoomIQ-FanPWM` config) only if and when a customer-facing surface is decided. Goes through [`docs/product-onboarding.md`](../product-onboarding.md) gates. Subject to the fan-driver `max-one-of` rule. | PACKAGE-GAP-001 FanPWM slice resolved. |
| **WEBFLASH-GAP-001 (FanPWM slice)** | Add the WebFlash wrapper under [`products/webflash/`](../../products/webflash/) for the FanPWM-bearing product. | PRODUCT-GAP-001 FanPWM slice resolved. |
| **RELEASE-GAP-001 (FanPWM slice)** | Add the [`config/webflash-builds.json`](../../config/webflash-builds.json) entry, the GitHub Release artifact, and the build / release proof per [`docs/webflash-release-proof.md`](../webflash-release-proof.md). Stable promotion goes through the 17-row [`docs/preview-to-stable-promotion-gates.md`](../preview-to-stable-promotion-gates.md) gate. | WEBFLASH-GAP-001 FanPWM slice resolved. |
| **WF-IMPORT-GAP-001 (FanPWM slice)** | WebFlash-side import per [`docs/webflash-release-handoff.md`](../webflash-release-handoff.md). | RELEASE-GAP-001 FanPWM slice resolved. |

A separate, later PR is required before any
retained-but-not-committed artifact class for `S360-311-R4` (KiCad
source, KiCad PCB, BOM, CPL, gerbers, drill files, STEP, images)
graduates to **may be committed**. That PR must choose a storage
backend per
[Future storage decision](hardware-artifact-policy.md#future-storage-decision)
in HW-ASSETS-001.

## Do-not-change guardrails

This PR does **not** edit:

- [`config/hardware-catalog.json`](../../config/hardware-catalog.json)
- [`config/product-catalog.json`](../../config/product-catalog.json)
- [`config/webflash-builds.json`](../../config/webflash-builds.json)
- [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json)
- any product YAML under [`products/`](../../products/), including
  [`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml)
- any WebFlash wrapper under
  [`products/webflash/`](../../products/webflash/)
- any package YAML under [`packages/`](../../packages/), including
  [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml),
  [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml),
  [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml),
  [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml),
  and [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml)
- any script under `scripts/`
- any test under `tests/`
- any workflow under `.github/workflows/`
- any component under `components/`
- any header under `include/`
- `.gitignore`
- the per-board reference docs
  ([`s360-100-r4-core.md`](s360-100-r4-core.md) and siblings) —
  the Core `J6` capture stays as it is; the
  UART-on-`J3`-pins-11/12 discrepancy is recorded here and is
  **not** rewritten on the Core-side doc.
- the curated artifact index
  [`docs/hardware/artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md) —
  its content is consumed by this audit, not rewritten by it.
- [`docs/hardware/schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf) —
  the schematic PDF stays byte-identical to the HW-ASSETS-003
  commit.

This PR does **not**:

- mark `S360-311` `verified`,
- add or set `schematic_file` for `S360-311` in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json),
- mark the pin map confirmed,
- change [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
  from `package-yaml-pending` to `confirmed-ok`,
- promote `S360-311` to `preview` / `stable` / `production`,
- add a `FanPWM` token to any Release-One or preview config string,
- add a `FanPWM`-bearing entry to
  [`config/webflash-builds.json`](../../config/webflash-builds.json),
- regenerate firmware, create a GitHub Release or tag, or change
  any WebFlash manifest / import,
- resolve the SX1509-channel vs direct-ESP32-GPIO mapping
  disagreement,
- resolve the single-channel vs four-channel canonical abstraction
  decision,
- resolve the Core `J6` 1-to-13 silkscreen verify flag,
- resolve the UART-on-`J3`-pins-11/12 routing question,
- resolve the `"NINE 4pin FANs"` section-title documentation
  question,
- change LED preview path or Sense360 LED Release-One exclusion,
- unblock FanTRIAC (HW-005 stays a separate gate),
- change the mains-voltage compliance status for `S360-320` or
  `S360-400` (COMPLIANCE-001),
- resolve the Core J10 vs RoomIQ J6 pin-order open question (HW-009
  `needs-silkscreen/bench-verification`),
- resolve the systemic Core abstract-bus mismatch enumerated in
  [`docs/release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups).

## See also

- [`docs/hardware/artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md)
  — HW-ASSETS-003 curated artifact index; the schematic-side
  source of truth this audit consumes.
- [`docs/hardware/schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf)
  — committed module-side schematic PDF (HW-ASSETS-003).
- [`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md) —
  Core schematic-backed reference; §J6 12 V PWM fan connector
  (13-pin); §Fan / driver outputs; §Pin assignments (`IO16 =
  TachIO`); §Open Questions #9 (`J6` 1-to-13 pin-order verify);
  §S360-100-BENCH-001 status (bench / silkscreen gate).
- [Hardware Artifact Policy (HW-ASSETS-001)](hardware-artifact-policy.md)
  — defines the per-board artifact-index schema and the
  retained-but-not-committed policy that HW-PINMAP-311 follow-up
  must respect.
- [S360 Board Readiness Matrix (HW-GAP-001)](board-readiness-matrix.md)
  — per-board readiness across hardware-evidence and productization
  axes; the `S360-311` row and follow-up PR sequence row that this
  audit closes the documentation side of.
- [Remaining Board Documentation Audit](remaining-board-documentation-audit.md)
  — per-board documentation-state classification; the
  `Sense360 PWM (S360-311)` subsection.
- [Firmware Package Mapping Audit (HW-009 / HW-010)](firmware-package-mapping-audit.md)
  — six-label package classification taxonomy; the systemic Core
  abstract-bus mismatch row (`needs-package-change`, owned by
  [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)).
- [Product Availability Taxonomy (PRODUCT-AVAIL-001)](../product-availability-taxonomy.md)
  — cross-cutting availability ladder; `S360-311 Sense360 PWM
  partially-documented + design-pending` snapshot row.
- [Release-One Hardware Audit](../release-one-hardware-audit.md) —
  Required follow-ups #2 / #3, the owner of the systemic Core
  abstract-bus rebind that this audit defers to.
- [`docs/hardware/s360-310-r4-relay.md`](s360-310-r4-relay.md) —
  HW-PINMAP-310 sibling audit (`pending — schematic/design
  evidence required`); the template this audit follows.
- [`docs/preview-to-stable-promotion-gates.md`](../preview-to-stable-promotion-gates.md)
  — RELEASE-006 canonical 17-row preview-to-stable gate document;
  applies to any future FanPWM-bearing stable promotion.
- [`docs/product-onboarding.md`](../product-onboarding.md) —
  PRODUCT-004 ordered safe onboarding sequence; any future
  PRODUCT-GAP-001 FanPWM slice goes through these gates.
- [`docs/webflash-contract.md`](../webflash-contract.md) — WebFlash
  artifact / grammar / token contract; `FanPWM` token mapping to
  `S360-311`.
- [`docs/cleanup-audit.md`](../cleanup-audit.md) — classification
  of stale / current / blocked-reference / legacy-compatible repo
  content; carries the HW-PINMAP-311 registration row.
- [`config/hardware-catalog.json`](../../config/hardware-catalog.json)
  — machine-readable hardware catalog. `S360-311` row at lines
  72–81.
- [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json)
  — machine-readable WebFlash taxonomy; `FanPWM` reserved in
  `canonical_modules` (line 12), subject to fan-driver
  `max-one-of`.
- [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
  — single-channel FanPWM package (this audit's primary subject).
- [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml)
  — legacy four-channel FanPWM package, used only by
  `legacy-compatible`
  [`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml).
- [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml)
  — SX1509 channel map (channels 0–3 fan PWM outputs, channels 4–7
  tachometer inputs); the schematic-backed source for the
  `TachPMW*` / `Pul_Cou*` route.

## PWM-BLOCKER-REMOVAL-001 readiness / blocker table

This section is the **PWM-BLOCKER-REMOVAL-001** (2026-05-25)
blocker-by-blocker readiness audit for the S360-311 / FanPWM lane.
It was produced after the Relay (S360-310) and FanDAC (S360-312)
lanes reached package + product-YAML + full-compile state, when
[`repo-freshness-roadmap-audit.md`](../repo-freshness-roadmap-audit.md)
named PWM the next hardware blocker.

This audit is **documentation only**. It does **not** edit any
package, product, config JSON, WebFlash wrapper, workflow, script,
test, component, header, firmware, or manifest; it does **not**
commit any BOM / gerber / CPL / STEP / image binary (those classes
stay **retained-but-not-committed** per
[`hardware-artifact-policy.md`](hardware-artifact-policy.md)); it does
**not** promote `S360-311` `schematic_status`, add a FanPWM product /
WebFlash / release surface, or claim compliance approval. It records
which blocker rows the now-available repo + Drive evidence closes and
which stay open.

### Newly inspected Drive evidence (provenance recorded, not committed)

A Drive folder named **`12vFan_PWM_PulseCounter`** — the exact
`old_name` carried for `S360-311` in
[`config/hardware-catalog.json`](../../config/hardware-catalog.json) —
was inspected this pass (owner `neilmcrae@googlemail.com`, the project
hardware owner). It holds a complete manufacturing artifact set that
the [HW-ASSETS-003 artifact index](artifacts/S360-311-R4.md#files-not-provided-in-this-upload)
previously recorded as `not provided in this upload`:

| Drive artifact | Format / size | Use in this audit |
|---|---|---|
| `bom/12vFan_PWM_PulseCounter.xlsx` | XLSX, 11,112 B | **BOM part-identity cross-check** (below). |
| `sch_pdf/12vFan_PWM_PulseCounter.pdf` | PDF, 357,151 B | Corroborating schematic export. Title block reads `12vFan_PWM_PulseCounter.kicad_sch`, **KiCad 9.0.2**, blank Rev — the *older project-name* export. The repo-committed [`schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf) is the renamed `S360-311-R4.kicad_sch` **KiCad 10.0.3** export, also blank Rev. Net / connector / part content matches; neither PDF carries an explicit `R4` rev stamp. |
| `images/12vFan_PWM_PulseCounter*.png` | 3 × PNG | Board render. Silkscreen reads **`SENSE 360 FAN`** with `J3`, `J5`, `J6`, `pwm1..4`, `pul1..4`, `rx`, `tx`, `Q1..Q4`, `R2/R6/R7`, `gnd`, `5v tx rx gnd`. Confirms board identity and the per-channel labels; does **not** by itself confirm the `J3` / `J6` 1-to-13 pin-1 numbering. |
| `gerbers/`, `cpl/`, `step_file/`, `Assets/` | Folders | Recorded as existing in Drive; not inspected pin-by-pin; not committed. |

**Provenance caveat.** The Drive set carries the board's pre-rename
project name (`12vFan_PWM_PulseCounter`) and a blank Rev field. Its
component list matches the committed `S360-311-R4` schematic 1:1 (see
BOM cross-check), so it cross-checks **at the part-identity layer**,
but it is **not** an explicitly `R4`-stamped artifact set. An operator
confirmation that this Drive folder is the same revision as the
committed `S360-311-R4` schematic is the one remaining identity
nicety; the part-identity match is not contradicted by any evidence.

### BOM part-identity cross-check (Drive `12vFan_PWM_PulseCounter.xlsx`)

Every component the BOM lists matches the
[§Schematic summary](#schematic-summary) of the committed
`S360-311-R4` schematic. No part is contradicted; no part is missing.

| Ref(s) | Value | MFR # | Manufacturer | Schematic role |
|---|---|---|---|---|
| `U1` | MT3608 | MT3608 | XI'AN Aerosemi | `+5V → +12V` boost converter (SOT-23-6). |
| `L1` | 22 µH | SRN6045TA-220M | BOURNS | Boost inductor. |
| `D1` | SS34 | SS34 | GOODWORK | Boost Schottky rectifier (SMA). |
| `R3` / `R5` | 38k / 2k | FRC0402F3832TS / FRC0402F2001TS | FOJAN | Boost feedback divider (sets ≈12 V). |
| `R4` | 330R | FRC0402F3300TS | FOJAN | `D2` indicator-LED series resistor. |
| `D2` | Blue | TZ-P2-0402YGTIA1-0.45T | TUOZHAN | Power / boost indicator LED. |
| `C1` / `C2` | 22 µF / 10V | GRM158R61A226ME15D | muRata | Boost input / output bulk caps. |
| `Q1`–`Q4` | Q_NMOS_GSD | **ME15N10-G** | MATSUKI | **Per-channel low-side N-FET** (TO-252), one per fan output. |
| `R1` / `R2` / `R6` / `R7` | 1k | FRC0402F1001TS | FOJAN | Gate series resistors for `Q1`–`Q4`. |
| `J1` / `J2` / `J4` / `J5` / `J6` | Conn_01x4 | SM04B-SRSS-TB(LF)(SN) | JST (SH, 1.0 mm) | Four 4-pin fan outputs + the 4-pin Nextion (`J6`). |
| `J3` | Conn_01x13 | SM13B-SRSS-TB(LF)(SN) | JST (SH, 1.0 mm) | 13-pin "From Core" harness connector. |

This closes the **BOM cross-check** precondition recorded for
`PACKAGE-PWM-001` in
[`package-readiness-matrix.md`](package-readiness-matrix.md#fan_pwmyaml--s360-311)
**at the part-identity layer** — the same standard `HW-BOM-ASSETS-002`
applied to `S360-400` / `S360-410`. It does **not** add the `.xlsx` to
git, and does **not** by itself promote `schematic_status`.

### Project-tracker corroboration (Drive `Sense360_R4_Tracker`, evidence-pass 2)

A second Drive artifact was inspected this pass that the prior pass did
**not** cite: the project tracker spreadsheet **`Sense360_R4_Tracker`**
(Google Sheet, owner `neilmcrae@googlemail.com`, last modified
2026-05-18, in the R4 shared drive). It is a task / status tracker, not
a manufacturing artifact, so it is recorded here for **provenance and
status corroboration only**; nothing is committed from it.

| Tracker reference | Status (per tracker) | Bearing on this audit |
|---|---|---|
| Boards tab — `S360-311` row | — | Restates the board identity **1:1** with [`config/hardware-catalog.json`](../../config/hardware-catalog.json): `Inline` / `Driver` / `Sense360 PWM` / `S360-311` / `R4` / old name `12vFan_PWM_PulseCounter` / `12V PWM fan driver, up to 4 fans with tach feedback.`. Independent confirmation of the catalog row. |
| Task `R07` — "Rename project to S360-311 R4 and upload to R4 drive" | **Done** | Confirms, at the project-management layer, that the `12vFan_PWM_PulseCounter` → `S360-311 R4` rename actually happened — the same rename the catalog `old_name` records. Corroborates that the Drive `12vFan_PWM_PulseCounter` set and the committed `S360-311-R4` schematic are the **same board lineage**. |
| Task `G01` — "Add the silkscreen markings on every R4 board: Sense360 logo, friendly name, P/N (SKU), REV, date code" | **Waiting** (High) — *"P/N, REV and date code are not currently on the boards. Waiting for Neil to send the SVG logo (see N01)."* | **Explains the blank `Rev` field** in both schematic exports and the absence of a `R4` rev stamp in the board renders: the P/N / REV / date-code silkscreen is a **board-wide, not-yet-applied** task across the entire R4 fleet, blocked on task `N01` (Neil to supply the logo SVG), **not** a board-specific identity ambiguity for `S360-311`. |
| Task `N01` — "Send the Sense360 SVG logo to Stephen" | **To do** (High) | Upstream blocker for `G01`. Until it lands, **no** R4 board carries an on-silkscreen P/N / REV / date code. |
| Task `C02` — "Change the fan connector to XH2.54" (Core, `S360-100`) | **Done** (High) — *"All small fans we can find use XH2.54, not the current connector."* | Records that the **Core-side** fan connector was migrated to **XH2.54 (2.54 mm pitch)**, while the committed `S360-311` module BOM uses **JST-SH (1.0 mm pitch)** for both the fan outputs (`SM04B`) and the "From Core" `J3` (`SM13B`). Adds a concrete **connector-type / pitch reconciliation** item at the Core ↔ module harness — see below. |

**What this advances:**

- **Row 2 (schematic / BOM identity).** The single residual on row 2
  was the missing explicit `R4` rev stamp. The tracker now **explains
  it**: `R07` (Done) confirms the project-level rename to `S360-311 R4`,
  and `G01` (Waiting) + `N01` (To do) show the blank `REV` is a tracked,
  fleet-wide silkscreen task pending Neil's SVG — not unresolved
  provenance. The rev-stamp item stays a **minor operator confirmation**
  (it will arrive via `G01`/`N01`), but it is no longer an unexplained
  gap. Row 2 stays **CLOSED (part-identity)**.

- **Row 3 (Core connector / bus / GPIO mapping).** The tracker adds a
  new, specific harness sub-item: **Core fan connector = XH2.54** (`C02`
  Done) vs **module connectors = JST-SH** (`J3` `SM13B`, fan outputs
  `SM04B`). Either the committed module schematic / BOM **predates** a
  matching module-side connector change, or the Core ↔ module harness
  **adapts** XH2.54 ↔ JST-SH. This is recorded as an open reconciliation
  point for `HW-PINMAP-311-FOLLOWUP`; it does **not** change row 3's
  **STILL BLOCKING** classification (it sharpens it).

- **Silkscreen-based pin-order verification.** `G01` (Waiting) confirms
  that **no R4 board currently carries a P/N / REV / date-code
  silkscreen**. The board renders in the Drive set show component
  reference designators (`J3` / `J5` / `J6`, `pwm1..4`, `pul1..4`,
  `Q1..Q4`) but cannot supply an operator-attributed `J3` / `J6`
  1-to-13 **pin-1 numbering** read; and the canonical board-identity
  silkscreen those renders would carry is itself pending `G01`/`N01`.
  This reinforces why the Core `J6` / module `J3` pin-order `verify`
  flags (row 3) stay open: the silkscreen evidence does not exist yet.

This subsection **does not** edit the catalog, any package / product /
config, or any binary; it records tracker provenance and refines the
basis of rows 2 and 3 only.

### Operator decisions (PWM-BLOCKER-REMOVAL-001, 2026-05-25)

The three design questions that evidence alone could not close were put
to the project operator during `PWM-BLOCKER-REMOVAL-001` and answered as
follows. These are recorded as **operator-attributed design decisions**
(provenance: operator response, 2026-05-25). They resolve the
*design-intent* questions; they do **not** supply bench / silkscreen /
waveform evidence and they do **not** authorize a package edit in this
PR — the operator explicitly directed that any SX1509 / Core
abstract-bus binding be recorded as the **next prerequisite**, not
fabricated here.

| # | Question | Operator decision |
|---|---|---|
| D1 | Single-channel vs four-channel canonical FanPWM abstraction | **Four-channel.** Canonical FanPWM exposes **4 independent fan PWM outputs + 4 tach inputs**, matching the board outputs `J1` / `J2` / `J4` / `J5` and the legacy `sense360_fan_pwm.yaml` shape. Product layers may use one channel, gang channels, or leave channels unused, but the **package abstraction represents all four hardware channels neutrally**. |
| D2 | SX1509-routed vs direct-ESP32 PWM / tach binding | **SX1509 expander.** Bind `TachPMW1..4` → **SX1509 channels 0..3** and `Pul_Cou1..4` → **SX1509 channels 4..7**; keep `TachIO` **direct on ESP32 `IO16`**. Do **not** preserve the older direct-ESP32 binding where it contradicts the schematic. If implementation needs a Core abstract-bus / SX1509 package follow-up, **record that as the next prerequisite** rather than fabricating direct-GPIO mappings. |
| D3 | Is the committed JST-SH schematic the current R4, given Core `C02` → XH2.54? | **Committed schematic is current R4.** JST-SH is correct for the module side (`SM04B` fan outputs, `SM13B` From-Core `J3`). The Core ↔ module harness **adapts XH2.54 ↔ JST-SH** where needed. No newer schematic / BOM is asserted; no new schematic is needed for this PR. |

**What this resolves (design-intent layer only):**

- **D1 + D2** define the reconciliation target for
  [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml):
  a **four-channel** package bound to **SX1509 channels 0..3 (PWM) /
  4..7 (tach)** with `TachIO` direct on `IO16`. This removes the
  cardinality and routing-direction ambiguity that previously gated
  rows 4 and 7. It does **not** remove the implementation gate: the
  parent Core abstract packages bind direct ESP32 GPIOs and
  `expansion_gpio1..4` were retired by `CORE-ABSTRACT-BUS-001C`, so the
  SX1509 four-channel binding is owed to a **`CORE-ABSTRACT-BUS-001` /
  SX1509 package follow-up** that must land before (or with)
  `PACKAGE-PWM-001-IMPLEMENT-001`.
- **D3** resolves the connector-identity sub-item raised in
  [§Project-tracker corroboration](#project-tracker-corroboration-drive-sense360_r4_tracker-evidence-pass-2):
  the committed JST-SH module schematic is current; the XH2.54-vs-JST-SH
  difference is a **harness adaptation**, not a stale module schematic.
  The Core `J6` / module `J3` 1-to-13 **silkscreen pin order** remains
  `verify` (no R4 silkscreen exists yet per tracker `G01`).

**What this does NOT resolve (still owed to bench / follow-up):** PWM
polarity (active-high vs active-low for the low-side N-FET gate path);
tach pull-up source (discrete / SX1509 on-die / fan-side);
pulses-per-revolution validation of the `multiply: 0.5` factor; per-fan
and aggregate current + thermal envelope (MT3608 ceiling); the Core `J6`
/ module `J3` silkscreen 1-to-13 pin order; the UART-on-`J3`-pins-11/12
routing question; and the `CORE-ABSTRACT-BUS-001` / SX1509 four-channel
package binding itself.

### Blocker table

Classifications: **CLOSED** (evidence resolves it), **PARTIAL**
(evidence advances it but a named gap remains), **STILL BLOCKING**
(open; needs design / bench work, not just evidence), **OUT OF SCOPE**
(owned elsewhere / deferred by design), **NEEDS OPERATOR INPUT**
(only the hardware owner can answer).

| # | Blocker row | Classification | Basis |
|---|---|---|---|
| 1 | Hardware evidence | **CLOSED** | Module-side schematic PDF committed (HW-ASSETS-003); Drive `12vFan_PWM_PulseCounter` set adds a corroborating schematic export, BOM, gerbers, CPL, STEP, and 3 board renders. |
| 2 | Schematic / BOM identity | **CLOSED (part-identity)** | Drive BOM cross-checks the committed R4 schematic 1:1 (table above). The residual explicit-`R4`-rev-stamp item is now **explained** by the project tracker (`R07` rename Done; blank `REV` is the fleet-wide silkscreen task `G01`/`N01`, not unresolved provenance) — see [§Project-tracker corroboration](#project-tracker-corroboration-drive-sense360_r4_tracker-evidence-pass-2). Still a minor operator confirmation, not a hard blocker. |
| 3 | Core connector / bus / GPIO mapping | **STILL BLOCKING** (connector identity + routing direction resolved) | `core_i2c` shared-bus rebind landed (`CORE-ABSTRACT-BUS-001B-IMPLEMENT-001`). Operator **D3** confirms the committed JST-SH module schematic is current R4 and the XH2.54-vs-JST-SH difference is a **harness adaptation** (connector-type item from [§Project-tracker corroboration](#project-tracker-corroboration-drive-sense360_r4_tracker-evidence-pass-2) closed). Operator **D2** sets the routing direction to **SX1509** (see [§Operator decisions](#operator-decisions-pwm-blocker-removal-001-2026-05-25)). Still blocking on: Core `J6` / module `J3` 1-to-13 **silkscreen pin order** (`verify`; no R4 silkscreen yet per tracker `G01`); UART-on-`J3`-pins-11/12 routing; and the `CORE-ABSTRACT-BUS-001` / SX1509 package binding (`expansion_gpio1..4` retired by `001C`). |
| 4 | PWM output pin mapping | **STILL BLOCKING** (target decided; implementation gated) | **Decision now made** (operator **D1** + **D2**, see [§Operator decisions](#operator-decisions-pwm-blocker-removal-001-2026-05-25)): **four-channel**, `TachPMW1..4` → **SX1509 ch 0..3**, `Pul_Cou1..4` → **SX1509 ch 4..7**, `TachIO` direct on **`IO16`**. The current [`sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml) `fan_pwm_pin: GPIO4` / `fan_tach_pin: GPIO5` direct-ESP32 binding **contradicts** that target and must not be preserved. Implementation is gated on the `CORE-ABSTRACT-BUS-001` / SX1509 four-channel binding follow-up (not fabricated here, per operator instruction). |
| 5 | Controlled load type | **CLOSED** | Up to four 12 V 4-wire PWM fans on JST-SH `SM04B` 1×4 connectors (`J1` / `J2` / `J4` / `J5`); confirmed by schematic + BOM + render silkscreen. |
| 6 | Output electrical characteristics | **PARTIAL / NEEDS OPERATOR INPUT** | `+5V → +12V` boost (MT3608, `L1` 22 µH, `D1` SS34, `R3` 38k / `R5` 2k); per-channel low-side `ME15N10-G` N-FET + 1 kΩ gate. Per-fan current budget, MT3608 output-current ceiling, multi-fan aggregate load, and locked-rotor / inrush envelope are **not** specified. |
| 7 | Package YAML existence / correctness | **CLOSED for the PWM-drive-only scope** (`PACKAGE-PWM-001-IMPLEMENT-001`) | [`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) has been **reconciled** from the legacy single-channel `ledc` + `pulse_counter` form into the four-channel **PWM-drive-only** package: four `fan: platform: speed` controllers (`fan_pwm_1..4`) on the neutral SX1509 PWM-drive outputs `fan_pwm_drive_1..4` (channels 0..3), **composing** [`fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml) (no duplicated channel map). No `ledc`, no `pulse_counter`, no RPM, no `${expansion_gpio*}`; `TachIO`/`GPIO16` reserved. Pinned by [`tests/test_fan_pwm_package.py`](../../tests/test_fan_pwm_package.py). The **product**-side correctness (PWM polarity wiring, current/thermal headroom) still depends on the row-6 bench items; per-fan RPM is deferred future work. |
| 8 | Product YAML eligibility | **CLOSED for the PWM-drive-only scope** (`PRODUCT-PWM-001`) | `PRODUCT-PWM-001` lands the canonical FanPWM product YAML [`products/sense360-ceiling-poe-fanpwm.yaml`](../../products/sense360-ceiling-poe-fanpwm.yaml) (config string `Ceiling-POE-FanPWM`) as the **product-YAML-only / no-WebFlash-exposure** slice: composes Core ceiling + PoE PSU + base/health + the PWM-drive-only [`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml). Matching non-WebFlash catalog row added (`status: hardware-pending`, `webflash_build_matrix: false`, no `artifact_name`, no `webflash_wrapper`). It is gated **after** `PACKAGE-PWM-001-IMPLEMENT-001` (row 7) and the validated full compile (row 9). **No RPM** (no `pulse_counter`; `TachIO`/`GPIO16` reserved). The legacy [`sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml) stays `legacy-compatible` and is **not** the candidate. WebFlash / release / import (rows 10) stay **out of scope**; the bench items (row 6) and silkscreen / UART / JSON-promotion (rows 3 / `c`/`e`/`g`) stay open. Pinned by [`tests/test_pwm_product_readiness.py`](../../tests/test_pwm_product_readiness.py). |
| 9 | Compile-only target eligibility | **ADVANCED — single compile-only validation target added** (`FW-COMPILE-PWM-001`) | `FW-COMPILE-PWM-001` adds **one** compile-only validation target `ceiling-poe-fanpwm-compile-only` → [`products/compile-only/ceiling-poe-fanpwm.yaml`](../../products/compile-only/ceiling-poe-fanpwm.yaml) (config string `Ceiling-POE-FanPWM`) to [`config/compile-only-targets.json`](../../config/compile-only-targets.json), composing Core ceiling + PoE PSU + base + health + the PWM-drive-only [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml). It validates the **PWM-drive-only package scope** only: `shipment_status: compile-only`, `webflash_exposure_allowed_now: false`, `hardware_required_for_validation: true`, `compile_validation_status: validated-full-compile` (the **full ESPHome compile is now recorded green** — `FW-COMPILE-PWM-RESULT-001` / PR #592 flipped this from `pending-ci` after the `Compile-only Firmware Validation` run `26414398902`, `compile_mode=full`, 10 targets, conclusion `success`, ran `scripts/validate_compile_targets.py --compile` against this YAML and passed), `rpm_supported: false`; no product YAML in this row, no `config/webflash-builds.json` row, no `webflash_build_matrix` flip, no `artifact_name`, no RPM. The skeleton lives under `products/compile-only/` so no `config/product-catalog.json` entry is added by this row; the **product YAML** is added separately by `PRODUCT-PWM-001` (row 8) and is a distinct file. The lane stays `defer` in [`config/compile-only-candidates.json`](../../config/compile-only-candidates.json) (`Ceiling-POE-FanPWM` listed in `currently_compile_only_config_strings`, blockers retain a `PACKAGE-PWM-001` + `CORE-ABSTRACT-BUS-001B` history per `test_compile_expansion_candidates.py`) — a single validation target is not lane-wide compile-only readiness. A clean full compile is **not** WebFlash exposure, **not** a release artifact, **not** RPM support, and **not** hardware-stable readiness. Pinned by [`tests/test_compile_targets.py`](../../tests/test_compile_targets.py) `FanPWMCompileOnlyCoverageTests`. |
| 10 | WebFlash / release readiness | **OUT OF SCOPE (this PR)** | No FanPWM entry in [`config/webflash-builds.json`](../../config/webflash-builds.json); gated behind `PRODUCT-PWM-001`; same design-deferred posture as Relay / DAC. Not advanced here. |
| 11 | Compliance / safety caveats | **CLOSED (no mains gate)** | SELV low-voltage board (5 V in → 12 V boost out); **no mains**, so `COMPLIANCE-001` (mains UK/EU) does **not** gate FanPWM. The current / thermal / inrush items in row 6 are bench characterisation, not a compliance approval. |

### Next-PR designation

- **`PACKAGE-PWM-001-READINESS-REFRESH`** — this PR
  (`PWM-BLOCKER-REMOVAL-001`). Records the Drive evidence, closes rows
  1 / 2 / 5 / 11, advances row 6, and narrows the `PACKAGE-PWM-001`
  gate. No package / product / config / WebFlash / release edit.
- **`PACKAGE-PWM-001-IMPLEMENT-001`** — **PACKAGE LANDED (PWM-drive-only);
  product / WebFlash / release still gated.** The canonical FanPWM package
  [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
  has been reconciled at the **package layer** to the PWM-drive-only scope:
  four independent SX1509 PWM-drive fan controllers (`fan_pwm_1..4` on the
  neutral outputs `fan_pwm_drive_1..4`, channels 0..3) composing the binding
  [`packages/expansions/fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml);
  no `pulse_counter`, no RPM, `TachIO`/`GPIO16` reserved/pending (see the
  [`PACKAGE-PWM-001-IMPLEMENT-001` audit-log row](#hw-pinmap-311-followup-audit-log)).
  SX1509 PWM-drive output **is supported** and is the mechanism used; per-fan
  RPM via an SX1509 `pulse_counter` stays compile-proven unsupported
  (`PWM-SX1509-TACH-PROOF-001`) and is not claimed. The **product / WebFlash /
  release** surface stays blocked behind the remaining bench gates below
  (PWM polarity; current/thermal envelope) plus product YAML, compile-only
  target/result, WebFlash/release/import/compliance, and the optional future
  RPM strategy. Status of the original gating set:
  - (a) single-channel-vs-four-channel — **RESOLVED: four-channel** (D1).
  - (b) SX1509-vs-direct-ESP32 routing — **RESOLVED: SX1509** (D2;
    `TachPMW1..4`→ch0..3, `Pul_Cou1..4`→ch4..7, `TachIO` direct `IO16`).
  - connector rev / XH2.54-vs-JST-SH — **RESOLVED: committed schematic
    is current R4; harness adapts** (D3).
  - (c) Core `J6` / module `J3` 1-to-13 silkscreen pin order — **STILL
    OPEN** (no R4 silkscreen exists yet, tracker `G01`).
  - (d) PWM polarity + tach pull-up source + pulses-per-revolution —
    **STILL OPEN** (bench).
  - (e) UART-on-`J3`-pins-11/12 routing — **STILL OPEN**.
  - (f) per-fan / aggregate current + thermal envelope — **STILL OPEN**
    (bench / MT3608 ceiling).
  - (g) `S360-311` `schematic_status: verified` JSON promotion — **STILL
    OPEN** (gated on c / e).
  - (h) expander-tach RPM decision — **RESOLVED-BY-DEFERRAL** by
    `PACKAGE-PWM-TACH-STRATEGY-001` (see
    [§Tach / RPM strategy (PACKAGE-PWM-TACH-STRATEGY-001)](#tach--rpm-strategy-package-pwm-tach-strategy-001)):
    the first FanPWM package is scoped **PWM-drive-only** (four SX1509 PWM
    outputs; no per-fan RPM; optional diagnostic binary tach only;
    `TachIO`/`GPIO16` reserved). Per-fan RPM is deferred to a future
    `COMPONENT-SX1509-TACH-001` or a bench-confirmed `TachIO` follow-up.
    Because no RPM is exposed, the tach-pull-up / pulses-per-revolution
    parts of (d) **stop gating** this PWM-only slice.

  **Explicit next prerequisite** (per operator instruction, *do not
  fabricate direct-GPIO mappings*): a **`CORE-ABSTRACT-BUS-001` / SX1509
  four-channel binding follow-up** that re-anchors the FanPWM PWM / tach
  substitutions onto SX1509 channels 0..3 / 4..7 (with `TachIO` direct
  on `IO16`), since `001C` retired `expansion_gpio1..4` and the current
  abstract-bus packages bind direct ESP32 GPIOs. That prerequisite landed
  as `CORE-ABSTRACT-BUS-SX1509-001` (below). After the PWM-only tach scope
  (h) and that binding layer, the **PWM-only**
  `PACKAGE-PWM-001-IMPLEMENT-001` still needs the PWM-drive bench items
  (PWM polarity from (d); current/thermal envelope (f)) and the
  four-channel reconciliation of `fan_pwm.yaml` before any FanPWM product
  surface; the silkscreen (c) / UART (e) / JSON-promotion (g) items remain
  open but are product-side, not PWM-only-package-blocking.
- **`CORE-ABSTRACT-BUS-SX1509-001`** — **LANDED** (this PR). Adds the
  neutral, binding-only Core abstract-bus layer
  [`packages/expansions/fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml)
  that is the explicit next prerequisite named above: the SX1509 hub on
  `core_i2c`, four neutral PWM-drive outputs (`fan_pwm_drive_1..4` →
  channels 0..3), four neutral tach inputs (`fan_pwm_tach_1..4` →
  channels 4..7), and a `tach_io_pin: GPIO16` substitution for the direct
  `TachIO` line, pinned by
  [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py)
  (`FanPwm*` classes). It adds **no** FanPWM product, WebFlash wrapper,
  release artifact, or product-facing entity; it does **not** edit
  `fan_pwm.yaml`, `sense360_fan_pwm.yaml`, `gpio_expander_sx1509.yaml`,
  `fan_relay.yaml`, `fan_gp8403.yaml`, any Core abstract package, any
  `config/**`, or any `schematic_status`. **ESPHome capability finding
  recorded (not faked):** PWM drive through the SX1509 (channels 0..3) is
  supported via the `output: platform: sx1509` LED-driver PWM engine, but
  the `Pul_Cou1..4` tach lines (channels 4..7) **cannot** back a hardware
  `pulse_counter` — ESPHome's `pulse_counter` requires an
  interrupt-capable native (`InternalGPIOPin`) pin, which an SX1509
  expander pin is not — so they are exposed as polled binary GPIO state
  only. Per-fan RPM via the expander is therefore a **new documented
  blocker** for `PACKAGE-PWM-001-IMPLEMENT-001` (resolve via the direct
  `tach_io_pin` / `IO16` line, a future custom component, or a
  bench-decided alternative). This PR does **not** make
  `PACKAGE-PWM-001-IMPLEMENT-001` implementation-ready: the bench set
  (c / d / e / f), the JSON promotion (g), and the new expander-tach RPM
  blocker all remain open, and the FanPWM product / WebFlash / release
  surface stays blocked. **The expander-tach RPM blocker raised here is
  classified and resolved-by-deferral in
  [§Tach / RPM strategy (PACKAGE-PWM-TACH-STRATEGY-001)](#tach--rpm-strategy-package-pwm-tach-strategy-001)
  below: the first FanPWM package is scoped PWM-drive-only, with per-fan
  RPM deferred to future work.**

## Tach / RPM strategy (PACKAGE-PWM-TACH-STRATEGY-001)

This section is the **PACKAGE-PWM-TACH-STRATEGY-001** (2026-05-25)
decision record for how — and whether — the S360-311 / FanPWM lane
exposes fan tachometer / RPM data. It exists to **classify and resolve**
the tach/RPM blocker surfaced when `CORE-ABSTRACT-BUS-SX1509-001` landed
the neutral binding layer
[`packages/expansions/fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml)
(see [§Next-PR designation](#next-pr-designation) and the
[`CORE-ABSTRACT-BUS-SX1509-001` audit-log row](#hw-pinmap-311-followup-audit-log)).

This section is **documentation only**. It does **not** edit any
package, product, config JSON, WebFlash wrapper, workflow, script,
test, component, header, firmware, or manifest; it does **not** add a
FanPWM product / WebFlash / release surface, build a custom component,
promote `S360-311` `schematic_status`, or claim RPM support of any
kind. It records a scope decision and re-points the next implementation
PR.

### Finding being resolved (compile-proven — PWM-SX1509-TACH-PROOF-001)

`CORE-ABSTRACT-BUS-SX1509-001` first recorded this ESPHome capability
finding; **PWM-SX1509-TACH-PROOF-001 (2026-05-25) upgrades it from an
inferred claim to a compile/config proof** so the wording no longer
rests on "online docs" or on reasoning-from-`InternalGPIOPin` alone:

- **SX1509 PWM drive (channels 0..3, `TachPMW1..4`) IS supported** via
  the on-chip LED-driver PWM engine (`output: platform: sx1509`). This
  is **not** in question and stays the basis of FanPWM drive. SX1509
  GPIO / binary input (channels 4..7, polled `binary_sensor: platform:
  gpio`) is supported too.
- **An SX1509 expander pin used as an ESPHome `pulse_counter` input is
  compile-proven unsupported by ESPHome validation.** The minimal
  fixture
  [`tests/esphome/sx1509_pulse_counter_proof.yaml`](../../tests/esphome/sx1509_pulse_counter_proof.yaml)
  (SX1509 hub on `core_i2c`, channel 4 = `Pul_Cou1`, behind a
  `sensor: platform: pulse_counter`) is **rejected by `esphome config`**
  (ESPHome 2026.5.1, exit code 2) with the captured error:

  ```text
  Failed config

  sensor.pulse_counter: [source sx1509_pulse_counter_proof.yaml:…]
    pin:
      [sx1509] is an invalid option for [pin]. Please check the indentation.
  ```

  The `pulse_counter` pin schema accepts only an interrupt-capable
  native ESP32 `InternalGPIOPin`; the SX1509 expander pin schema is not
  a valid option for it.
- **Per-fan RPM via `Pul_Cou1..4` is therefore un-implementable** as a
  standard ESPHome `pulse_counter` RPM input — a compile-proven blocker
  for the originally-scoped four-PWM + four-per-fan-RPM
  `PACKAGE-PWM-001-IMPLEMENT-001`.

Two control checks in
[`tests/test_sx1509_tach_pulse_counter_proof.py`](../../tests/test_sx1509_tach_pulse_counter_proof.py)
confirm the rejection is specific to `pulse_counter` + SX1509, not a
generic config error: the **same** SX1509 pin validates as a
`binary_sensor: platform: gpio` (`esphome config` passes), and
`pulse_counter` validates on a **native** ESP32 GPIO (the `TachIO` /
`GPIO16` line) (`esphome config` passes). The decision below
(PWM-drive-only first) is unchanged by the proof — it confirms, rather
than revises, `PACKAGE-PWM-TACH-STRATEGY-001`.

Cross-checks consistent with the finding (not new evidence): the only
`pulse_counter` uses in the repo are on **native ESP32 GPIOs**
([`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) `${fan_tach_pin}`,
[`sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml)
`GPIO8`/`GPIO12`/`GPIO14`/`GPIO16`,
[`sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml)
`GPIO5`); both SX1509 packages
([`fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml),
[`gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml))
expose tach as `binary_sensor: platform: gpio`, never `pulse_counter`;
no custom tach/RPM component exists in [`components/`](../../components).

### Candidate strategies and classification

| Strategy | Description | Classification |
|---|---|---|
| **A — No per-fan RPM initially** | PWM drive only; expose tach as diagnostic binary state or omit tach sensors; per-fan RPM is future custom-component / bench work. | **SELECTED** (operator decision D-T1, below). |
| B — Direct `TachIO` aggregate RPM | One aggregate/native RPM input on the direct ESP32 `TachIO` / `IO16` line. | **DEFERRED.** `TachIO` is the **shared MOSFET-drain rail** ([§Per-fan-output drive topology](#per-fan-output-drive-topology-module-side)), so at most one *aggregate* signal, not per-fan; and whether it carries a usable tach waveform is **not** bench-confirmed. Also gated on the `GPIO16` conflict below. No RPM claimed. |
| C — Custom SX1509 tach component | Future component listens to the SX1509 interrupt / reads registers and computes RPM. | **DEFERRED — `COMPONENT-SX1509-TACH-001`.** Not present, not proven; **not built in this PR**. |
| D — Hardware rework / reroute | Re-route a per-fan tach to a native ESP32 pin. | **OUT OF SCOPE** unless the hardware owner confirms a rework. |
| E — Alternative ESPHome component | Some other official ESPHome component does frequency/pulse counting on SX1509-like pins. | **NONE EXISTS** (verified against current `sx1509` / `pulse_counter` component behaviour — not guessed). No such path. |

### Operator decision (PACKAGE-PWM-TACH-STRATEGY-001, 2026-05-25)

Recorded as **operator-attributed design decisions** (provenance:
operator response, 2026-05-25). They resolve the *scope* question; they
do **not** supply bench / waveform evidence and do **not** authorize a
package edit in this PR.

| # | Question | Operator decision |
|---|---|---|
| D-T1 | Is the first FanPWM package allowed to be PWM-drive-only, with no per-fan RPM initially? | **Yes — PWM-drive-only first.** `PACKAGE-PWM-001-IMPLEMENT-001` may proceed as a PWM-drive-only initial package: **four** independent SX1509 PWM outputs (channels 0..3); **no** per-fan RPM sensors; **optional diagnostic binary tach states only** (channels 4..7) if safe and useful; per-fan RPM remains **future work**. Do **not** claim RPM via SX1509 `pulse_counter`; do **not** build a custom component in this PR; keep the RPM limitation documented as a future `COMPONENT-SX1509-TACH-001` or bench-confirmed `TachIO` follow-up. |
| D-T2 | Should `TachIO` `GPIO16` be used as one aggregate/native RPM input if bench confirms it? | **Reserve only — no RPM claim now.** Document `GPIO16` / `TachIO` as reserved/pending: no aggregate RPM claim now, no per-fan RPM claim now, **no `TachIO` sensor in the initial package** unless separately proven safe. Future aggregate RPM via `TachIO` requires bench confirmation that `TachIO` carries a usable signal **and** resolution of any `GPIO16` conflict. |

### Allowed implementation posture for `PACKAGE-PWM-001-IMPLEMENT-001`

The next implementation PR is now scoped (strategy A) as:

- **Four PWM-drive outputs** bound to SX1509 channels 0..3
  (`TachPMW1..4`), per operator decisions D1 / D2 in
  [§Operator decisions](#operator-decisions-pwm-blocker-removal-001-2026-05-25).
- **No per-fan RPM sensors.** Per-fan RPM is explicitly **out of scope**
  for the first package.
- **Optional diagnostic binary tach states only** on SX1509 channels
  4..7 (`Pul_Cou1..4`), exposed as polled `binary_sensor` state — and
  only if safe and useful; they are **not** RPM and must not be labelled
  or presented as RPM.
- **`TachIO` / `GPIO16` reserved/pending.** No `TachIO` sensor and no
  RPM claim in the first package.

This posture does **not** make `PACKAGE-PWM-001-IMPLEMENT-001`
implementation-ready by itself: it still needs the bench items that the
PWM-drive path actually depends on — **PWM polarity** (active-high vs
active-low for the per-fan low-side N-FET gate path) and the **per-fan /
aggregate current + thermal envelope** (MT3608 ceiling) — and the
**four-channel reconciliation** of
[`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
against the neutral SX1509 binding. Tach pull-up source and
pulses-per-revolution validation are **no longer gating** for this slice
because no RPM is exposed (they return only if/when an RPM path is
pursued).

### What stays blocked

- **Per-fan RPM** — blocked by the SX1509 hardware-`pulse_counter`
  limitation (above). Only a future `COMPONENT-SX1509-TACH-001` could
  change this; not pursued here.
- **Aggregate RPM via `TachIO`** — blocked pending (i) bench
  confirmation that the shared `TachIO` net carries a usable tach
  waveform, and (ii) resolution of the `GPIO16` conflict below.
- **FanPWM product / WebFlash / release surface** — unchanged; stays
  blocked behind `PACKAGE-PWM-001-IMPLEMENT-001` → `PRODUCT-PWM-001`.

### `GPIO16` conflict — open verification item

Recorded as an **open item**, not a resolved conflict. The direct
`TachIO` line is `GPIO16`, but `GPIO16` is also bound elsewhere in the
Core package stack: **W5500 Ethernet `RST` on the PoE core**
([`packages/hardware/sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml))
— material because the candidate FanPWM product family is
`Ceiling-POE-VentIQ-FanPWM-RoomIQ` (PoE power path) — and **Speaker I2S
`DIN` on the voice cores**
([`sense360_core_voice_ceiling.yaml`](../../packages/hardware/sense360_core_voice_ceiling.yaml),
[`sense360_core_voice_wall.yaml`](../../packages/hardware/sense360_core_voice_wall.yaml)).
Any future use of `TachIO` for aggregate RPM must first resolve this
`GPIO16` contention; the resolution belongs to the
`CORE-ABSTRACT-BUS-001` rebind + bench follow-up, **not** to this PR. No
conflict is asserted as resolved here.

## HW-PINMAP-311-FOLLOWUP audit log

This section is the dated investigation log for
**HW-PINMAP-311-FOLLOWUP** — the docs-only evidence-pass re-check
of the schematic-side, package-side, bench-side, harness-side, and
silkscreen-side evidence that must land before this audit can move
off `partial — schematic evidence available; package
reconciliation pending` (see [§Status](#status)). Each row records
a documentation-only re-check of the committed evidence against
the [§Repository evidence](#repository-evidence),
[§Schematic summary](#schematic-summary),
[§Connector / pin-map findings](#connector--pin-map-findings),
[§UART pins on `J3` pins 11–12](#uart-pins-on-j3-pins-1112),
[§Per-fan-output drive topology (module side)](#per-fan-output-drive-topology-module-side),
[§Open documentation questions](#open-documentation-questions),
[§Existing package abstraction](#existing-package-abstraction),
[§Parent Core packages that resolve `fan_pwm_pin` / `fan_tach_pin`](#parent-core-packages-that-resolve-fan_pwm_pin--fan_tach_pin),
[§Reconciliation findings](#reconciliation-findings),
[§Known unresolved issues](#known-unresolved-issues),
[§Package YAML status](#package-yaml-status),
[§Product / WebFlash status](#product--webflash-status),
[§Required evidence before promotion](#required-evidence-before-promotion),
and [§Follow-up PR sequence](#follow-up-pr-sequence) tables and
lists above. An audit-log entry **does not** by itself promote
the top-line status, close any reconciliation row, resolve the
SX1509-channel vs direct-ESP32-GPIO mapping disagreement, resolve
the UART-on-`J3`-pins-11/12 routing question, resolve the
single-channel vs four-channel canonical abstraction decision,
resolve the `"NINE 4pin FANs"` section-title documentation
question, change [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml)
from `package-yaml-pending` / `needs-package-reconciliation`,
change the legacy four-channel
[`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml)
from `legacy-compatible`, edit any Core abstract package
([`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml),
[`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml)),
edit [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml),
change the JSON `schematic_status` of `S360-311` from
`cataloged_unverified` in
[`config/hardware-catalog.json`](../../config/hardware-catalog.json),
set `schematic_file` for `S360-311`, add a `FanPWM`-bearing entry
to [`config/product-catalog.json`](../../config/product-catalog.json)
or [`config/webflash-builds.json`](../../config/webflash-builds.json),
add or modify any product YAML under
[`products/`](../../products/) or any WebFlash wrapper under
[`products/webflash/`](../../products/webflash/), regenerate
firmware, create a GitHub Release or tag, change any WebFlash
manifest / import, advance any of the named follow-up PRs
(`HW-PINMAP-311-FOLLOWUP`, `S360-311 schematic_status` promotion,
`PACKAGE-PWM-001` / `PACKAGE-GAP-001` FanPWM slice,
`PRODUCT-PWM-001`, `WEBFLASH-PWM-001`, `RELEASE-PWM-001`,
`WF-IMPORT-PWM-001`), advance `CORE-ABSTRACT-BUS-001` (alias for
[`docs/release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)),
close `S360-100-BENCH-001` (
[`s360-100-r4-core.md` S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status)),
close `HW-PINMAP-320-FOLLOWUP` (
[`s360-320-r4-triac.md` HW-PINMAP-320-FOLLOWUP audit log](s360-320-r4-triac.md#hw-pinmap-320-followup-audit-log)),
close `COMPLIANCE-001` (
[`../compliance/mains-voltage-uk-eu-assessment.md` COMPLIANCE-001 audit log](../compliance/mains-voltage-uk-eu-assessment.md#compliance-001-audit-log)),
change Release-One, change the LED preview path, unblock
FanTRIAC (HW-005), or change the Sense360 LED Release-One
exclusion. The only way to move this record off `partial —
schematic evidence available; package reconciliation pending` is
to land the evidence enumerated in
[§Required evidence before promotion](#required-evidence-before-promotion)
under the named follow-up PRs in
[§Follow-up PR sequence](#follow-up-pr-sequence).

| Audit date | Scope | Files inspected | Outcome |
|---|---|---|---|
| 2026-05-18 | HW-PINMAP-311-FOLLOWUP evidence-pass investigation (per the [`docs/cleanup-audit.md`](../cleanup-audit.md) follow-up PR list, after the 2026-05-18 [`S360-100-BENCH-001` evidence-pass re-check](../cleanup-audit.md#s360-100-bench-001-update-2026-05-18-evidence-pass-investigation), the [`HW-PINMAP-320-FOLLOWUP` evidence-pass re-check](../cleanup-audit.md#hw-pinmap-320-followup-update-2026-05-18-evidence-pass-investigation), the [`COMPLIANCE-001` evidence-pass re-check](../cleanup-audit.md#compliance-001-update-2026-05-18-mains-voltage-advancedmanual-warning-sign-off-evidence-pass-investigation), the [`PACKAGE-TRIAC-001` deferral](../cleanup-audit.md#package-triac-001-update-deferred--hw-005--hw-pinmap-320-followup--compliance-001-not-landed), the [`TRIAC-QUEUE-001` normalization](../cleanup-audit.md#triac-queue-001-update-remaining-fantriac-chain-normalized-after-package-deferral), the [`PRODUCT-TRIAC-002` deferral](../cleanup-audit.md#product-triac-002-update-deferred--package-triac-001-not-landed), and the [`PRODUCT-TRIAC-001` notes-only reclassification](../cleanup-audit.md#product-triac-001-update-reclassify-fantriac-as-advancedmanual-warning)). Re-check whether any new committed evidence supports (i) silkscreen reading of the Core-side `J6` 1-to-13 pin order (resolving [`s360-100-r4-core.md` Open Questions #9](s360-100-r4-core.md#open-questions--verification-needed) and the parallel module-side `J3` 1-to-13 silkscreen reading); (ii) physical 13-pin Core ↔ module harness inspection (conductor-by-conductor pin mapping); (iii) bench / scope / waveform capture of the per-fan PWM drive (`TachPMW*` on `J1` / `J2` / `J4` / `J5`), the per-fan tach (`Pul_Cou*`), or the shared open-drain `TachIO` net; (iv) pulses-per-revolution validation of the `multiply: 0.5` factor in [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) lines 76–80; (v) UART-on-`J3`-pins-11/12 resolution (extend the Core `J6` capture to record `UART_RX` / `UART_TX` on pins 11 / 12 of the 13-pin Core ↔ module cable, or document the separate harness alternative); (vi) PWM polarity (active-high vs active-low for the per-fan low-side N-FET gate path with 1 kΩ series resistor) and tach pull-up identification (discrete external, on-die SX1509 channel pull-up, or fan-side); (vii) the single-channel-vs-four-channel canonical abstraction decision for the FanPWM token (single `${fan_pwm_pin}` / `${fan_tach_pin}` pair vs four per-fan SX1509-channel bindings plus the direct-ESP32 `TachIO` passthrough); (viii) KiCad schematic source (`S360-311-R4.kicad_sch`), KiCad PCB source (`S360-311-R4.kicad_pcb`), KiCad project metadata (`*.kicad_pro` / `*.kicad_prl` / `fp-lib-table` / `sym-lib-table`), BOM, CPL / pick-and-place, Gerbers, drill files, STEP, or board images for `S360-311-R4`; or (ix) progress on the systemic Core abstract-bus rebind (`CORE-ABSTRACT-BUS-001`, alias for [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)) that owns the disagreement between the Core abstract `${expansion_gpio1}` / `${expansion_gpio2}` resolutions (`GPIO5` = `SEN0609_TX` / `GPIO6` = `out(gpio6)` on the ceiling Core; `GPIO4` / `GPIO5` on the generic Core) and the schematic-verified SX1509 channel routing for `TachPMW1..4` / `Pul_Cou1..4`. | [`docs/hardware/schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf) (HW-ASSETS-003 module-side schematic, byte-identical to upload; SHA256 `c910b3364be1d58fc44d12b5a189dade47efddf6cae158a86577ec7501e48006`; 91,543 bytes); [`docs/hardware/artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md) (HW-ASSETS-003 curated artifact index — uploaded inventory, schematic content capture, retained-but-not-committed list, artifact-side Open Questions, [§Files NOT provided in this upload](artifacts/S360-311-R4.md#files-not-provided-in-this-upload)); [`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md) §[J6 — 12 V PWM fan connector (13-pin)](s360-100-r4-core.md#j6--12-v-pwm-fan-connector-13-pin), §[Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs), §[Pin assignments](s360-100-r4-core.md#pin-assignments) (`IO16 = TachIO`), §[Open Questions #9](s360-100-r4-core.md#open-questions--verification-needed) (`J6` 1-to-13 silkscreen pin-order **verify**), and §[S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status) (bench / silkscreen / manufacturing-artifact gate); this document (§[Status](#status), §[Repository evidence](#repository-evidence), §[Schematic summary](#schematic-summary), §[Connector / pin-map findings](#connector--pin-map-findings), §[UART pins on `J3` pins 11–12](#uart-pins-on-j3-pins-1112), §[Per-fan-output drive topology (module side)](#per-fan-output-drive-topology-module-side), §[Open documentation questions](#open-documentation-questions), §[Existing package abstraction](#existing-package-abstraction), §[Parent Core packages that resolve `fan_pwm_pin` / `fan_tach_pin`](#parent-core-packages-that-resolve-fan_pwm_pin--fan_tach_pin), §[Reconciliation findings](#reconciliation-findings), §[Known unresolved issues](#known-unresolved-issues), §[Package YAML status](#package-yaml-status), §[Product / WebFlash status](#product--webflash-status), §[Required evidence before promotion](#required-evidence-before-promotion), §[Follow-up PR sequence](#follow-up-pr-sequence), §[Do-not-change guardrails](#do-not-change-guardrails)); [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) (single-channel; `output.platform: ledc` with `pin: ${fan_pwm_pin}` line 45; `sensor.platform: pulse_counter` with `pin.number: ${fan_tach_pin}` line 67; `fan_pwm_frequency: "25000"` line 32; `multiply: 0.5` "Standard fans output 2 pulses per revolution" lines 76–80; substitutions sourced from parent Core package per header comment lines 22–29); [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml) (legacy four-channel; `fan1_pwm_pin: GPIO7` / `fan2_pwm_pin: GPIO11` / `fan3_pwm_pin: GPIO13` / `fan4_pwm_pin: GPIO15` lines 40–43; `fan1_tach_pin: GPIO8` / `fan2_tach_pin: GPIO12` / `fan3_tach_pin: GPIO14` / `fan4_tach_pin: GPIO16` lines 46–49; `tach_io_pin: GPIO6` line 52; `nextion_tx_pin: GPIO43` / `nextion_rx_pin: GPIO44` lines 55–56); [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml) (SX1509 channel map header comment lines 1–10; output declarations lines 25–44; binary-sensor declarations lines 97–140 — channels 0–3 reserved for fan PWM outputs, 4–7 reserved for tachometer inputs); [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) (`expansion_gpio1: GPIO5` / `expansion_gpio2: GPIO6` lines 65–66; `fan_pwm_pin: ${expansion_gpio1}` / `fan_tach_pin: ${expansion_gpio2}` lines 72–73 — disagrees with Core schematic `IO5 = SEN0609_TX` / `IO6 = out(gpio6)` per [§Pin assignments](s360-100-r4-core.md#pin-assignments)); [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) (`expansion_gpio1: GPIO4` / `expansion_gpio2: GPIO5` lines 57–58 — same systemic disagreement); [`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml) (legacy pre-WebFlash standalone fan-board product YAML; consumes the four-channel [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml); classified `legacy-compatible`; not WebFlash-shippable; not the FanPWM productization candidate); [`config/hardware-catalog.json`](../../config/hardware-catalog.json) `S360-311` row lines 72–81 (`group: Inline`, `type: Driver`, `friendly_name: Sense360 PWM`, `sku: S360-311`, `rev: R4`, `old_name: 12vFan_PWM_PulseCounter`, `description: 12V PWM fan driver, up to 4 fans with tach feedback.`, `schematic_status: cataloged_unverified`, no `schematic_file`); [`config/product-catalog.json`](../../config/product-catalog.json) (no FanPWM-bearing entry; the two shipping entries are `Ceiling-POE-VentIQ-RoomIQ` `status: production` / `channel: stable` and `Ceiling-POE-VentIQ-RoomIQ-LED` `status: preview` / `channel: preview`; `Ceiling-POE-VentIQ-FanTRIAC-RoomIQ` stays `status: blocked` / `blocker: HW-005` / `webflash_build_matrix: false`, no `artifact_name`); [`config/webflash-builds.json`](../../config/webflash-builds.json) (no FanPWM-bearing build; only `Ceiling-POE-VentIQ-RoomIQ` stable and `Ceiling-POE-VentIQ-RoomIQ-LED` preview); [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) (`FanPWM` reserved in `canonical_modules` line 12, subject to the fan-driver `max-one-of` rule enforced by `FAN_DRIVER_TOKENS` in [`tests/validate_webflash_builds.py`](../../tests/validate_webflash_builds.py)); [`docs/hardware/board-readiness-matrix.md`](board-readiness-matrix.md) `S360-311` hardware-evidence and productization-axis rows + §[`S360-311` Sense360 PWM](board-readiness-matrix.md#s360-311-sense360-pwm) board-by-board notes (`partially-documented`, `not-needed-for-release-one`, `package-yaml-pending`); [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md) `fan_pwm.yaml` row (`needs-package-reconciliation` + `bench-evidence-pending`) and §[`fan_pwm.yaml` / S360-311](package-readiness-matrix.md#fan_pwmyaml--s360-311) detail; [`docs/hardware/firmware-package-mapping-audit.md`](firmware-package-mapping-audit.md) HW-009 row for `fan_pwm.yaml`; [`docs/hardware/remaining-board-documentation-audit.md`](remaining-board-documentation-audit.md) §[Sense360 PWM (`S360-311`)](remaining-board-documentation-audit.md#sense360-pwm-s360-311) (`partially-documented`, `not-needed-for-release-one`); [`docs/product-availability-taxonomy.md`](../product-availability-taxonomy.md) `S360-311` snapshot row (`partially-documented` + `design-pending`); [`docs/product-readiness-matrix.md`](../product-readiness-matrix.md) `Ceiling-POE-VentIQ-FanPWM-RoomIQ` row (`missing-product-yaml`); [`docs/webflash-exposure-readiness-matrix.md`](../webflash-exposure-readiness-matrix.md) §[PWM / S360-311 WebFlash posture](../webflash-exposure-readiness-matrix.md#pwm--s360-311-webflash-posture) (`not-webflash-ready`); [`docs/release-artifact-readiness-matrix.md`](../release-artifact-readiness-matrix.md) (no FanPWM artifact); [`docs/release-one-hardware-audit.md`](../release-one-hardware-audit.md) §[Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups) (Core abstract-bus rebind, alias `CORE-ABSTRACT-BUS-001`); [`docs/cleanup-audit.md`](../cleanup-audit.md) §[S360-100-BENCH-001 update](../cleanup-audit.md#s360-100-bench-001-update-2026-05-18-evidence-pass-investigation), §[HW-PINMAP-320-FOLLOWUP update](../cleanup-audit.md#hw-pinmap-320-followup-update-2026-05-18-evidence-pass-investigation), §[COMPLIANCE-001 update](../cleanup-audit.md#compliance-001-update-2026-05-18-mains-voltage-advancedmanual-warning-sign-off-evidence-pass-investigation), §[PACKAGE-TRIAC-001 update](../cleanup-audit.md#package-triac-001-update-deferred--hw-005--hw-pinmap-320-followup--compliance-001-not-landed), §[TRIAC-QUEUE-001 update](../cleanup-audit.md#triac-queue-001-update-remaining-fantriac-chain-normalized-after-package-deferral), §[PRODUCT-TRIAC-001 update](../cleanup-audit.md#product-triac-001-update-reclassify-fantriac-as-advancedmanual-warning), §[PRODUCT-TRIAC-002 update](../cleanup-audit.md#product-triac-002-update-deferred--package-triac-001-not-landed); [`docs/compliance/mains-voltage-uk-eu-assessment.md`](../compliance/mains-voltage-uk-eu-assessment.md) (COMPLIANCE-001 mains-voltage tracker — applies to `S360-320` / `S360-400`; does not gate FanPWM but tracked here as a sibling evidence record). | **Status remains `partial — schematic evidence available; package reconciliation pending`.** No new committed bench / silkscreen / harness / waveform / KiCad schematic source / KiCad PCB source / KiCad project metadata / BOM / CPL / Gerbers / drill / STEP / board-image evidence for `S360-311-R4` has been recorded since HW-PINMAP-311 landed; the artifact index at [`docs/hardware/artifacts/S360-311-R4.md` §Files NOT provided in this upload](artifacts/S360-311-R4.md#files-not-provided-in-this-upload) still marks every one of those classes as `not provided in this upload`. **The Core-side `J6` 1-to-13 silkscreen pin order remains `verify`** ([`s360-100-r4-core.md` Open Questions #9](s360-100-r4-core.md#open-questions--verification-needed)); the parallel module-side `J3` 1-to-13 silkscreen pin order is similarly **not** confirmed; no physical 13-pin Core ↔ module harness has been inspected, and the conductor-by-conductor `Core J6 pin N ↔ Module J3 pin N` mapping stays schematic-only. **The UART-on-`J3`-pins-11/12 routing question stays unresolved** — neither (a) extension of the Core-side `J6` capture in [`s360-100-r4-core.md` §J6](s360-100-r4-core.md#j6--12-v-pwm-fan-connector-13-pin) to record `UART_RX` / `UART_TX` on pins 11 / 12, nor (b) documentation of the separate harness alternative, has been committed. **No bench / scope / waveform evidence** is committed for the per-fan PWM drive (`TachPMW*` on `J1` / `J2` / `J4` / `J5`), the per-fan tach feedback (`Pul_Cou*`), or the shared open-drain `TachIO` net; no PWM polarity (active-high vs active-low for the per-fan low-side N-FET gate path with 1 kΩ series resistor), no tach pull-up source identification (discrete external, SX1509 on-die channel pull-up, or fan-side), and no pulses-per-revolution validation of the `multiply: 0.5` factor in [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) lines 76–80 are recorded. **The single-channel vs four-channel canonical abstraction decision is not made** — [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) still binds a single `${fan_pwm_pin}` / `${fan_tach_pin}` pair while the committed schematic exposes four 4-pin fan output connectors (`J1` / `J2` / `J4` / `J5`); the cardinality mismatch stays unresolved. **The SX1509-channel vs direct-ESP32-GPIO mapping disagreement stays unresolved** — the Core schematic routes per-fan `TachPMW1..4` / `Pul_Cou1..4` through the SX1509 (U3) I/O bank (channels 0–3 fan PWM, 4–7 tach per [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml)), with `TachIO` as the only direct ESP32 passthrough on `IO16`; the parent Core abstract packages bind `fan_pwm_pin` / `fan_tach_pin` to direct ESP32 expansion GPIOs (`expansion_gpio1` / `expansion_gpio2` → `GPIO5` / `GPIO6` on [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) lines 65–66, 72–73; `GPIO4` / `GPIO5` on [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) lines 57–58). `GPIO5` is `SEN0609_TX` (RoomIQ radar UART) and `GPIO6` is `out(gpio6)` per [`s360-100-r4-core.md` §Pin assignments](s360-100-r4-core.md#pin-assignments) — the direct-ESP32 abstract values disagree with the Core schematic both for fan-PWM routing **and** at the ESP32-pin-identity layer. Resolving that disagreement belongs to `CORE-ABSTRACT-BUS-001` (alias for [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)), not to HW-PINMAP-311-FOLLOWUP. **The `"NINE 4pin FANs"` section-title documentation question stays unresolved** — the schematic sheet still groups four visible fan-output connectors under a title that reads `"NINE 4pin FANs"`, with no committed evidence explaining whether it is a stale label copied from a different design, a marker / project code, or an undelivered intent. [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) is **not** edited by this re-check: `output.platform: ledc`, `pin: ${fan_pwm_pin}`, `fan_pwm_frequency: "25000"`, `sensor.platform: pulse_counter`, `pin.number: ${fan_tach_pin}`, `mode: input: true, pullup: true`, `multiply: 0.5`, the fan-stall logic, the quiet-mode logic, and the substitution comment block (lines 22–29) are all preserved. [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml) is **not** edited by this re-check: the legacy four-channel `fan1..4_pwm_pin` / `fan1..4_tach_pin` / `tach_io_pin` / `nextion_tx_pin` / `nextion_rx_pin` bindings are preserved; consumption by [`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml) (`legacy-compatible`) is preserved. [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml), [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml), and [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) are also **not** edited. Downstream gates are unchanged by this re-check: `S360-311` `schematic_status` stays `cataloged_unverified` with no `schematic_file` in [`config/hardware-catalog.json`](../../config/hardware-catalog.json) (the `S360-311 schematic_status` promotion remains gated on HW-PINMAP-311-FOLLOWUP); `HW-PINMAP-311-FOLLOWUP` stays **outstanding** (no standalone schematic-backed reference doc in the per-board `docs/hardware/s360-311-r4-*.md` pattern; no pin-map reconciliation; no UART-on-J3-11/12 resolution; no single-vs-four-channel decision; no PWM polarity / tach pull-up / pulses-per-revolution decisions); `PACKAGE-PWM-001` (alias: `PACKAGE-GAP-001` FanPWM slice) stays **blocked** and the `fan_pwm.yaml` row in [`package-readiness-matrix.md`](package-readiness-matrix.md) stays `needs-package-reconciliation` + `bench-evidence-pending`; `PRODUCT-PWM-001`, `WEBFLASH-PWM-001`, `RELEASE-PWM-001` (alias: `RELEASE-GAP-001` FanPWM slice), and `WF-IMPORT-PWM-001` (alias: `WF-IMPORT-GAP-001` FanPWM slice) stay **blocked** behind `PACKAGE-PWM-001`; no FanPWM-bearing entry exists in [`config/product-catalog.json`](../../config/product-catalog.json), [`config/webflash-builds.json`](../../config/webflash-builds.json), [`products/`](../../products/), or [`products/webflash/`](../../products/webflash/); `FanPWM` stays reserved in [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) `canonical_modules` (line 12) subject to the fan-driver `max-one-of` rule; `CORE-ABSTRACT-BUS-001` is not advanced; `S360-100-BENCH-001` is not closed (the Core J6 1-to-13 silkscreen verification gate stays `pending — bench/manufacturing evidence required`); `HW-PINMAP-320-FOLLOWUP` is not closed; `COMPLIANCE-001` is not closed. Release-One is unchanged: stays `Ceiling-POE-VentIQ-RoomIQ`, version `1.0.0`, channel `stable`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`, tag [`v1.0.0`](https://github.com/sense360store/esphome-public/releases/tag/v1.0.0). LED preview is unchanged: stays `Ceiling-POE-VentIQ-RoomIQ-LED`, `status: preview`, `channel: preview`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-LED-v1.0.0-preview.bin`. The Sense360 LED Release-One exclusion is unchanged. FanTRIAC stays `status: blocked`, `blocker: HW-005`, `webflash_build_matrix: false`. The Core J10 vs RoomIQ J6 pin-order open question is not resolved. No FanPWM-bearing product / WebFlash / release / import is made ready by this re-check. |
| 2026-05-25 | `PWM-BLOCKER-REMOVAL-001` evidence-pass: re-check the S360-311 / FanPWM lane against repo **and** Google Drive evidence now that Relay (#579) / FanDAC (#580/#581) are clean and [`repo-freshness-roadmap-audit.md`](../repo-freshness-roadmap-audit.md) names PWM the next hardware blocker; classify each blocker row (see [§PWM-BLOCKER-REMOVAL-001 readiness / blocker table](#pwm-blocker-removal-001-readiness--blocker-table)); record the Drive `12vFan_PWM_PulseCounter` artifact set with provenance; cross-check the Drive BOM against the committed `S360-311-R4` schematic; re-confirm the post-`CORE-ABSTRACT-BUS-001A/B/C` state of the parent Core bindings. | Drive folder `12vFan_PWM_PulseCounter` (owner `neilmcrae@googlemail.com`; = `config/hardware-catalog.json` `S360-311` `old_name`): `bom/12vFan_PWM_PulseCounter.xlsx` (11,112 B), `sch_pdf/12vFan_PWM_PulseCounter.pdf` (357,151 B; KiCad 9.0.2; pre-rename project name; blank Rev), `images/12vFan_PWM_PulseCounter*.png` (×3 board renders; silkscreen `SENSE 360 FAN`, `J3` / `J5` / `J6`, `pwm1..4`, `pul1..4`, `rx`, `tx`, `Q1..Q4`), `gerbers/` / `cpl/` / `step_file/` / `Assets/` (existence recorded; not committed); committed [`schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf) (KiCad 10.0.3; blank Rev) and [`artifacts/S360-311-R4.md`](artifacts/S360-311-R4.md); [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) (still single-channel `ledc` + `pulse_counter`); [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) (post-`001C`: `expansion_gpio1..4` retired; no `fan_pwm_pin` / `fan_tach_pin`; `fan_status_led_pin: GPIO46`, `roomiq_sen0609_output_pin: GPIO6`), [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) and [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml) (`fan_pwm_pin: GPIO4` / `fan_tach_pin: GPIO5` direct-ESP32 binding), [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml) (`sx1509_i2c_id: core_i2c` — `001B` landed); [`config/compile-only-candidates.json`](../../config/compile-only-candidates.json) `Ceiling-POE-FanPWM` row; [`tests/test_compile_expansion_candidates.py`](../../tests/test_compile_expansion_candidates.py) (FanPWM-candidate core-bus-blocker invariant). | **Status remains `partial — schematic evidence available; package reconciliation pending`** (an audit-log entry cannot promote the top-line status). **Blockers closed at this pass:** hardware-evidence (row 1) and controlled-load-type (row 5) are **CLOSED**; schematic/BOM identity (row 2) is **CLOSED at the part-identity layer** by the Drive BOM 1:1 cross-check (`U1` MT3608 boost, `Q1`–`Q4` `ME15N10-G` low-side N-FETs, `D1` SS34, `L1` SRN6045TA 22 µH, `R3` 38k / `R5` 2k divider, `R4` 330R, `R1`/`R2`/`R6`/`R7` 1k gate, `C1`/`C2` 22 µF/10V, `J1`/`J2`/`J4`/`J5`/`J6` JST-SH `SM04B`, `J3` JST-SH `SM13B`) — same standard `HW-BOM-ASSETS-002` applied to `S360-400` / `S360-410`; compliance (row 11) is **CLOSED — no mains gate** (SELV 5 V→12 V boost; `COMPLIANCE-001` does not apply). **Output electrical characteristics (row 6)** advance to **PARTIAL** (topology + part identities known; per-fan / aggregate current + thermal envelope still owed). **Still blocking:** Core connector / GPIO mapping (row 3), PWM output pin mapping (row 4), package-YAML correctness (row 7), product eligibility (row 8), compile-only eligibility (row 9), WebFlash/release (row 10 — out of scope here). **No binary committed** (`.xlsx` / gerbers / CPL / STEP / PNG stay retained-but-not-committed per [`hardware-artifact-policy.md`](hardware-artifact-policy.md)); `S360-311` `schematic_status` stays `cataloged_unverified` with no `schematic_file`; [`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) and [`sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml) unedited; no Core / SX1509 package edited; no `config/**` edited (the `Ceiling-POE-FanPWM` compile-only candidate keeps `defer` and all four blockers — its `CORE-ABSTRACT-BUS-001B-not-landed` + scaffold-missing labels are flagged **stale** for a later refresh, **not** flipped here); no FanPWM product / WebFlash / release / import made ready. **`CORE-ABSTRACT-BUS-001B` no longer blocks at the shared-I²C-bus layer** (rename to `core_i2c` landed via `CORE-ABSTRACT-BUS-001B-IMPLEMENT-001`); the remaining `PACKAGE-PWM-001` gate is the FanPWM-specific operator + bench set enumerated in [§Next-PR designation](#next-pr-designation). `PACKAGE-PWM-001-IMPLEMENT-001` stays **NOT READY**; this PR is the `PACKAGE-PWM-001-READINESS-REFRESH`. Release-One (`Ceiling-POE-VentIQ-RoomIQ` / `v1.0.0` / `stable`), the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |
| 2026-05-25 (evidence-pass 2) | `PWM-BLOCKER-REMOVAL-001` second evidence-pass: inspect the Drive project tracker `Sense360_R4_Tracker` (not cited at the first pass) for any status that closes or refines a FanPWM blocker row; re-run the repo validation suite to confirm the docs-only state is green. | Drive spreadsheet `Sense360_R4_Tracker` (Google Sheet; owner `neilmcrae@googlemail.com`; modified 2026-05-18; R4 shared drive) — Boards tab `S360-311` row (1:1 with [`config/hardware-catalog.json`](../../config/hardware-catalog.json)); Tasks tab `R07` (S360-311 → `S360-311 R4` rename, **Done**), `G01` (fleet-wide P/N/REV/date-code silkscreen, **Waiting**; *"not currently on the boards"*), `N01` (Neil to supply logo SVG, **To do**), `C02` (Core `S360-100` fan connector → **XH2.54**, **Done**); cross-referenced against the committed Drive BOM (`12vFan_PWM_PulseCounter.xlsx`: module connectors JST-SH `SM04B`/`SM13B`) and [§Project-tracker corroboration](#project-tracker-corroboration-drive-sense360_r4_tracker-evidence-pass-2). | **Status remains `partial — schematic evidence available; package reconciliation pending`** (a tracker is provenance, not committed evidence, and cannot promote the top-line status). **Refinements:** row 2 (schematic/BOM identity) stays **CLOSED (part-identity)** — the residual explicit-`R4`-rev-stamp item is now **explained** (the rename `R07` is Done; the blank `REV` is the tracked fleet-wide silkscreen task `G01`/`N01`, pending Neil's SVG — not unresolved provenance); row 3 (Core connector / GPIO mapping) stays **STILL BLOCKING**, now with a sharpened sub-item — the Core fan connector is **XH2.54** (`C02` Done) while the module is **JST-SH** (`SM04B`/`SM13B`), a connector-type/pitch reconciliation owed to `HW-PINMAP-311-FOLLOWUP` (either the committed module schematic/BOM predates a matching module-side change, or the harness adapts). `G01` (Waiting) also confirms **no R4 board yet carries a P/N/REV/date-code silkscreen**, so the Core `J6` / module `J3` 1-to-13 silkscreen pin-order `verify` flags cannot be closed from current renders. **Operator decisions captured this pass** (see [§Operator decisions](#operator-decisions-pwm-blocker-removal-001-2026-05-25)): **D1** four-channel canonical abstraction; **D2** SX1509 routing (`TachPMW1..4`→ch0..3, `Pul_Cou1..4`→ch4..7, `TachIO` direct `IO16`); **D3** committed JST-SH schematic is current R4 (harness adapts XH2.54↔JST-SH). These resolve the design-intent gates on rows 4 / 7 and the connector-identity sub-item on row 3, and set the explicit next prerequisite as a `CORE-ABSTRACT-BUS-001` / SX1509 four-channel binding follow-up (operator: record, do not fabricate). **No blocker reclassified to CLOSED by this pass** (the resolved items are design decisions, not bench/silkscreen evidence); rows 3/4/7 stay STILL BLOCKING on implementation + bench, rows 8/9/10 stay blocking, row 6 stays PARTIAL. Nothing committed from Drive; no catalog / package / product / config / WebFlash / workflow / firmware / binary edit. Validation suite re-run green (`tests/validate_configs.py`, `scripts/validate_compile_targets.py --metadata-only`, `tests/test_compile_targets.py`, `tests/test_firmware_combination_matrix.py`, `tests/test_firmware_build_gap_report.py`, `tests/validate_webflash_builds.py`, `python3 -m unittest discover -s tests -p "test_*.py"`). `PACKAGE-PWM-001-IMPLEMENT-001` stays **NOT READY**. Release-One, the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |

| 2026-05-25 (`CORE-ABSTRACT-BUS-SX1509-001`) | Land the explicit next prerequisite named in [§Next-PR designation](#next-pr-designation): a neutral, binding-only Core abstract-bus layer that re-anchors the four FanPWM PWM-drive lines + four tach lines onto the SX1509 expander (operator decision D2), with `TachIO` direct on `IO16`, **without** adding any FanPWM product / WebFlash / release surface or fabricating direct-GPIO mappings. Verify ESPHome can actually back the binding (PWM through SX1509; tach via `pulse_counter`). | New package [`packages/expansions/fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml) (SX1509 hub `id: sx1509_expander` on `i2c_id: ${sx1509_i2c_id}` default `core_i2c`, `address: 0x3E`, `sx1509_interrupt_pin: GPIO17`; four `output: platform: sx1509` drive outputs `fan_pwm_drive_1..4` on channels 0..3; four `binary_sensor: platform: gpio` tach inputs `fan_pwm_tach_1..4` routed through the expander on channels 4..7; `tach_io_pin: GPIO16` substitution for the direct line); new regression classes in [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) (`FanPwmSx1509BindingExistsTests`, `FanPwmSx1509BoundThroughCoreI2cTests`, `FanPwmSx1509EightChannelTests`, `FanPwmTachIoDirectOnGpio16Tests`, `FanPwmNoDirectEsp32FanChannelTests`, `FanPwmSx1509NeutralityTests`, `FanRelayAndFanDacUnchangedTests`); cross-checked against [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml) (channels 0–3 PWM / 4–7 tach, same hub binding), [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) (single-channel `ledc` + `pulse_counter` on a direct GPIO — left unchanged), [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs) (`TachPMW1..4` / `Pul_Cou1..4` SX1509-routed; `TachIO` = `IO16`), and the upstream ESPHome `sx1509` / `pulse_counter` component contracts. | **Status remains `partial — schematic evidence available; package reconciliation pending`** (a Core-bus binding prerequisite does not promote this audit's top-line status). **What landed:** the `CORE-ABSTRACT-BUS-001` / SX1509 four-channel binding prerequisite is now provided as a neutral binding-only package — the explicit gate named in [§Operator decisions](#operator-decisions-pwm-blocker-removal-001-2026-05-25) D2 and [§Next-PR designation](#next-pr-designation). **ESPHome capability finding (recorded, not faked):** SX1509 **PWM drive** (channels 0..3) IS supported via the on-chip LED-driver PWM (`output: platform: sx1509`); SX1509 **tach** (`Pul_Cou1..4`, channels 4..7) is **NOT** supported as a hardware `pulse_counter` (that platform requires an interrupt-capable native `InternalGPIOPin`; an expander pin is not one), so the tach channels are exposed as polled binary GPIO state only and per-fan RPM via the expander becomes a **new documented blocker** for `PACKAGE-PWM-001-IMPLEMENT-001` (resolve via the direct `tach_io_pin` / `IO16` line, a future custom component, or a bench-decided alternative). **What this PR explicitly does NOT do:** it does not close any bench / silkscreen / current / thermal / PWM-polarity / tach-pull-up / pulses-per-revolution evidence (rows 4 / 6 / 7 of the [§Blocker table](#pwm-blocker-removal-001-readiness--blocker-table) stay STILL BLOCKING / PARTIAL on bench); it does not promote `S360-311` `schematic_status` (stays `cataloged_unverified`, no `schematic_file`); it does not edit [`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml), [`sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml), [`gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml), any Core abstract package, or any `config/**`; it adds no FanPWM product YAML / WebFlash wrapper / `webflash_build_matrix` flip / `artifact_name` / release artifact, and **FanRelay** and **FanDAC** are unchanged. It does **not** make `PACKAGE-PWM-001-IMPLEMENT-001` implementation-ready — the remaining package blockers (bench evidence, expander-tach RPM, single-channel `fan_pwm.yaml` four-channel reconciliation, JSON promotion) are not all closed — and the FanPWM **product / WebFlash / release** surface stays blocked. No compile-only target was added. Release-One (`Ceiling-POE-VentIQ-RoomIQ` / `v1.0.0` / `stable`), the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |
| 2026-05-25 (`PACKAGE-PWM-TACH-STRATEGY-001`) | Classify and resolve the tach/RPM blocker that `CORE-ABSTRACT-BUS-SX1509-001` surfaced (SX1509 expander pins cannot back a hardware `pulse_counter`, so per-fan RPM via `Pul_Cou1..4` is un-implementable). Evaluate the five candidate tach strategies (A no per-fan RPM / B direct-`TachIO` aggregate / C custom component / D hardware rework / E alternative ESPHome component); put the scope question to the operator where evidence alone could not decide; record the decision and re-scope the next implementation PR. Docs-only. | This document (new [§Tach / RPM strategy (PACKAGE-PWM-TACH-STRATEGY-001)](#tach--rpm-strategy-package-pwm-tach-strategy-001), [§Next-PR designation](#next-pr-designation), [§PWM-BLOCKER-REMOVAL-001 readiness / blocker table](#pwm-blocker-removal-001-readiness--blocker-table)); [`packages/expansions/fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml) (capability section: PWM drive via `output: platform: sx1509`; tach as polled `binary_sensor: platform: gpio`; `tach_io_pin: GPIO16`); [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml) (tach as `binary_sensor: platform: gpio`, no `pulse_counter`); [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) / [`packages/expansions/sense360_fan_pwm.yaml`](../../packages/expansions/sense360_fan_pwm.yaml) / [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml) (the only `pulse_counter` uses — all on native ESP32 GPIOs); [`packages/hardware/sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml) (W5500 `RST` on `GPIO16`), [`packages/hardware/sense360_core_voice_ceiling.yaml`](../../packages/hardware/sense360_core_voice_ceiling.yaml) / [`packages/hardware/sense360_core_voice_wall.yaml`](../../packages/hardware/sense360_core_voice_wall.yaml) (Speaker I2S `DIN` on `GPIO16`) — the `GPIO16` conflict surface; [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs) (`TachIO` = shared MOSFET-drain rail on `IO16`); [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md) and [`docs/product-readiness-matrix.md`](../product-readiness-matrix.md) (addenda updated). | **Status remains `partial — schematic evidence available; package reconciliation pending`** (a strategy decision does not promote the top-line status). **Decision recorded (operator-attributed, 2026-05-25):** **D-T1** — the first FanPWM package is allowed to be **PWM-drive-only** (four SX1509 PWM outputs on channels 0..3; **no** per-fan RPM sensors; optional diagnostic binary tach states only on channels 4..7; per-fan RPM deferred to future work); **D-T2** — `TachIO` / `GPIO16` is **reserved/pending** with no aggregate-RPM and no per-fan-RPM claim and no `TachIO` sensor in the initial package. **Strategy A selected**; B (direct-`TachIO` aggregate) deferred (needs bench + `GPIO16` conflict resolution); C (custom component) deferred to a future `COMPONENT-SX1509-TACH-001`, **not built here**; D (hardware rework) out of scope; E (alternative ESPHome component) verified to not exist. **Effect on gates:** the expander-tach RPM gate on `PACKAGE-PWM-001-IMPLEMENT-001` is **resolved-by-deferral** (PWM-only scope); tach-pull-up / pulses-per-revolution stop gating the PWM-only slice (no RPM exposed); the remaining gate narrows to **PWM polarity** + **per-fan / aggregate current + thermal envelope** (bench) + the **four-channel reconciliation** of `fan_pwm.yaml`. **What this PR does NOT do:** no package / product / config / WebFlash / workflow / firmware / binary edit; no custom component built; no RPM support claimed (per-fan **or** aggregate); `S360-311` `schematic_status` stays `cataloged_unverified`; `fan_pwm.yaml`, `fan_pwm_sx1509.yaml`, `sense360_fan_pwm.yaml`, `gpio_expander_sx1509.yaml`, and all Core abstract packages are **unedited**; the FanPWM product / WebFlash / release surface stays blocked. `PACKAGE-PWM-001-IMPLEMENT-001` (PWM-only) is the designated next PR but is **not** made implementation-ready by this PR (the bench PWM-polarity / current-thermal set and the four-channel reconciliation remain open). Release-One (`Ceiling-POE-VentIQ-RoomIQ` / `v1.0.0` / `stable`), the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |
| 2026-05-25 (`PWM-SX1509-TACH-PROOF-001`) | Replace the previously-inferred / over-strong SX1509 tach blocker wording with an actual ESPHome compile/config proof. Prior wording asserted, by reasoning from `InternalGPIOPin`, that SX1509 expander pins "cannot" back a `pulse_counter`; prove the repo behaviour with a minimal `esphome config` target. Keep PWM-output support and GPIO/binary-input support distinct from the `pulse_counter`/RPM limitation. | New proof fixture [`tests/esphome/sx1509_pulse_counter_proof.yaml`](../../tests/esphome/sx1509_pulse_counter_proof.yaml) (SX1509 hub on `core_i2c`; channel 4 = `Pul_Cou1`; behind a `sensor: platform: pulse_counter`); new test [`tests/test_sx1509_tach_pulse_counter_proof.py`](../../tests/test_sx1509_tach_pulse_counter_proof.py) (runs the proof when the `esphome` CLI is available, else skips; two control checks; wording-regression guards); capability note in [`packages/expansions/fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml) rewritten from inferred to compile-proven; addenda in [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md) and [`docs/product-readiness-matrix.md`](../product-readiness-matrix.md); [`UPCOMING_PR.md`](../../UPCOMING_PR.md). | **Status remains `partial — schematic evidence available; package reconciliation pending`** (a config proof does not promote the top-line status). **Proof result (ESPHome 2026.5.1, `esphome config`, exit code 2):** the fixture is **rejected** with `[sx1509] is an invalid option for [pin]`. The `pulse_counter` pin schema accepts only an interrupt-capable native ESP32 `InternalGPIOPin`; the SX1509 expander pin schema is not a valid option for it. Two control checks confirm the rejection is `pulse_counter`+SX1509-specific: the same SX1509 pin validates as a `binary_sensor: platform: gpio` (`esphome config` passes), and `pulse_counter` validates on a native ESP32 GPIO (`esphome config` passes). **Wording corrected:** SX1509 PWM-drive **output** IS supported (`output: platform: sx1509`) and stays the basis of FanPWM drive; SX1509 GPIO/binary **input** is supported; **only** per-fan RPM via an SX1509 `pulse_counter` is **compile-proven unsupported by ESPHome validation** (no longer merely inferred from component internals or attributed to uncited docs). **Decision:** the proof **confirms** the PWM-drive-only-first strategy (`PACKAGE-PWM-TACH-STRATEGY-001`), so the next PR **remains** `PACKAGE-PWM-001-IMPLEMENT-001` scoped PWM-drive-only; per-fan RPM stays future work (`COMPONENT-SX1509-TACH-001` or a bench-confirmed `TachIO` follow-up). **What this PR does NOT do:** no FanPWM product YAML / WebFlash wrapper / `webflash_build_matrix` flip / `artifact_name` / release artifact / compile-only target; no edit to `fan_pwm.yaml`, `sense360_fan_pwm.yaml`, `gpio_expander_sx1509.yaml`, any Core abstract package, or any `config/**`; `S360-311` `schematic_status` stays `cataloged_unverified`; no WebFlash / import / release / compliance / hardware-readiness claim. FanRelay and FanDAC are unchanged; Release-One (`Ceiling-POE-VentIQ-RoomIQ` / `v1.0.0` / `stable`), the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |
| 2026-05-25 (`PACKAGE-PWM-001-IMPLEMENT-001`) | Implement the canonical FanPWM package as **PWM-drive-only** (operator decision D-T1 / `PACKAGE-PWM-TACH-STRATEGY-001`, confirmed by the `PWM-SX1509-TACH-PROOF-001` proof): reconcile [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) from the legacy single-channel `ledc` + `pulse_counter` form into the four-channel SX1509 PWM-drive form by **composing** the neutral binding [`packages/expansions/fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml) (operator decision D1/D2). Package layer only — no product / WebFlash / release / compile-only / config / firmware edit; no RPM of any kind. | [`packages/expansions/fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml) **rewritten** to the PWM-drive-only canonical package (`packages:` `!include fan_pwm_sx1509.yaml`; four `fan: platform: speed` controllers `fan_pwm_1..4` driving the neutral SX1509 PWM-drive outputs `fan_pwm_drive_1..4` on channels 0..3; no `sensor`, no `pulse_counter`, no `ledc`, no `${tach_io_pin}` / `GPIO16` consumer); [`packages/expansions/fan_pwm_sx1509.yaml`](../../packages/expansions/fan_pwm_sx1509.yaml) (composed, **unchanged** — still owns the SX1509 hub, the four PWM-drive outputs, the four diagnostic tach binary inputs `fan_pwm_tach_1..4` on channels 4..7, and `tach_io_pin: GPIO16`); new [`tests/test_fan_pwm_package.py`](../../tests/test_fan_pwm_package.py) (four independent PWM-drive channels; reuse-not-duplicate of the binding; no `pulse_counter`; no RPM; `TachIO`/`GPIO16` reserved; no direct ESP32 mapping; no product / WebFlash surface; FanRelay + FanDAC unchanged); cross-checked against [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) / [`packages/expansions/fan_dac.yaml`](../../packages/expansions/fan_dac.yaml) (unchanged); addenda in [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md), [`docs/product-readiness-matrix.md`](../product-readiness-matrix.md), [`docs/repo-freshness-roadmap-audit.md`](../repo-freshness-roadmap-audit.md), and [`UPCOMING_PR.md`](../../UPCOMING_PR.md). | **Status remains `partial — schematic evidence available; package reconciliation pending`** (a package implementation does not promote the top-line status, which is gated on bench / silkscreen / harness evidence). **What landed:** the **package-layer** four-channel reconciliation of `fan_pwm.yaml` for the **PWM-drive-only** scope — four independent SX1509 PWM-drive fan-speed controllers (channels 0..3), composing the neutral binding rather than duplicating it. **SX1509 PWM-drive output is supported and is the mechanism used** (`output: platform: sx1509`, the on-chip LED-driver PWM engine). **No RPM is implemented or claimed:** per-fan RPM via an SX1509 `pulse_counter` is compile-proven unsupported (`PWM-SX1509-TACH-PROOF-001`: `esphome config` rejects it with `[sx1509] is an invalid option for [pin]`), so the package wires **no** `pulse_counter` and **no** RPM sensor; the four `Pul_Cou1..4` lines stay as the binding's INTERNAL diagnostic binary GPIO states (`fan_pwm_tach_1..4`), never labelled or surfaced as RPM. **`TachIO` / `GPIO16` stays reserved/pending** — the package binds no `TachIO` sensor and makes no aggregate-RPM claim. **What this PR does NOT do / claim:** no FanPWM product YAML / WebFlash wrapper / `webflash_build_matrix` flip / `artifact_name` / release artifact / compile-only target; no edit to `sense360_fan_pwm.yaml`, `gpio_expander_sx1509.yaml`, any Core abstract package, `products/**`, `products/webflash/**`, `config/**`, `.github/workflows/**`, `components/**`, `include/**`, or `firmware/**`; `S360-311` `schematic_status` stays `cataloged_unverified`; no WebFlash / import / release / compliance / hardware-stable / product-readiness claim. **Remaining gates before any FanPWM product surface:** bench **PWM polarity** (active-high vs active-low for the per-fan low-side N-FET gate path); per-fan / aggregate **current + thermal envelope** (MT3608 ceiling); **product YAML**; **compile-only target / result**; **WebFlash / release / import / compliance**; and the **optional future RPM strategy** (`COMPONENT-SX1509-TACH-001` or a bench-confirmed `TachIO` follow-up). FanRelay and FanDAC are unchanged; Release-One (`Ceiling-POE-VentIQ-RoomIQ` / `v1.0.0` / `stable`), the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |
| 2026-05-25 (`FW-COMPILE-PWM-001`) | Add a single **compile-only validation target** for the PWM-drive-only FanPWM package landed by `PACKAGE-PWM-001-IMPLEMENT-001` / PR #590, closing the compile-only side of [§Blocker table](#pwm-blocker-removal-001-readiness--blocker-table) row 9. Keep it non-product / non-WebFlash; do **not** add a product YAML, a WebFlash wrapper, a `config/webflash-builds.json` row, an `artifact_name`, a release artifact, or any RPM claim. | New compile-only skeleton [`products/compile-only/ceiling-poe-fanpwm.yaml`](../../products/compile-only/ceiling-poe-fanpwm.yaml) (Core ceiling + PoE PSU + base + health + `!include ../../packages/expansions/fan_pwm.yaml`; config string `Ceiling-POE-FanPWM`); new target row in [`config/compile-only-targets.json`](../../config/compile-only-targets.json) (`id: ceiling-poe-fanpwm-compile-only`, `shipment_status: compile-only`, `webflash_exposure_allowed_now: false`, `hardware_required_for_validation: true`, `blocked: false`, `compile_validation_status: pending-ci`, `rpm_supported: false`; `totals.targets` 9 → 10); [`config/compile-only-candidates.json`](../../config/compile-only-candidates.json) (`Ceiling-POE-FanPWM` added to `currently_compile_only_config_strings`; the `defer` candidate row's blockers refreshed to keep a retained `PACKAGE-PWM-001` + `CORE-ABSTRACT-BUS-001B` history, `compile_only_safe` stays `false`); new `FanPWMCompileOnlyCoverageTests` in [`tests/test_compile_targets.py`](../../tests/test_compile_targets.py); `products/compile-only/` exemption added to [`tests/test_fan_pwm_package.py`](../../tests/test_fan_pwm_package.py); addenda in [`docs/compile-only-firmware-validation.md`](../compile-only-firmware-validation.md), [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md), [`docs/product-readiness-matrix.md`](../product-readiness-matrix.md), and [`UPCOMING_PR.md`](../../UPCOMING_PR.md). | **Status remains `partial — schematic evidence available; package reconciliation pending`** (a compile-only metadata target does not promote the top-line status, which is gated on bench / silkscreen / harness evidence). **What landed:** one compile-only **validation** target that exercises the PWM-drive-only FanPWM package syntax / substitutions / includes (SX1509 hub on `core_i2c`; four `output: platform: sx1509` PWM-drive outputs; four `fan: platform: speed` controllers). **What it validates:** the **PWM-drive-only package scope** only. **What it does NOT claim:** **no RPM support** (the package wires no `pulse_counter` and no RPM sensor — an SX1509 expander pin is compile-proven unable to back an ESPHome `pulse_counter` per `PWM-SX1509-TACH-PROOF-001`; `TachIO` / `GPIO16` stays reserved); **no product / WebFlash / release / import readiness** (`PRODUCT-PWM-001` / `WEBFLASH-PWM-001` / `RELEASE-PWM-001` / `WF-IMPORT-PWM-001` stay blocked; `S360-311` `schematic_status` stays `cataloged_unverified`); **no full compile success** — `compile_validation_status: pending-ci`, so a GitHub Actions / `workflow_dispatch` `--compile` run remains required before any compile result is claimed (only the metadata lane is asserted). **What this PR does NOT edit:** `products/webflash/**`, `config/webflash-builds.json`, `firmware/**`, `manifest.json`, `firmware/sources.json`, `.github/workflows/**`, `components/**`, `include/**`, `fan_pwm.yaml`, `fan_pwm_sx1509.yaml`, any Core abstract package, or any release artifact / checksum / build-info manifest. The bench gates (PWM polarity; per-fan / aggregate current + thermal envelope) and the optional future RPM strategy (`COMPONENT-SX1509-TACH-001` or a bench-confirmed `TachIO` follow-up) stay open. FanRelay and FanDAC are unchanged; Release-One (`Ceiling-POE-VentIQ-RoomIQ` / `v1.0.0` / `stable`), the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |
| 2026-05-25 (`FW-COMPILE-PWM-RESULT-001`) | Record the **full ESPHome compile result** for the FanPWM compile-only target now that CI has run the `--compile` lane. Flip the target's `compile_validation_status` from `pending-ci` to `validated-full-compile`; record the run id. Keep it compile-only: no product YAML, no WebFlash wrapper, no `config/webflash-builds.json` row, no `artifact_name`, no release artifact, no RPM claim. | [`config/compile-only-targets.json`](../../config/compile-only-targets.json) (`ceiling-poe-fanpwm-compile-only`: `compile_validation_status` `pending-ci` → `validated-full-compile`; `rpm_supported` stays `false`; `shipment_status` stays `compile-only`; `webflash_exposure_allowed_now` stays `false`); [`tests/test_compile_targets.py`](../../tests/test_compile_targets.py) (`FanPWMCompileOnlyCoverageTests.test_fanpwm_compile_validated_by_full_compile_run` asserts the flipped status / run id). | **Status remains `partial — schematic evidence available; package reconciliation pending`** (a compile result does not promote the top-line status). **Result:** the `Compile-only Firmware Validation` run `26414398902` (`compile_mode=full`, 10 compile-only targets, conclusion `success`) ran `scripts/validate_compile_targets.py --compile` against [`products/compile-only/ceiling-poe-fanpwm.yaml`](../../products/compile-only/ceiling-poe-fanpwm.yaml) and **passed**, so the PWM-drive-only package composition is now **full-compile-validated**. **What this records is the compile-only result ONLY:** it is **not** WebFlash exposure, **not** WebFlash import, **not** a release artifact, **not** RPM support, **not** compliance approval, and **not** hardware-stable readiness — `webflash_exposure_allowed_now` stays `false`, `shipment_status` stays `compile-only`, and `rpm_supported` stays `false`. `PRODUCT-PWM-001` / `WEBFLASH-PWM-001` / `RELEASE-PWM-001` / `WF-IMPORT-PWM-001` stay blocked; `S360-311` `schematic_status` stays `cataloged_unverified`. FanRelay and FanDAC are unchanged; Release-One, the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |
| 2026-05-26 (`PRODUCT-PWM-001`) | Add the canonical FanPWM **product YAML** as the **product-YAML-only / no-WebFlash-exposure** slice, after the PWM-drive-only package (PR #590) and the validated full compile (run `26414398902`, PR #591 / #592). Compose the same proven base as the compile-only skeleton (Core ceiling + PoE PSU + base + health + `!include ../packages/expansions/fan_pwm.yaml`). Add the matching non-WebFlash catalog row only. Do **not** add a WebFlash wrapper, a `config/webflash-builds.json` row, a `webflash_build_matrix` flip, an `artifact_name`, a release artifact, or any RPM claim; keep the compile-only target as the separate CI proof target. | New product YAML [`products/sense360-ceiling-poe-fanpwm.yaml`](../../products/sense360-ceiling-poe-fanpwm.yaml) (config string `Ceiling-POE-FanPWM`; composes Core ceiling + PoE PSU + base/health + the PWM-drive-only [`fan_pwm.yaml`](../../packages/expansions/fan_pwm.yaml); carries explicit full-compile-validated / no-RPM / PWM-polarity / current-thermal / TachIO-reserved / no-WebFlash / no-release / no-compliance / not-hardware-stable caveat wording); new non-WebFlash catalog row in [`config/product-catalog.json`](../../config/product-catalog.json) (`Ceiling-POE-FanPWM`, `status: hardware-pending`, `webflash_build_matrix: false`, no `artifact_name`, no `webflash_wrapper`); regenerated [`config/firmware-combination-matrix.json`](../../config/firmware-combination-matrix.json) (`Ceiling-POE-FanPWM` row `missing-product-yaml` → `blocked-hardware` + `product_yaml`) and [`docs/firmware-build-gap-report.md`](../firmware-build-gap-report.md); new [`tests/test_pwm_product_readiness.py`](../../tests/test_pwm_product_readiness.py); `allowed` set updated in [`tests/test_fan_pwm_package.py`](../../tests/test_fan_pwm_package.py) and [`tests/test_compile_targets.py`](../../tests/test_compile_targets.py); addenda in [`docs/product-readiness-matrix.md`](../product-readiness-matrix.md), [`docs/webflash-exposure-readiness-matrix.md`](../webflash-exposure-readiness-matrix.md), [`docs/release-artifact-readiness-matrix.md`](../release-artifact-readiness-matrix.md), [`docs/compile-only-firmware-validation.md`](../compile-only-firmware-validation.md), and [`UPCOMING_PR.md`](../../UPCOMING_PR.md). | **Status remains `partial — schematic evidence available; package reconciliation pending`** (a product-YAML-only slice does not promote the top-line hardware status, which is gated on bench / silkscreen / harness evidence). **What landed:** the canonical FanPWM product YAML + its non-WebFlash catalog row, closing the [§Blocker table](#pwm-blocker-removal-001-readiness--blocker-table) row-8 product-YAML-eligibility item for the PWM-drive-only scope. The **package** (PR #590) and the **full compile** (run `26414398902`, `validated-full-compile`) are **complete for the PWM-drive-only scope**. **RPM is NOT supported** — the package wires no `pulse_counter` and no per-fan / aggregate RPM (an SX1509 expander pin is compile-proven unable to back an ESPHome `pulse_counter` per `PWM-SX1509-TACH-PROOF-001`); `TachIO` / `GPIO16` stays reserved/pending; per-fan RPM stays deferred (`COMPONENT-SX1509-TACH-001` or a bench-confirmed `TachIO` follow-up). **The product remains blocked from WebFlash / release / import / compliance:** no WebFlash wrapper, no `config/webflash-builds.json` row, no `webflash_build_matrix` flip, no `artifact_name`, no release artifact; `WEBFLASH-PWM-001` / `RELEASE-PWM-001` / `WF-IMPORT-PWM-001` stay blocked. **Bench gates stay open:** PWM polarity; per-fan / aggregate current + thermal envelope; product bench not complete. `S360-311` `schematic_status` stays `cataloged_unverified` (SELV low-voltage board; no mains, so `COMPLIANCE-001` does not gate FanPWM). **This is not hardware-stable readiness and not product-release approval.** The FW-COMPILE-PWM-001 compile-only skeleton stays the separate CI validation target (unchanged, still `validated-full-compile`). **What this PR does NOT edit:** `products/webflash/**`, `config/webflash-builds.json`, `firmware/**`, `manifest.json`, `firmware/sources.json`, `.github/workflows/**`, `components/**`, `include/**`, `fan_pwm.yaml`, `fan_pwm_sx1509.yaml`, any Core abstract package, or any release artifact / checksum / build-info manifest. FanRelay and FanDAC are unchanged; Release-One (`Ceiling-POE-VentIQ-RoomIQ` / `v1.0.0` / `stable`), the LED preview, and FanTRIAC (`blocked` / `HW-005`) are unchanged. |

The next audit-log entry against HW-PINMAP-311-FOLLOWUP should
appear when committed evidence is added to this repository that
the field-evidence rules above can cite: operator-attributed
silkscreen captures of the Core-side `J6` 1-to-13 pin order
and the parallel module-side `J3` 1-to-13 pin order; physical
13-pin Core ↔ module harness inspection with
conductor-by-conductor pin mapping recorded; oscilloscope
captures of the per-fan PWM drive (`TachPMW*`), the per-fan tach
feedback (`Pul_Cou*`), or the shared open-drain `TachIO` net
against a populated `S360-100-R4` + `S360-311-R4` pair, with
operator / reviewer identity and review date recorded;
pulses-per-revolution validation against a real fan via the
SX1509 path and / or the direct-ESP32 `TachIO` path; resolution
of the UART-on-`J3`-pins-11/12 routing alternative (Core `J6`
capture extended to record `UART_RX` / `UART_TX` on pins 11 / 12,
or the separate harness documented); PWM polarity (active-high
vs active-low for the per-fan low-side N-FET gate path) and
tach pull-up identification; a standalone schematic-backed
reference doc (e.g. `docs/hardware/s360-311-r4-fanpwm.md` in the
per-board reference pattern of
[`s360-200-r4-roomiq.md`](s360-200-r4-roomiq.md) /
[`s360-211-r4-ventiq.md`](s360-211-r4-ventiq.md) /
[`s360-300-r4-led.md`](s360-300-r4-led.md)) tying together the
module-side `J3`, the Core-side `J6`, the SX1509 channel map,
the direct-ESP32 `TachIO` passthrough, and the FanPWM package
bindings; a single-vs-four-channel canonical-abstraction
decision against the FanPWM token; a `"NINE 4pin FANs"` title
documentation resolution; KiCad schematic source
(`S360-311-R4.kicad_sch`) / KiCad PCB source
(`S360-311-R4.kicad_pcb`) / KiCad project metadata
(`*.kicad_pro` / `*.kicad_prl` / `fp-lib-table` /
`sym-lib-table`) / BOM / CPL / Gerbers / drill files / STEP / or
board images for `S360-311-R4` that the audit can cite by repo
path; or progress on the systemic Core abstract-bus rebind
(`CORE-ABSTRACT-BUS-001`, alias for
[`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)).
Until any of those land, the next audit-log entry should report
the same `partial — schematic evidence available; package
reconciliation pending` outcome with the new inspection date.

## BLOCKER-BURNDOWN-001 consolidation note (2026-05-26)

This board's blockers are consolidated into the cross-lane
[`docs/blocker-burndown.md`](../blocker-burndown.md) §2A (FanPWM /
S360-311). No new PWM evidence appeared this session: a Drive re-search
found only the already-recorded `12vFan_PWM_PulseCounter` CAD / BOM /
gerber / CPL / STEP / render set and the `Sense360_R4_Tracker`
(2026-05-18, unchanged) — **no** bench artifact for PWM polarity,
per-fan / aggregate current, MT3608 ceiling, inrush, thermal, or product
bench. Those rows stay open exactly as recorded in the
[§Blocker table](#blocker-table). The package stays PWM-drive-only:
SX1509 PWM-drive output is supported and is the basis of FanPWM drive,
kept distinct from the per-fan RPM limitation — per-fan RPM via an SX1509
`pulse_counter` is compile-proven unsupported (`PWM-SX1509-TACH-PROOF-001`,
captured `esphome config` rejection `[sx1509] is an invalid option for
[pin]`), so `rpm_supported` stays `false`. Recommended next FanPWM PR:
**`S360-311-BENCH-EVIDENCE-REQUEST-001`** (the open bench items are
converted into operator questions in
[`blocker-burndown.md` §5](../blocker-burndown.md#5-minimum-operator-checklist)).
This note makes no config / product / package / WebFlash / release /
RPM / `schematic_status` change and fabricates no evidence.

## S360-311-BENCH-EVIDENCE-REQUEST-001 — FanPWM bench evidence checklist & contract (2026-05-26)

This section turns the still-open FanPWM bench blockers
(`PWM-3` / `PWM-6` / `PWM-10` / `PWM-11` / `PWM-13` in
[`blocker-burndown.md` §2A](../blocker-burndown.md#2a-fanpwm--s360-311))
into one **operator-answerable checklist** and an explicit **pass/fail
evidence contract**. It is **documentation only**: it requests evidence,
it does not record results, change behaviour, or relax any guardrail in
[§Do-not-change guardrails](#do-not-change-guardrails). The lane is
**bench / operator / WebFlash-access gated, not repo gated** — package,
product, and full-compile are complete (`PWM-4` / `PWM-7` / `PWM-8` /
`PWM-9` CLOSED).

### Drive re-search result (2026-05-26)

A fresh Drive search for new FanPWM bench evidence (`S360-311`,
`S360-311-R4`, `FanPWM`, `PWM bench`, `polarity`, `current`, `thermal`,
`TachIO`, `GPIO16`, `fan test`, `product bench`, photos / videos /
spreadsheets / logs) found **no bench artifact**. The only FanPWM-bearing
material is **design / CAD only**:

- the already-recorded `12vFan_PWM_PulseCounter` set (schematic PDF,
  `.xlsx` BOM, gerbers, CPL, STEP, KiCad sources, 3 board renders);
- a canonically-named `S360-311-R4` Drive folder (owner
  `kanyugistash@gmail.com`, folder created 2026-05-16) holding the same
  artifact classes under the board's catalog id — `S360-311-R4.kicad_sch`
  / `.kicad_pcb` / `.kicad_pro`, `S360-311-R4.step`,
  `S360-311-R4_GERBERS.zip`, `S360-311-R4_positions.csv` (CPL),
  `S360-311-R4.pdf` (schematic), `S360-311-R4_BOM.xlsx` (modified
  2026-02-08), and three renders `S360-311-R4.png` / `_2.png` / `_3.png`
  (modified 2026-05-14);
- the `Sense360_R4_Tracker` (2026-05-18, unchanged).

None of these is a PWM-polarity waveform, a per-fan / aggregate current
measurement, an MT3608 boost-ceiling note, an inrush / locked-rotor
capture, a thermal observation, or a product-bench sign-off. The
`S360-311-R4` folder is **board design / manufacturing provenance**, the
same artifact class as the committed
[`schematics/S360-311-R4.pdf`](schematics/S360-311-R4.pdf); it closes no
bench blocker and is recorded for provenance only — **no Drive file is
committed by this PR and no bench evidence is fabricated.**

### Operator checklist (fill in and attach evidence)

Every row is answerable by the hardware owner at the bench. Leave a row
`UNANSWERED` rather than guessing. Each row names the blocker it feeds.

| # | Item | Answer (operator fills in) | Feeds |
|---|---|---|---|
| 1 | **Board revision tested** (silkscreen P/N + rev; note if blank per tracker `G01`) | | PWM-3 |
| 2 | **Fan / load model tested** (make / model; 4-wire PWM fan; rated current) | | PWM-6 |
| 3 | **Supply voltage** (input rail into the board, e.g. 5 V PoE-derived) | | PWM-6 |
| 4 | **PSU / current limit** (bench PSU model; set current limit during test) | | PWM-6 |
| 5 | **Number of channels tested** (1–4 of `J1` / `J2` / `J4` / `J5`) | | PWM-6 |
| 6 | **PWM frequency used** (Hz at the SX1509 PWM-drive output) | | PWM-10 |
| 7 | **PWM duty range tested** (min%–max% commanded) | | PWM-10 |
| 8a | **PWM polarity — does increasing duty increase fan speed?** (yes / no) | | PWM-10 |
| 8b | **PWM polarity — is inversion required?** (yes / no; where: package vs gate path) | | PWM-10 |
| 9a | **Boot / default output — before ESPHome boots** (fan state: off / full / spinning?) | | PWM-10 / PWM-11 |
| 9b | **Boot / default output — after ESPHome boots** (commanded default duty / fan state) | | PWM-10 / PWM-11 |
| 9c | **Boot / default output — after restart** (glitch / transient on reboot?) | | PWM-10 / PWM-11 |
| 10 | **Per-channel current measured** (A per channel at full speed; method) | | PWM-6 |
| 11 | **Aggregate current with all four channels active** (total A at full speed) | | PWM-6 |
| 12 | **Inrush / startup behaviour** (peak A at spin-up; locked-rotor draw if observed) | | PWM-6 |
| 13a | **Thermal — duration** (how long the worst-case load ran) | | PWM-6 / PWM-13 |
| 13b | **Thermal — ambient temp** (°C) | | PWM-6 / PWM-13 |
| 13c | **Thermal — hottest component / location** (e.g. MT3608, `L1`, an `ME15N10-G` FET) | | PWM-6 / PWM-13 |
| 13d | **Thermal — measured temp or qualitative observation** (°C if measured, else note) | | PWM-6 / PWM-13 |
| 14 | **MT3608 / boost-converter observation** (output-current ceiling; sag / dropout under 4-fan load) | | PWM-6 |
| 15 | **Connector / pin-1 / silkscreen confirmation** (Core `J6` & module `J3` 1-to-13 order; fan-output `J1`/`J2`/`J4`/`J5` pin-1; UART on `J3` pins 11/12?) | | PWM-3 |
| 16 | **TachIO / GPIO16 observation (if any)** — *no RPM claim unless separately proven* (idle level, transitions; **not** an RPM figure) | | PWM-12 (deferred) |
| 17 | **Photos / videos / logs attached** (scope captures, multimeter shots, thermal photos, ESPHome log) | | all |
| 18 | **Operator / date / source / provenance** (who, when, board serial, Drive path of uploaded evidence) | | all |

### Pass/fail evidence contract

What evidence **closes** each open bench blocker, and what does **not**.

| Blocker | Closes when (PASS) | Does NOT close / FAIL |
|---|---|---|
| **PWM polarity (`PWM-10`)** | Scope or observed behaviour showing increasing commanded duty increases fan speed at the SX1509 PWM-drive output → fan PWM input, with the inversion-required answer recorded (items 6–8b). | A duty sweep with no recorded speed direction, or an un-scoped "it works" claim. |
| **Per-fan current (`PWM-6`)** | Measured per-channel current at full speed for the fan/load and supply under test, with method (items 2–5, 10). | Datasheet-only fan rating with no board-side measurement. |
| **Aggregate current (`PWM-6`)** | Measured total current with all four channels active at full speed, plus the inrush / locked-rotor peak (items 11–12, 14). | Single-channel figure multiplied by four; no inrush capture. |
| **Thermal (`PWM-6` / `PWM-13`)** | A worst-case-load thermal observation with duration, ambient, hottest location, and a measured or qualitative reading (items 13a–13d). Bench characterisation only — **not** a compliance / EMC approval. | No duration/ambient context, or a claim of certification. |
| **Product bench (`PWM-11`)** | Operator end-to-end FanPWM product-bench sign-off on the `S360-311-R4` board (boot/default behaviour items 9a–9c plus polarity/current/thermal above), with operator/date/serial and attached evidence (items 17–18). | A compile-pass or package test treated as a product-bench pass. |

### Out of scope (stays blocked regardless of this checklist)

These are **not** unlocked by any answer above and must not be claimed:

- **RPM support** — no `pulse_counter`; per-fan RPM via an SX1509 pin is
  compile-proven unsupported (`PWM-SX1509-TACH-PROOF-001`). `TachIO` /
  `GPIO16` stays reserved/pending; any RPM needs a separate approved tach
  strategy (`COMPONENT-SX1509-TACH-001`, future). A TachIO observation
  (item 16) is diagnostic only and is **not** an RPM claim.
- **WebFlash exposure** — no wrapper, no `config/webflash-builds.json`
  row, no `webflash_build_matrix` flip, no `artifact_name`; gated and
  additionally `NEEDS WEBFLASH ACCESS` (`PWM-15`).
- **Release artifact** — no `.bin` / tag / checksum / build-info
  (`RELEASE-PWM-001` stays blocked).
- **Import readiness** — `WF-IMPORT-PWM-001` stays blocked behind
  `RELEASE-PWM-001` (WebFlash-owned).
- **Hardware-stable promotion** — `S360-311` `schematic_status` stays
  `cataloged_unverified`; promotion is a separate JSON-only PR gated on
  the silkscreen / pin-map items, not on this checklist, unless policy
  explicitly allows.
- **Compliance approval** — board is SELV (5 V → 12 V boost, no mains), so
  `COMPLIANCE-001` does not apply; the thermal item is characterisation,
  not certification.

### Next-PR recommendation

- **If the operator uploads / answers the checklist and attaches bench
  evidence** → **`S360-311-BENCH-RESULT-001`** records the results and
  closes the bench rows it actually proves.
- **While evidence is still missing** (the case today) →
  **`S360-311-BENCH-RESULT-001` remains gated** until the operator
  uploads or answers the checklist above; no FanPWM bench row can close
  in a docs-only pass.
- **WebFlash wrapper / build PRs stay blocked**
  (`WEBFLASH-PWM-001` / `RELEASE-PWM-001` / `WF-IMPORT-PWM-001`); the
  live-access re-check `WEBFLASH-PWM-LIVE-CHECK-001` stays queued behind
  `sense360store/WebFlash` access restoration.

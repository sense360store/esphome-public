# S360-310-R4 Relay Pin / Package Mapping Audit (HW-PINMAP-310)

## Status

**Status: partial — schematic evidence available; package reconciliation pending.**

This document is the **HW-PINMAP-310** audit record for the
`Sense360 Relay` board, revision **R4** (`S360-310-R4`). It exists to
close the documentation gap recorded at
[`docs/hardware/hardware-artifact-policy.md` Follow-up PR sequence #3](hardware-artifact-policy.md#follow-up-pr-sequence)
and at
[`docs/hardware/board-readiness-matrix.md` Follow-up PR sequence #1](board-readiness-matrix.md#follow-up-pr-sequence),
which together call for a per-board pin/package mapping audit
**before** any `FanRelay`-bearing package, product, build, or
WebFlash work can proceed.

When HW-PINMAP-310 originally landed, the audit was
`pending — schematic/design evidence required`: no module-side
`S360-310-R4` schematic existed in this repository, and no curated
artifact index built from it had been committed. **HW-ASSETS-310 has
since landed** — the module-side schematic PDF is committed at
[`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf)
and the curated artifact index lives at
[`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md).
**HW-PINMAP-310-FOLLOWUP** (this revision) consumes that evidence to
record a schematic-backed reconciliation view. It does **not**
resolve the firmware-side disagreements it surfaces — those are
owed to separate, evidence-bearing follow-up PRs (silkscreen /
harness / BOM / bench evidence; `PACKAGE-RELAY-001`;
`CORE-ABSTRACT-BUS-001`). The bench / silkscreen / harness / BOM /
load-contact evidence-capture **checklist** is recorded under
`S360-310-BENCH-001` at
§[S360-310-BENCH-001 — Relay bench evidence](#s360-310-bench-001--relay-bench-evidence);
every one of the ten enumerated hardware-evidence rows currently
sits at `pending — bench evidence required`.

The schematic-backed evidence on file proves the **logical**
module-side `J2` ↔ Core-side `J4` net match (`+5V` / `Relay` /
`GND`) and the module-side relay drive topology (`Q1` MMBT3904
NPN low-side, `R1` 1 kΩ base, `R2` 10 kΩ pull-down, `D1` flyback,
`K1` mechanical relay, `J1` 3-pin "Inline Fan" load-side). It does
**not** prove silkscreen pin-1 orientation on either side, does
**not** prove `J1` NO / COM / NC mapping, does **not** prove the
`K1` part number or coil-voltage / contact-current rating, does
**not** establish the Core-to-module harness identity, and does
**not** resolve the systemic `IO3` (Core schematic) vs `GPIO4`
(ceiling Core abstract `relay_pin`) vs `GPIO10` (generic Core
abstract `relay_pin`) disagreement — that resolution remains owned
by `CORE-ABSTRACT-BUS-001` per
[`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups),
not by HW-PINMAP-310 or its FOLLOWUP.

## Purpose and scope

This document is **documentation only**. It does **not**:

- change any value in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json),
  [`config/product-catalog.json`](../../config/product-catalog.json),
  [`config/webflash-builds.json`](../../config/webflash-builds.json),
  or [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json),
- change any product YAML under [`products/`](../../products/),
  any WebFlash wrapper under
  [`products/webflash/`](../../products/webflash/), or any package
  YAML under [`packages/`](../../packages/),
- change any workflow under `.github/workflows/`, any script under
  [`scripts/`](../../scripts/), any test under
  [`tests/`](../../tests/), any component under `components/`, or any
  header under `include/`,
- mark `S360-310` `verified` — `schematic_status` stays
  `cataloged_unverified`,
- add or change `schematic_file` for `S360-310` in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json),
- edit
  [`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf)
  or
  [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md)
  (their content is **consumed** by this audit, not rewritten by it),
- mark the pin map confirmed, or mark
  [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
  as `confirmed-ok` — its status stays
  `package-yaml-pending` / `needs-package-reconciliation`,
- resolve the `IO3` (Core schematic `Relay` net) vs `GPIO4` (Core
  ceiling abstract package `relay_pin`) vs `GPIO10` (Core abstract
  package `relay_pin`) disagreement; that resolution belongs to the
  systemic Core abstract-bus rebind owned by
  [`docs/release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)
  and to a future evidence-bearing `CORE-ABSTRACT-BUS-001` PR,
- promote `S360-310` to `preview` / `stable` / `production`,
- add a `FanRelay` token to any current or future Release-One config
  string,
- add a `FanRelay`-bearing entry to
  [`config/webflash-builds.json`](../../config/webflash-builds.json)
  or to any product / WebFlash wrapper,
- regenerate firmware, create a release, or change any WebFlash
  manifest / import,
- unblock FanTRIAC (`Ceiling-POE-VentIQ-FanTRIAC-RoomIQ` stays
  `status: blocked`, `blocker: HW-005`,
  `webflash_build_matrix: false`),
- change the mains-voltage compliance status of `S360-320` or
  `S360-400` (owned by COMPLIANCE-001;
  [`docs/compliance/mains-voltage-uk-eu-assessment.md`](../compliance/mains-voltage-uk-eu-assessment.md)),
- change the Release-One configuration
  `Ceiling-POE-VentIQ-RoomIQ` (`status: production`,
  `channel: stable`, artifact
  `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`, tag
  `v1.0.0`),
- change the LED preview entry `Ceiling-POE-VentIQ-RoomIQ-LED`
  (stays `status: preview`, `channel: preview`).

If this audit and any source-of-truth document drift, **the
source-of-truth document wins** and this audit must be updated. The
authoritative module-side capture lives in
[`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md);
the authoritative Core-side capture lives in
[`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md).

## Board identity

Mirrored from
[`config/hardware-catalog.json`](../../config/hardware-catalog.json)
lines 62–70 without modification.

| Field | Value |
|---|---|
| `group` | `Inline` |
| `type` | `Driver` |
| `friendly_name` | `Sense360 Relay` |
| `sku` | `S360-310` |
| `rev` | `R4` |
| `old_name` | `S360-Relay-C` |
| `description` | `On/off relay for bathroom fans.` |
| `schematic_status` | `cataloged_unverified` (unchanged by this PR) |
| `schematic_file` | _(not set; unchanged by this PR)_ |
| Curated artifact index | [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md) (HW-ASSETS-310; landed) |
| Committed schematic PDF | [`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf) (HW-ASSETS-310; landed) |

## Repository evidence

### What IS in the repo today

- **Module-side schematic PDF.** Committed at
  [`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf)
  under HW-ASSETS-310 (38,967 bytes; SHA256
  `441f0ccd681b5425fa9d276e6d7fb1bf289597674342dc8d9b8e656a23896c7d`;
  byte-identical to the upload). Upload inventory recorded in
  [`docs/hardware/artifacts/S360-310-R4.md` Checksums / file sizes](artifacts/S360-310-R4.md#checksums--file-sizes).
- **Curated artifact index.**
  [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md)
  records the schematic content (section title "INLINE FAN CONTROL
  USING RELAY"; KiCad 10.0.3 single-sheet A4 export; `J2` 3-pin
  "From Core" connector matching Core-side `J4` net order; `K1`
  mechanical relay; `Q1` MMBT3904 NPN low-side coil driver with
  `R1` 1 kΩ base series, `R2` 10 kΩ base pull-down, `D1` flyback
  diode; `J1` 3-pin "Inline Fan" load-side output; coil rail `+5V`
  with no on-board boost; no opto, no indicator LED, no snubber, no
  Nextion connector, no mounting-hole references, no `K1` part
  number / coil-voltage / contact-rating label visible) and the
  artifact-side opens
  ([§Open questions / verification needed](artifacts/S360-310-R4.md#open-questions--verification-needed)).
- **Core-side `J4` 3-pin connector capture.**
  [`docs/hardware/s360-100-r4-core.md` §J4](s360-100-r4-core.md#j4--relay-module-connector-3-pin)
  records the Core-side mate as `J4` (Relay module connector,
  3-pin) with the net list `+5V`, `Relay`, `GND`. Pin order is
  schematic-only on the Core side (silkscreen reading of `J4`
  pin-1 is implicitly part of the broader Core silkscreen evidence
  gate in
  [`s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status)).
- **Core-side `Relay` source-pin capture.**
  [`s360-100-r4-core.md` §Pin assignments](s360-100-r4-core.md#pin-assignments)
  line 100 records `IO3 → Relay (J4 Relay module gate)`. The
  parallel
  [`s360-100-r4-core.md` §Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs)
  line 276 records `Relay ↔ J4 Relay module (3-pin)` originating at
  ESP32 `IO3`.
- **Module-side firmware package.**
  [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
  exists and binds `fan_relay_pin: ${relay_pin}` (file line 27;
  see [§Existing package abstraction](#existing-package-abstraction)).
- **WebFlash compatibility token reservation.**
  [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json)
  line 11 reserves `FanRelay` as a `canonical_modules` token,
  subject to the fan-driver `max-one-of` rule enforced by
  `FAN_DRIVER_TOKENS` in
  [`tests/validate_webflash_builds.py`](../../tests/validate_webflash_builds.py).
- **Existing classifications.**
  [`docs/hardware/board-readiness-matrix.md` `S360-310` row](board-readiness-matrix.md#s360-310-sense360-relay)
  — `partially-documented`, `not-needed-for-release-one`;
  `package-yaml-pending`.
  [`docs/hardware/remaining-board-documentation-audit.md` Sense360 Relay (`S360-310`)](remaining-board-documentation-audit.md#sense360-relay-s360-310)
  — same classification.
  [`docs/hardware/firmware-package-mapping-audit.md` Release-One package stack](firmware-package-mapping-audit.md#release-one-product-yaml-package-stack)
  — flags `relay_pin: GPIO4` as wrong (`IO4 = SEN0609_RX`; the
  `Relay` net is on `IO3`).
  [`docs/product-availability-taxonomy.md` Current board / product snapshot](../product-availability-taxonomy.md#current-board--product-snapshot)
  line 586 — `partially-documented` + `design-pending`.

### What is NOT in the repo today

- **No standalone schematic-backed reference doc** in the
  `docs/hardware/sXXX-r4-<role>.md` per-board reference pattern of
  [`s360-100-r4-core.md`](s360-100-r4-core.md),
  [`s360-200-r4-roomiq.md`](s360-200-r4-roomiq.md),
  [`s360-210-r4-airiq.md`](s360-210-r4-airiq.md),
  [`s360-211-r4-ventiq.md`](s360-211-r4-ventiq.md), or
  [`s360-300-r4-led.md`](s360-300-r4-led.md). This audit is the
  **schematic-backed reconciliation record**, not the per-board
  reference doc itself; the per-board reference doc is owed to a
  later PR after silkscreen / harness / BOM evidence arrives.
- **No KiCad source / KiCad PCB / project metadata / BOM / CPL /
  gerbers / drill files / STEP / board images / silkscreen photos /
  bench-continuity-harness photos** for `S360-310-R4`; see
  [`docs/hardware/artifacts/S360-310-R4.md` Files NOT provided in this upload](artifacts/S360-310-R4.md#files-not-provided-in-this-upload).
- **No silkscreen verification.** No physical pin-1 reading of
  either the module-side `J2` 3-pin "From Core" connector, the
  module-side `J1` 3-pin "Inline Fan" load-side connector, or the
  Core-side `J4` 3-pin relay-module connector.
- **No harness verification.** No physical Core-to-module 3-pin
  cable has been inspected; the conductor-by-conductor mapping
  from Core `J4` pin N to module `J2` pin N is schematic-only.
- **No `K1` BOM identity.** Relay part number, coil voltage,
  contact configuration (SPST / SPDT), contact current rating, and
  isolation rating are not on the schematic and must come from the
  BOM (or from board / silkscreen photos labelling the part).
- **No bench / continuity / waveform evidence.** No populated
  `S360-310-R4` board on a `S360-100-R4` Core has been exercised
  with operator / reviewer / serial recorded; no measurement of
  the relay coil-drive waveform, the `K1` switching behaviour, or
  the load-side continuity.
- **No active product YAML** consumes `S360-310`. The legacy
  [`products/sense360-fan-pwm.yaml`](../../products/sense360-fan-pwm.yaml)
  is `legacy-compatible` and targets `S360-311` Sense360 PWM, not
  `S360-310` Sense360 Relay.
- **No WebFlash wrapper** under
  [`products/webflash/`](../../products/webflash/) targets
  FanRelay.
- **No entry in
  [`config/webflash-builds.json`](../../config/webflash-builds.json)**
  consumes the `FanRelay` token. The two committed builds are
  `Ceiling-POE-VentIQ-RoomIQ` (`stable`) and
  `Ceiling-POE-VentIQ-RoomIQ-LED` (`preview`).
- **No release artifact** for any `FanRelay`-bearing config.
- **No WebFlash import** for any `FanRelay`-bearing config.

## Schematic summary

This section restates only what is directly visible in the
committed schematic PDF
[`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf)
and inventoried in
[`docs/hardware/artifacts/S360-310-R4.md` What the schematic appears to contain](artifacts/S360-310-R4.md#what-the-schematic-appears-to-contain).
It does **not** invent electrical characteristics, relay part
identity, coil voltage, contact ratings, isolation ratings,
mains-side topology, or NO / COM / NC mapping beyond what is
labelled on the sheet.

- **Section title: "INLINE FAN CONTROL USING RELAY".** One
  section, no sub-sheets. Single-sheet A4 KiCad 10.0.3 export;
  title-block File field reads `S360-310-R4.kicad_sch`; Title /
  Date / Rev fields blank.
- **"From Core" connector `J2`** — 3-pin. Net list as labelled in
  the PDF:

  | Module-side `J2` pin | Net |
  |---:|---|
  | 1 | `+5V` |
  | 2 | `Relay` |
  | 3 | `GND` |

  The net order (`+5V` → `Relay` → `GND`) matches the Core-side
  `J4` capture in
  [`s360-100-r4-core.md` §J4](s360-100-r4-core.md#j4--relay-module-connector-3-pin)
  pin-for-pin by net.
- **Relay coil-drive topology.** Low-side NPN BJT switch from a
  module-supplied `+5V` rail (no on-board boost):
  - `Q1 MMBT3904` — NPN BJT in SOT-23. Emitter to `GND`,
    collector to the relay coil node.
  - `R1 1 kΩ` — base series resistor on the `Relay` signal path
    from `J2` pin 2 to the `Q1` base node.
  - `R2 10 kΩ` — base-to-`GND` pull-down at the `Q1` base node
    (keeps `Q1` off when the `Relay` net is floating or Hi-Z).
  - `D1` — flyback diode across the relay coil (cathode to `+5V`,
    anode to the `Q1` collector / coil node). No part number
    visible.
- **Relay `K1`.** Mechanical relay. Coil between `+5V` and `Q1`
  collector; flyback `D1` across the coil as above. **No relay
  part number, no coil-voltage label, and no contact-current
  rating visible on the schematic sheet.** The relay reference is
  `K1` only.
- **"Inline Fan" load-side connector `J1`** — 3-pin. Wired to the
  switching-contact side of `K1` (visible contact-side pin labels
  `1` / `2` / `3`). A 3-pin output is consistent with SPDT contact
  topology (`NO` / `COM` / `NC`), but the specific NO / COM / NC
  assignment to `J1` pins 1 / 2 / 3 is **not** labelled on the
  schematic.
- **What the schematic does NOT visibly contain.**
  - No opto-isolator between the `Relay` input and the relay coil
    (the `Q1` MMBT3904 is a direct low-side switch).
  - No on-board indicator LED.
  - No mains-side snubber across the `K1` switching contacts.
  - No "NEXTION DISPLAY" connector (unlike `S360-311-R4` /
    `S360-312-R4`).
  - No mounting-hole references (`H1`..`H4`).
  - No I²C / UART / DAC / boost-converter circuitry.

Everything beyond the list above — module dimensions, exact
component values not printed on the visible sheet, on-board PCB
silkscreen, layer stackup, fab notes, harness identity, or any
electrical timing / contact-rating / isolation-rating claim — is
**unknown** and would have to come from the
retained-but-not-committed KiCad source / KiCad PCB / BOM / CPL /
gerbers / drill / STEP / board images / silkscreen photos / bench
photos per
[`hardware-artifact-policy.md`](hardware-artifact-policy.md).

## Connector / pin-map findings

### Module-side `J2` ↔ Core-side `J4` (3-pin harness)

The two connectors mate via a 3-pin Core ↔ module harness. The
module-side capture (above) is **schematic-confirmed** against the
committed S360-310-R4 PDF. The Core-side capture in
[`s360-100-r4-core.md` §J4](s360-100-r4-core.md#j4--relay-module-connector-3-pin)
records the same three nets (`+5V`, `Relay`, `GND`) in the same
1-to-3 order on the Core side; the silkscreen reading of `J4`
pin-1 is part of the broader Core silkscreen evidence gate in
[`s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status).

| Element | Core side (`J4`) | Module side (`J2`) | Reconciliation status |
|---|---|---|---|
| Connector identity | `J4` — Relay module connector (3-pin) — [§J4](s360-100-r4-core.md#j4--relay-module-connector-3-pin) | `J2` — "From Core" (3-pin) — schematic-confirmed | Naming mismatch is expected (each board labels its own connectors); 3-pin pin count matches. |
| Net list | `+5V`, `Relay`, `GND` | `+5V`, `Relay`, `GND` | **Net-by-net match** between Core-side `J4` and module-side `J2`. Logical alignment is **schematic-backed** on both sides. |
| 1-to-3 pin order | Pin 1 `+5V`, pin 2 `Relay`, pin 3 `GND` per Core capture — **schematic-only** on the Core side | Pin 1 `+5V`, pin 2 `Relay`, pin 3 `GND` per module capture — **schematic-only** on the module side | Schematic-only on both sides; **requires silkscreen verification** to confirm the harness wires `+5V` (Core `J4` pin 1) to `+5V` (Module `J2` pin 1), and so on through to `GND` (Module `J2` pin 3). Resolution belongs to a future bench / silkscreen / harness slice. |
| Physical pin-1 orientation / harness keying | Not derivable from PDF | Not derivable from PDF | **Pending — silkscreen / harness evidence required.** |

### Module-side `J1` (3-pin "Inline Fan" load-side)

`J1` is the load-side output that switches a fan load via the
`K1` mechanical relay contacts. The schematic shows three labelled
contact-side pins on `K1` (`1` / `2` / `3`) wired to `J1`, but
does **not** assign NO / COM / NC.

| Element | Source | Value | Status |
|---|---|---|---|
| `J1` connector type | `S360-310-R4.pdf` | 3-pin contact-side connector | Schematic-confirmed. |
| `J1` pin labels | `S360-310-R4.pdf` | Pins labelled `1` / `2` / `3` | Schematic-confirmed. |
| `J1` NO / COM / NC mapping | _(not labelled on the schematic)_ | Unknown | **Pending — silkscreen evidence required.** Cannot publish switched-load wiring guidance without this. |
| `K1` contact configuration | Implied by 3-pin output | SPDT-consistent (could be SPST-NO + COM + NC, or SPDT with NO / COM / NC) | **Pending — `K1` BOM identity / silkscreen / continuity testing required.** |

## Relay drive findings

The module-side `Relay` net (at `J2` pin 2) feeds the base of `Q1`
through `R1` (1 kΩ series); `R2` (10 kΩ) pulls the base to `GND`;
`Q1` (MMBT3904 NPN) sinks the relay coil; `D1` is the flyback
diode across the coil; the coil's high side is the module-supplied
`+5V` rail at `J2` pin 1.

| Element | Source | Value | Status |
|---|---|---|---|
| Drive transistor | `S360-310-R4.pdf` | `Q1` MMBT3904 NPN BJT (SOT-23) | Schematic-confirmed. |
| Base series resistor | `S360-310-R4.pdf` | `R1` 1 kΩ | Schematic-confirmed. |
| Base pull-down | `S360-310-R4.pdf` | `R2` 10 kΩ to `GND` | Schematic-confirmed. |
| Flyback diode | `S360-310-R4.pdf` | `D1` across the relay coil (cathode to `+5V`, anode to `Q1` collector) | Schematic-confirmed; **part number not visible**. |
| Coil rail | `S360-310-R4.pdf` | Module-supplied `+5V` at `J2` pin 1; no on-board boost | Schematic-confirmed. |
| Drive-level compatibility with 3.3 V ESP32 GPIO | Topology with 1 kΩ base series + 10 kΩ pull-down | **Consistent with** direct 3.3 V CMOS drive into MMBT3904 base; no level shifter is required between the ESP32 and the module input | Schematic-backed at the topology layer **only**. Does **not** establish which Core-side GPIO physically carries the `Relay` net — see [§Reconciliation findings](#reconciliation-findings) and [§Existing package abstraction](#existing-package-abstraction). |
| Coil current vs Q1 SOA | `S360-310-R4.pdf` shows no coil-current label | Unknown | **Pending — `K1` BOM identity required** (MMBT3904 max Ic ≈ 200 mA; whether the chosen `K1` coil falls inside SOA depends on `K1`'s coil current draw). |

## Load / contact findings

The load side is whatever connects to `J1` through the `K1`
contacts. The schematic does not label NO / COM / NC, does not
label `K1`'s contact rating, and does not show a mains-side
snubber.

| Element | Source | Value | Status |
|---|---|---|---|
| Load-side connector | `S360-310-R4.pdf` | `J1` 3-pin "Inline Fan" output | Schematic-confirmed. |
| Contact configuration | Implied | SPDT-consistent | **Pending — `K1` BOM identity + silkscreen / continuity testing required.** |
| Contact rating (current / voltage / AC / DC) | _(not labelled)_ | Unknown | **Pending — `K1` BOM identity / vendor datasheet required.** Hardware-catalog description (`On/off relay for bathroom fans.`) is consistent with a small mechanical relay but does not establish a contact rating. |
| Mains-side snubber | _(not present)_ | None on this sheet | Schematic-confirmed (absent). Whether one is needed depends on the actual switched load and the relay's contact behaviour. Not a HW-PINMAP-310 question. |
| Mains-voltage compliance | Out of scope for HW-PINMAP-310 / HW-PINMAP-310-FOLLOWUP | — | If and when a `FanRelay`-bearing product surfaces that switches mains loads, a separate COMPLIANCE-001-equivalent slice would own the compliance evidence. |

## Existing package abstraction

This section audits how
[`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
binds its relay-drive signal and what that binding resolves to
under the current Core abstract-bus packages.

### `packages/expansions/fan_relay.yaml`

Found at
[`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml).
Single-channel on/off relay package.

| Field | Value as found |
|---|---|
| Logical role | `FanRelay` (on/off AC fan control via on-board relay or external SSR) |
| Schematic-backed | **Partially.** The module-side schematic is committed under HW-ASSETS-310; the package is **not** reconciled against it. The disagreement is on the Core-side abstract-bus binding, not on the module-side package wiring. |
| GPIO assumption | `fan_relay_pin: ${relay_pin}` — file line 27. Inherits whichever value the parent Core abstract package binds. |
| Override hook | `fan_relay_pin: ${expansion_gpio1}` documented in a comment as the way to drive an external SSR from an expansion pin instead — file lines 22–25. |
| Default behaviour | Reuses the built-in relay declared by the core board. |
| Reconciliation against module-side schematic | The module-side schematic confirms the `Relay` input at `J2` pin 2 drives a `Q1` MMBT3904 base via `R1` 1 kΩ; it does **not** establish which Core-side ESP32 GPIO physically carries that net. |
| Current status | `package-yaml-pending` / `needs-package-reconciliation`. **Not** `confirmed-ok`. |

This file is **not edited** by this PR.

### Parent Core packages that resolve `${relay_pin}` differently

The `fan_relay.yaml` package inherits `${relay_pin}` from whichever
Core package is included by the parent product YAML. **The two Core
abstract packages historically disagreed with each other and with
the Core schematic.** That substitution-layer disagreement is
**resolved** by `CORE-ABSTRACT-BUS-001A` (landed 2026-05-21); the
table below records the pre-001A and post-001A state.

| Source | File | Line | Pre-001A `relay_pin` value | Post-001A `relay_pin` value | Status against the Core schematic |
|---|---|---|---|---|---|
| Ceiling Core abstract | [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) | 78 | `GPIO4` | `GPIO3` | **Matches the schematic post-001A** (`IO3 = Relay`). The pre-001A `GPIO4` conflicted with `IO4 = SEN0609_RX` per [`s360-100-r4-core.md` Pin assignments](s360-100-r4-core.md#pin-assignments) line 88. The HW-009 enumeration in [`firmware-package-mapping-audit.md` Release-One package stack](firmware-package-mapping-audit.md#release-one-product-yaml-package-stack) (the `relay_pin: GPIO4 — IO4 is SEN0609_RX; the Relay net is on IO3` bullet) records the pre-001A state. |
| Generic Core abstract | [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) | 49 | `GPIO10` | `GPIO3` | **Matches the schematic post-001A** (`IO3 = Relay`). The pre-001A `GPIO10` value did not match. |
| Mapping Core abstract | [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml) | 51 | `GPIO10` | `GPIO3` | **Matches the schematic post-001A.** |
| PoE Core abstract | [`packages/hardware/sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml) | 64 | `GPIO10` | `GPIO3` | **Matches the schematic post-001A.** |
| Wall Core abstract | [`packages/hardware/sense360_core_wall.yaml`](../../packages/hardware/sense360_core_wall.yaml) | 71 | `GPIO4` | `GPIO3` | **Matches the schematic post-001A.** |
| Core schematic / HW-009 | [`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md) | 100, 145–151, 276 | `IO3` | `IO3` | The Core-side schematic captures `Relay` ↔ `J4` pin 2, drive from ESP32 `IO3`. This is the only schematic-backed evidence in the repo for the relay drive signal on the Core side. |
| Module schematic / HW-ASSETS-310 | [`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf) | (single sheet) | `Relay` net at module `J2` pin 2 drives `Q1` base via `R1` | unchanged | The module side confirms the input topology but **does not** establish which Core-side ESP32 GPIO physically carries that net. |

Voice-variant Core packages
([`packages/hardware/sense360_core_voice_ceiling.yaml`](../../packages/hardware/sense360_core_voice_ceiling.yaml)
and
[`packages/hardware/sense360_core_voice_wall.yaml`](../../packages/hardware/sense360_core_voice_wall.yaml))
remain at `relay_pin: GPIO4` post-001A — they are deliberately out
of scope for the 001A slice and a later, separately-evidenced
slice owns their rebind.

**HW-PINMAP-310-FOLLOWUP did not resolve the substitution-layer
disagreement.** The Core abstract-bus substitution layer is now
fixed by `CORE-ABSTRACT-BUS-001A` (landed 2026-05-21; see
[§HW-PINMAP-310-FOLLOWUP audit log](#hw-pinmap-310-followup-audit-log)
2026-05-21 row). Resolution of the broader S360-310 evidence
gaps still requires:

1. **`PACKAGE-RELAY-001`** (alias: `PACKAGE-GAP-001` FanRelay slice)
   that reconciles
   [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
   against the rebound Core abstract substitutions and against the
   silkscreen / harness / BOM evidence that this audit still owes.
   The substitution-layer prerequisite (`CORE-ABSTRACT-BUS-001A`,
   alias for
   [`docs/release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)
   relay-pin slice) landed 2026-05-21; the schematic-correct
   `relay_pin: GPIO3` value is now bound in
   [`sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml),
   [`sense360_core.yaml`](../../packages/hardware/sense360_core.yaml),
   [`sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml),
   [`sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml),
   and
   [`sense360_core_wall.yaml`](../../packages/hardware/sense360_core_wall.yaml).
2. **Module-side silkscreen / harness / `K1` BOM evidence** as
   enumerated in [§Required evidence before promotion](#required-evidence-before-promotion).
   `PACKAGE-RELAY-001` cannot proceed without it even though the
   substitution-layer disagreement is now resolved.

The `relay_pin` substitution disagreement is **resolved at the
Core abstract-bus substitution layer** by 001A. The broader
S360-310 silkscreen / harness / BOM / load / contact / `K1` rating
evidence remains owed before `PACKAGE-RELAY-001` /
`PRODUCT-RELAY-001` / `WEBFLASH-RELAY-001` / `RELEASE-RELAY-001` /
`WF-IMPORT-RELAY-001` can proceed.

### Other package references

- [`packages/expansions/fan_triac.yaml`](../../packages/expansions/fan_triac.yaml)
  mentions `FanRelay` only in comments stating that FanTRIAC is
  **not** interchangeable with FanRelay / FanPWM / FanDAC (file
  lines 7, 33). This is a compatibility comment, not a relay-pin
  binding.
- [`packages/README.md`](../../packages/README.md) line 183 lists
  `fan_relay.yaml - FanRelay (S360-310, on/off relay)` as catalog
  text. No pin binding.

Neither file is edited by this PR.

## Reconciliation findings

This is the heart of HW-PINMAP-310-FOLLOWUP: a per-element
classification of where the module schematic, the Core-side
capture, the Core abstract packages, and the FanRelay package are
consistent versus where they require future reconciliation.

Every "requires future reconciliation" entry below names the
future PR that owns the resolution. **No entry says "should be
changed now."**

| Element | Source(s) | Status | Owner if reconciliation is needed |
|---|---|---|---|
| Module-side `J2` 3-pin pinout (schematic capture) | `S360-310-R4.pdf` + [`artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md) | **Schematic-confirmed** on the module side: pin 1 `+5V`, pin 2 `Relay`, pin 3 `GND`. Pin order is schematic-only until silkscreen verification. | A later bench / silkscreen slice (paired with S360-100-BENCH-001 for the Core-side `J4`). |
| Core-side `J4` 3-pin pinout (net list) | [`s360-100-r4-core.md` §J4](s360-100-r4-core.md#j4--relay-module-connector-3-pin) | **Schematic-confirmed** on the Core side: pin 1 `+5V`, pin 2 `Relay`, pin 3 `GND`. Pin order is schematic-only until silkscreen verification (paired with the broader Core silkscreen gate in [`s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status)). | Same. |
| Module `J2` ↔ Core `J4` net-by-net match | Both captures above | **Schematic-backed logical match**: `+5V` ↔ `+5V`, `Relay` ↔ `Relay`, `GND` ↔ `GND`. | No firmware-side reconciliation owed at the logical layer. Physical harness mapping still requires bench / silkscreen evidence. |
| Module `J2` ↔ Core `J4` physical pin-1 orientation | _(not derivable from PDF)_ | **Pending — silkscreen / harness evidence required.** | Bench / silkscreen slice. |
| `Relay` net source pin (Core side) | [`s360-100-r4-core.md` §Pin assignments](s360-100-r4-core.md#pin-assignments) line 100 | `IO3`. **Schematic-confirmed** on the Core side. | No HW-PINMAP-310 reconciliation owed at this layer. |
| Module-side relay coil-drive topology | `S360-310-R4.pdf` | `Q1` MMBT3904 NPN low-side; `R1` 1 kΩ base series; `R2` 10 kΩ pull-down; `D1` flyback diode; coil rail `+5V`; no opto; no indicator LED. **Schematic-confirmed** on the module side. | No firmware-side reconciliation owed at the topology layer. |
| Module-side relay input drive-level compatibility | Module schematic + Core `IO3` source | **Topology is consistent** with direct 3.3 V CMOS drive into the MMBT3904 base. | No firmware-side reconciliation owed at the drive-level layer, **but** the package binding still depends on which Core-side GPIO physically carries the `Relay` net (see next row). |
| Ceiling-Core `relay_pin` resolution | [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) line 61 | `GPIO4`. **Disagrees** with the Core schematic (`IO3`; `IO4 = SEN0609_RX`). Belongs to the systemic Core abstract-bus mismatch. | `CORE-ABSTRACT-BUS-001` per [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups). Not owned by HW-PINMAP-310 or its FOLLOWUP. |
| Generic-Core `relay_pin` resolution | [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) line 63 | `GPIO10`. **Disagrees** with the Core schematic. Same systemic mismatch. | `CORE-ABSTRACT-BUS-001`. Not owned by HW-PINMAP-310 or its FOLLOWUP. |
| `fan_relay.yaml` default | [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) line 27 | `fan_relay_pin: ${relay_pin}`. Inherits whichever value the parent Core abstract package binds — and both candidate values disagree with the Core schematic. | `PACKAGE-RELAY-001` (depends on `CORE-ABSTRACT-BUS-001` landing first or with it). |
| Module-side `J1` 3-pin load-side connector identity | `S360-310-R4.pdf` | 3 contact-side pins labelled `1` / `2` / `3` wired to `K1` contacts. **Schematic-confirmed** at the connector / pin-count layer. | — |
| `J1` NO / COM / NC mapping | _(not labelled on the schematic)_ | **Unknown.** SPDT-consistent but specific assignment to `J1` pins 1 / 2 / 3 is silkscreen-only. | Bench / silkscreen slice. |
| `K1` part number / coil voltage / contact rating | _(not labelled on the schematic)_ | **Unknown.** Relay reference is `K1` only; no datasheet identity, no coil voltage, no contact current / voltage / AC vs DC rating, no isolation rating. | BOM (or board / silkscreen photographs). |
| Core-to-module 3-pin harness identity | _(not inspected)_ | **Unknown.** Conductor count, length, connector key / polarity at both ends, conductor-by-conductor mapping. | Bench / harness inspection slice. |
| Module-side opto-isolation | `S360-310-R4.pdf` | **Absent.** No opto between the `Relay` input and the relay coil drive transistor. | Recorded; not a HW-PINMAP-310 question. Any mains-side isolation comes from the `K1` contact-to-coil isolation alone (relay part identity not visible). |
| Module-side on-board indicator LED | `S360-310-R4.pdf` | **Absent.** No indicator LED on the sheet. | Recorded; not a HW-PINMAP-310 question. Board-design decision, not a firmware question. |
| Module-side mains-side snubber | `S360-310-R4.pdf` | **Absent.** No RC snubber or MOV across the `K1` switching contacts. | Recorded; not a HW-PINMAP-310 question. |
| Date / Title / Rev fields on the schematic | `S360-310-R4.pdf` title block | **Blank.** Provenance is the title-block file field (`S360-310-R4.kicad_sch`); no commit / revision metadata is on the sheet. | A later HW-ASSETS-310-equivalent re-ingest if KiCad source / project metadata is ever supplied. |

The classification above does **not** change any package, product,
WebFlash wrapper, build, release, or catalog status.

## Known unresolved issues

The list below is the full evidence-gap inventory for
HW-PINMAP-310 after HW-PINMAP-310-FOLLOWUP. Resolving any of these
items belongs to a separate, evidence-bearing follow-up — **not**
to this PR.

- **Systemic Core abstract-bus mismatch (`relay_pin`).** The
  ceiling Core abstract package binds `relay_pin: GPIO4`; the
  generic Core abstract package binds `relay_pin: GPIO10`; the Core
  schematic places `Relay` on `IO3`. Resolution is owed to
  `CORE-ABSTRACT-BUS-001` per
  [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups).
- **Module-side `J2` silkscreen pin-1 location.** Not derivable
  from the schematic; required before the physical Core ↔ module
  harness conductor-by-conductor mapping can be confirmed.
- **Core-side `J4` silkscreen pin-1 location.** Not derivable
  from the Core schematic capture; part of the broader Core
  silkscreen evidence gate in
  [`s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status).
- **Module-side `J1` NO / COM / NC mapping.** Not labelled on
  the schematic; required before any switched-load wiring guidance
  is published.
- **`K1` relay part identity, coil voltage, and contact rating.**
  Not visible on the schematic; required from the BOM (or from
  board-image / silkscreen photos) to determine which fan loads
  are safe to switch and whether the `Q1` MMBT3904 SOA is adequate
  for the chosen relay coil.
- **Core-to-module harness identity.** Cable pin order, conductor
  count, length, connector key / polarity at both ends.
- **No bench / continuity / waveform verification.** No populated
  `S360-310-R4` board on a `S360-100-R4` Core has been exercised;
  no operator / reviewer / serial recorded.
- **No KiCad source / KiCad PCB / project metadata / BOM / CPL /
  gerbers / drill / STEP / board images / silkscreen photos /
  bench photos.** Per
  [`docs/hardware/artifacts/S360-310-R4.md` Files NOT provided in this upload](artifacts/S360-310-R4.md#files-not-provided-in-this-upload).
- **No standalone schematic-backed reference doc** in the
  per-board `docs/hardware/sXXX-r4-<role>.md` reference pattern
  (`s360-100-r4-core.md` / `s360-200-r4-roomiq.md` /
  `s360-210-r4-airiq.md` / `s360-211-r4-ventiq.md` /
  `s360-300-r4-led.md` style). This audit is the schematic-backed
  reconciliation record, not a per-board reference doc. A later
  PR may rewrite this file (or add a companion
  `s360-310-r4-relay-reference.md`) once silkscreen / harness /
  BOM evidence arrives.

## Package YAML status

**Status: `package-yaml-pending` / `needs-package-reconciliation`.**
Not `confirmed-ok`. Not `needs-package-change` (a definitive change
requires schematic-backed reconciliation we have only partially
performed — the module schematic exists and the module-side drive
topology is recorded, but the Core abstract `relay_pin`
disagreement and the silkscreen / harness / BOM evidence have not
landed). Not `blocked` (no named blocker prevents the work — it
just has not been done).

[`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
stays as-is. This PR does not edit it.

The package's status will move only after:

1. `CORE-ABSTRACT-BUS-001` (alias for
   [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups))
   resolves the `IO3` / `GPIO4` / `GPIO10` disagreement at the Core
   abstract-bus layer.
2. Module-side silkscreen / harness / BOM evidence (`J2` pin-1
   orientation; `J1` NO / COM / NC; `K1` part identity / contact
   rating; Core-to-module harness conductor-by-conductor mapping)
   lands under a later evidence-bearing slice.
3. `PACKAGE-RELAY-001` (per
   [`board-readiness-matrix.md` Follow-up PR sequence](board-readiness-matrix.md#follow-up-pr-sequence))
   chooses an action — confirm, change, or defer — and that
   decision is recorded in
   [`firmware-package-mapping-audit.md`](firmware-package-mapping-audit.md)
   under the existing six-label taxonomy.

**PACKAGE-RELAY-001 investigation outcome.** PACKAGE-RELAY-001 has
been investigated against the conditions enumerated above and is
**confirmed deferred**: `CORE-ABSTRACT-BUS-001` (alias for
[`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups))
has not landed, so the `IO3` (Core schematic) vs `GPIO4`
([`sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml)
line 61) vs `GPIO10`
([`sense360_core.yaml`](../../packages/hardware/sense360_core.yaml)
line 63) `relay_pin` disagreement is unresolved; the module-side
silkscreen / harness / `K1` BOM evidence (module-side `J2` pin-1
orientation; module-side `J1` pin-1 orientation; module-side `J1`
NO / COM / NC mapping; Core-side `J4` pin-1 orientation; `K1` part
identity / coil voltage / contact configuration / contact rating /
isolation rating; Core-to-module 3-pin harness conductor-by-
conductor mapping) is not on file; and no bench / continuity /
waveform evidence against a populated `S360-310-R4` + `S360-100-R4`
pair has been recorded.
[`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
is **not** edited by the PACKAGE-RELAY-001 investigation — the
package is already correctly abstracted (`fan_relay_pin: ${relay_pin}`
line 27 inherits whichever value the parent Core abstract package
binds), and the wrong values live in the Core abstract packages,
not in `fan_relay.yaml`. The override-hook comment block (lines
22–25), the `switch.platform: gpio` declaration with
`pin: ${fan_relay_pin}` (line 38), the `restore_mode:
RESTORE_DEFAULT_OFF` posture, the `on_turn_on` /  `on_turn_off`
log actions, the `fan_auto_mode` global (lines 50–53), and the
`fan_emergency_stop` script (lines 58–65) are all preserved. The
Core abstract packages
[`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml)
and
[`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml)
are also **not** edited — the `IO3` vs `GPIO4` vs `GPIO10`
`relay_pin` resolution stays owned by `CORE-ABSTRACT-BUS-001`, not
by PACKAGE-RELAY-001. The investigation outcome and full
do-not-change inventory are recorded in
[`docs/cleanup-audit.md` §PACKAGE-RELAY-001 update](../cleanup-audit.md#package-relay-001-update-deferred--core-abstract-bus-001--silkscreen--harness--k1-bom-evidence-not-landed).
The package's status stays `package-yaml-pending` /
`needs-package-reconciliation` (not `confirmed-ok`, not
`needs-package-change`, not `blocked`); the `schematic-evidence-
pending` + `needs-package-reconciliation` row in
[`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md#fan_relayyaml--s360-310)
is unchanged. FanRelay remains **not** Release-One, **not**
REQUIRED_CONFIGS, **not** kit / default, **not** recommended, and
**not** present in any active product YAML, WebFlash wrapper,
build-matrix row, release artifact, or WebFlash import.

## Product / WebFlash status

**Unchanged.** This PR does not promote, demote, add, or remove any
product, WebFlash wrapper, build, release, or import.

Availability snapshot for `S360-310` after this PR (same as before
this PR):

- Hardware-evidence axis: `cataloged-unverified` JSON +
  `partially-documented` audit (Core-side `J4` captured;
  module-side schematic now committed under HW-ASSETS-310;
  silkscreen / harness pin-1 evidence still owed).
- Pin-map axis: schematic-backed logical match recorded by
  HW-PINMAP-310-FOLLOWUP; silkscreen / harness verification still
  owed.
- Package-YAML axis: `package-yaml-pending` /
  `needs-package-reconciliation`.
- Product-YAML axis: no active product consumes the board.
  `firmware-missing` for any FanRelay-bearing config.
- WebFlash-availability axis: `firmware-build-pending` /
  `design-pending` for any FanRelay-bearing config.
- WebFlash wizard treatment: must show any FanRelay-bearing
  selection as `design-pending — not buildable today`, per
  [`docs/product-availability-taxonomy.md` How WebFlash should consume this taxonomy](../product-availability-taxonomy.md#how-webflash-should-consume-this-taxonomy).
  This audit does not edit any WebFlash-side code.

Release-One stays `Ceiling-POE-VentIQ-RoomIQ` on `stable` with
artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`
and tag `v1.0.0`. The LED preview entry
`Ceiling-POE-VentIQ-RoomIQ-LED` stays `status: preview`,
`channel: preview`. FanTRIAC
(`Ceiling-POE-VentIQ-FanTRIAC-RoomIQ`) stays `status: blocked`,
`blocker: HW-005`, `webflash_build_matrix: false`. Mains-voltage
compliance for `S360-320` and `S360-400` (COMPLIANCE-001) is
unchanged. The fan-driver `max-one-of` rule in
[`config/webflash-compatibility.json`](../../config/webflash-compatibility.json)
continues to apply: any future `FanRelay`-bearing config is
mutually exclusive with `FanPWM` / `FanDAC` / `FanTRIAC`.

## Required evidence before promotion

A future PR that wants to close HW-PINMAP-310 and unlock any
downstream package / product / WebFlash work must supply, at
minimum:

1. **Core abstract-bus rebind decision (`CORE-ABSTRACT-BUS-001`).**
   Re-anchor
   [`sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml)
   and
   [`sense360_core.yaml`](../../packages/hardware/sense360_core.yaml)
   against the verified Core schematic. Resolves the
   `relay_pin: IO3` vs `GPIO4` vs `GPIO10` disagreement. This is
   the **only** acceptable place to make that decision.
2. **Silkscreen verification.** Reading of the module-side `J2`
   1-to-3 silkscreen pin order, the module-side `J1` 1-to-3
   silkscreen pin order (with NO / COM / NC labels if present), and
   the Core-side `J4` 1-to-3 silkscreen pin order against a
   physical board.
3. **Harness verification.** Inspection of the physical 3-pin
   Core ↔ module cable; conductor-by-conductor pin mapping from
   Core `J4` pin N to module `J2` pin N.
4. **`K1` BOM identity.** Relay part number, coil voltage, contact
   configuration (SPST / SPDT), contact current / voltage / AC vs
   DC rating, isolation rating. Required to determine which fan
   loads are safe to switch and whether the `Q1` MMBT3904 SOA is
   adequate.
5. **`J1` NO / COM / NC mapping.** Either from the silkscreen, the
   `K1` part datasheet plus its module-side wiring, or
   continuity testing of a populated module.
6. **Optional bench / continuity / waveform evidence.** Populated
   `S360-310-R4` board on a `S360-100-R4` Core, with operator /
   reviewer / serial recorded, and confirmation of the
   coil-drive waveform, the `K1` switching behaviour, and the
   load-side continuity.
7. **Standalone schematic-backed reference doc.** A later PR may
   rewrite this audit (or add a companion
   `s360-310-r4-relay-reference.md`) in the per-board reference
   style of
   [`s360-200-r4-roomiq.md`](s360-200-r4-roomiq.md) /
   [`s360-211-r4-ventiq.md`](s360-211-r4-ventiq.md) /
   [`s360-300-r4-led.md`](s360-300-r4-led.md), with pin / connector
   tables sourced from the schematic plus silkscreen / harness
   evidence.
8. **Schematic-status promotion PR (separate, JSON-only).** Flips
   `schematic_status` for `S360-310` from `cataloged_unverified`
   to `verified` and sets `schematic_file` to
   `docs/hardware/schematics/S360-310-R4.pdf` in
   [`config/hardware-catalog.json`](../../config/hardware-catalog.json).
9. **Package implementation PR (`PACKAGE-RELAY-001`).** Reconciles
   [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
   against the now-verified schematic evidence and the rebound
   Core abstract substitutions.
10. **Product onboarding PR (`PRODUCT-RELAY-001`).** Adds a
    FanRelay-bearing product YAML (e.g. a future
    `Ceiling-POE-VentIQ-FanRelay-RoomIQ` config) only if and when
    a customer-facing surface is decided. Goes through
    [`docs/product-onboarding.md`](../product-onboarding.md) gates.
    If the product switches mains loads, a separate
    COMPLIANCE-001-equivalent slice owns the mains-side compliance
    evidence.
11. **WebFlash onboarding PRs (`WEBFLASH-RELAY-001`,
    `RELEASE-RELAY-001`, `WF-IMPORT-RELAY-001`).** WebFlash wrapper
    under [`products/webflash/`](../../products/webflash/),
    build-matrix entry in
    [`config/webflash-builds.json`](../../config/webflash-builds.json),
    GitHub Release artifact, and WebFlash-side import per
    [`docs/webflash-release-handoff.md`](../webflash-release-handoff.md).

Curating S360-310 artifacts and committing a module-side schematic
PDF (HW-ASSETS-310) does **not** by itself upgrade the JSON status
of the board. Promotion requires both the pin-map reconciliation
above and a separate JSON PR — same rule applied to
[S360-100](artifacts/S360-100-R4.md), [S360-311](artifacts/S360-311-R4.md),
[S360-312](artifacts/S360-312-R4.md), and
[S360-320](artifacts/S360-320-R4.md).

## PACKAGE-RELAY-001 readiness refresh after CORE-ABSTRACT-BUS-001C / 001A

This section is a docs-only readiness refresh that re-evaluates the
[`PACKAGE-RELAY-001`](#follow-up-pr-sequence) blocker set against the
post-`CORE-ABSTRACT-BUS-001C` / post-`CORE-ABSTRACT-BUS-001A` repo
state. It is the consolidated lens for the
[2026-05-21 — PACKAGE-RELAY-001 readiness refresh after 001C / 001A](#hw-pinmap-310-followup-audit-log)
audit-log row. It does **not** promote the audit, does **not** edit
[`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml),
does **not** edit any Core abstract package, does **not** add any
product YAML, WebFlash wrapper, build-matrix row, release artifact,
WebFlash import, kit, or compliance claim, and does **not** promote
`S360-310` `schematic_status` from `cataloged_unverified` in
[`config/hardware-catalog.json`](../../config/hardware-catalog.json).

### Inputs consumed

- [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
  — single-channel FanRelay package; `fan_relay_pin: ${relay_pin}`
  (line 27) inherits the parent Core abstract package binding;
  byte-identical to the HW-PINMAP-310-FOLLOWUP investigation state.
- [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml)
  — Release-One Core; post-001A `relay_pin: GPIO3`; post-001C UART
  split + retired `status_led_pin` + retired `expansion_gpio1..4` +
  `pir_sensor_pin: GPIO15` + `fan_status_led_pin: GPIO46`; consumed by
  [`products/sense360-ceiling-poe-ventiq-roomiq.yaml`](../../products/sense360-ceiling-poe-ventiq-roomiq.yaml).
- [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py)
  — pin-pinning regression scaffold landed by 001C / PR #557 and
  extended by 001A / PR #558; `RELAY_REBIND_PACKAGES` list pins
  `relay_pin: GPIO3` across the five affected Core abstract packages;
  `MainRelaySwitchBindingTests` asserts `id: main_relay` binds
  `pin: ${relay_pin}`; `RoomIQHiLinkUartTests` / `RoomIQSen0609UartTests`
  / `ExpanderIntPinTests` / `SX1509InterruptPinTests` / `PirSensorPinTests`
  / `ComfortCeilingAlsIntTests` / `NoSubstitutionCollisionTests` lock
  the 001C rebinds.
- [`docs/hardware/core-abstract-bus-reconciliation.md`](core-abstract-bus-reconciliation.md)
  — parent audit doc; §[2026-05-21 — CORE-ABSTRACT-BUS-001C implementation](core-abstract-bus-reconciliation.md#2026-05-21--core-abstract-bus-001c-implementation)
  and §[2026-05-21 — CORE-ABSTRACT-BUS-001A implementation](core-abstract-bus-reconciliation.md#2026-05-21--core-abstract-bus-001a-implementation)
  audit-log entries record the substitution-layer state.
- [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md)
  — operator-decision evidence; decisions #16 / #17 record the
  `GPIO3` strap-pin boot-behaviour observation as **scoped to the
  populated `S360-310-R4` + `S360-100-R4` pair under operator
  review**, not a generic claim.
- [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md)
  §[`fan_relay.yaml` / S360-310](package-readiness-matrix.md#fan_relayyaml--s360-310)
  — row status stays `schematic-evidence-pending` +
  `needs-package-reconciliation` (refreshed by this PR to record the
  001A / 001C substitution-layer resolution).
- [`docs/hardware/firmware-package-mapping-audit.md` Release-One product YAML package stack](firmware-package-mapping-audit.md#release-one-product-yaml-package-stack)
  — the `relay_pin: GPIO4` finding refreshed by this PR to record
  the post-001A `relay_pin: GPIO3` value.

### Readiness table

Each row enumerates a known PACKAGE-RELAY-001 blocker, the previous
state recorded in the HW-PINMAP-310-FOLLOWUP audit, the current state
after PR #557 / PR #558, the evidence source that supports the
verdict, whether the blocker still blocks PACKAGE-RELAY-001, and what
must land to unblock it.

| Blocker | Previous state | Current state after PR #557 / PR #558 | Evidence source | Still blocks PACKAGE-RELAY-001? | What unblocks it |
|---|---|---|---|---|---|
| `GPIO3` collision (`relay_pin: GPIO3` vs `comfort_ceiling_als_int_pin: GPIO3` / `expander_int_pin: GPIO3` / `sx1509_interrupt_pin: GPIO3`) | **Blocking**. The schematic-correct relay net on `IO3` collided with three other abstract bindings in Release-One via [`comfort_ceiling.yaml`](../../packages/expansions/comfort_ceiling.yaml), [`sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml), and [`gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml). | **Resolved at the substitution layer.** `comfort_ceiling_als_int_pin: GPIO47`, `expander_int_pin: GPIO17`, `sx1509_interrupt_pin: GPIO17`. `relay_pin: GPIO3` is now collision-free. | [`packages/expansions/comfort_ceiling.yaml`](../../packages/expansions/comfort_ceiling.yaml) line 46; [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml); [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml) line 21; [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) `NoSubstitutionCollisionTests` / `ComfortCeilingAlsIntTests` / `ExpanderIntPinTests` / `SX1509InterruptPinTests`. | **No** (substitution-layer only). | n/a — closed by PR #557. |
| `relay_pin` substitution disagreement (`IO3` schematic vs `GPIO4` ceiling Core vs `GPIO10` generic Core) | **Blocking**. Five Core abstract packages bound `relay_pin` to `GPIO4` or `GPIO10`, neither matching the Core schematic `IO3 = Relay`. | **Resolved at the substitution layer.** All five non-voice Core abstract packages now bind `relay_pin: GPIO3`. | [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) line 49; [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) line 78; [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml) line 51; [`packages/hardware/sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml) line 64; [`packages/hardware/sense360_core_wall.yaml`](../../packages/hardware/sense360_core_wall.yaml) line 71; [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) `RelayPinRebindTests`. | **No** (substitution-layer only). | n/a — closed by PR #558. |
| `fan_relay.yaml` abstraction correctness | **Already correct.** `fan_relay_pin: ${relay_pin}` (line 27) inherits the parent Core abstract package binding; the override-hook comment block (lines 22–25), the `switch.platform: gpio` declaration (line 38), and the `restore_mode: RESTORE_DEFAULT_OFF` / `fan_emergency_stop` script wiring carry no hardcoded GPIOs. | **Unchanged and now resolves to schematic-correct GPIO3.** The package was not edited by 001A / 001C; the substitution `${relay_pin}` now resolves to `GPIO3` against the rebound Core abstract packages. | [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) line 27; [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) `MainRelaySwitchBindingTests`. | **No.** | n/a — package abstraction is correct. |
| Pin-pinning regression test for `relay_pin` | **Absent.** No test scaffold existed for `relay_pin` or the Core abstract-bus values. | **Landed.** `tests/test_core_abstract_bus.py` `RelayPinRebindTests` pins `relay_pin: GPIO3` across the five Core abstract packages and asserts the pre-001A `GPIO4` / `GPIO10` values are absent. `MainRelaySwitchBindingTests` asserts `id: main_relay` binds `pin: ${relay_pin}` so downstream products inherit the schematic-correct value through substitution. | [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) (`RELAY_REBIND_PACKAGES`, `RelayPinRebindTests`, `MainRelaySwitchBindingTests`). | **No.** | n/a — closed by PR #558. |
| S360-100 Core `J4` silkscreen pin-1 orientation | **Pending — silkscreen evidence required.** No physical pin-1 reading of Core-side `J4` recorded. | **Still pending.** No silkscreen evidence committed; gate stays open. | [`docs/hardware/s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status); [§Required evidence before promotion](#required-evidence-before-promotion) item 2. | **Yes.** | `S360-100-BENCH-001` closure (operator-attributed silkscreen capture of Core `J4` 1-to-3 pin order). |
| S360-310 module-side `J2` silkscreen pin-1 orientation | **Pending — silkscreen evidence required.** No physical pin-1 reading of module-side `J2` recorded. | **Still pending.** No silkscreen evidence committed; gate stays open. | [`docs/hardware/artifacts/S360-310-R4.md` Files NOT provided in this upload](artifacts/S360-310-R4.md#files-not-provided-in-this-upload); [§Required evidence before promotion](#required-evidence-before-promotion) item 2. | **Yes.** | Module-side silkscreen capture (operator-attributed) committed under a later HW-ASSETS-310-followup or per-board bench slice. |
| S360-310 module-side `J1` silkscreen pin-1 orientation + NO / COM / NC mapping | **Pending — silkscreen evidence required.** The `K1` contact-side connector pins are labelled `1` / `2` / `3` on the schematic but `NO` / `COM` / `NC` mapping is unknown. | **Still pending.** Same gap as before; no silkscreen / continuity evidence committed. | [§Reconciliation findings](#reconciliation-findings) `J1` NO / COM / NC row; [§Required evidence before promotion](#required-evidence-before-promotion) item 5. | **Yes.** | Module-side silkscreen capture **or** continuity testing of a populated module **or** `K1` BOM identity + module-side wiring trace. |
| S360-310 Relay connector / harness identity (Core ↔ module 3-pin cable) | **Pending — harness inspection required.** No physical inspection of conductor count, length, key / polarity, or conductor-by-conductor mapping. | **Still pending.** No bench / harness inspection evidence committed. The operator-confirmed pair-scoped boot observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decision #19 does **not** substitute for harness identity evidence. | [§Reconciliation findings](#reconciliation-findings) harness row; [§Required evidence before promotion](#required-evidence-before-promotion) item 3. | **Yes.** | Bench / harness inspection slice with conductor-by-conductor pin mapping recorded (paired with `S360-100-BENCH-001` Core-side `J4` evidence). |
| `K1` BOM identity (part number, coil voltage, contact configuration, isolation rating) | **Unknown.** Relay reference is `K1` only on the schematic; no datasheet identity / coil voltage / isolation rating. | **Still unknown.** No BOM evidence has landed against `S360-310` (the `HW-BOM-ASSETS-002` / PR #535 ingest covered `S360-400` / `S360-410`, **not** `S360-310`). | [`config/hardware-catalog.json`](../../config/hardware-catalog.json) `S360-310` row (no `schematic_status: verified`); [§Required evidence before promotion](#required-evidence-before-promotion) item 4. | **Yes.** | `S360-310` BOM ingest (HW-BOM-ASSETS-S360-310 or sibling) committing the curated BOM evidence index entry. |
| `K1` contact-current rating | **Unknown.** No rating on the schematic; description text `On/off relay for bathroom fans.` is consistent with a small mechanical relay but not a rating. | **Still unknown.** No BOM evidence has landed; the `Q1` MMBT3904 NPN SOA vs `K1` coil-current adequacy claim cannot be made yet. | [§Load / contact findings](#load--contact-findings) contact-rating row; [§Required evidence before promotion](#required-evidence-before-promotion) item 4. | **Yes.** | `K1` BOM identity + vendor datasheet review (paired with the BOM ingest above). |
| Relay load / contact proof (populated `S360-310-R4` + `S360-100-R4` pair bench observation; coil-drive waveform; `K1` switching behaviour; load-side continuity) | **No bench / continuity / waveform evidence on file.** | **Still missing.** The operator-confirmed pair-scoped boot observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17 records that the populated pair booted normally with `GPIO3` in its strap-pin role and the relay stayed de-energized at boot, but it does **not** record coil-drive waveform, `K1` switching behaviour, load-side continuity, or `Q1` MMBT3904 SOA characterisation. | [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17; [§Required evidence before promotion](#required-evidence-before-promotion) item 6. | **Yes** — at the level of evidence required for `PACKAGE-RELAY-001` promotion. The pair-scoped boot observation is necessary but not sufficient. | Bench slice capturing coil-drive waveform + `K1` switching behaviour + load-side continuity through `J1` `NO` / `COM` / `NC` contacts (and ideally `Q1` SOA characterisation under the actual `K1` coil-current draw), against a populated `S360-310-R4` + `S360-100-R4` pair with operator / reviewer / serial / date recorded. |
| ESP32-S3 `GPIO3` strap-pin boot behaviour characterisation (general claim, not pair-scoped) | **Pending — bench characterisation required.** | **Still pending.** Operator decisions #16 / #17 in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) record an observed-OK boot **scoped to the populated `S360-310-R4` + `S360-100-R4` pair under operator review**, **not** a generic claim about any future relay / expansion module attached to `J4`. The `001A` implementation acknowledges this gap explicitly in its outcome bullet. | [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17; [`docs/hardware/core-abstract-bus-reconciliation.md` §2026-05-21 — CORE-ABSTRACT-BUS-001A implementation](core-abstract-bus-reconciliation.md#2026-05-21--core-abstract-bus-001a-implementation) outcome bullet 2. | **Yes** — at the level of evidence required for `PACKAGE-RELAY-001` promotion. | Bench characterisation of the ESP32-S3 `GPIO3` JTAG-select strap pin behaviour against multiple populated `S360-310-R4` + `S360-100-R4` units (oscilloscope-traced reset / boot / strap-state captures with operator / reviewer / serial / date recorded). |
| Whether `fan_relay.yaml` needs behaviour / package cleanup beyond inheriting `${relay_pin}` | **Open — investigation needed once 001A lands.** | **Closed — no cleanup needed.** The package is already structurally correct for the post-001A substitution-layer state: `fan_relay_pin: ${relay_pin}` (line 27) resolves to the schematic-correct `GPIO3`; the override-hook comment block (lines 22–25), the `switch.platform: gpio` declaration with `pin: ${fan_relay_pin}` (line 38), `restore_mode: RESTORE_DEFAULT_OFF`, the `on_turn_on` / `on_turn_off` log actions, the `fan_auto_mode` global, and the `fan_emergency_stop` script all carry no hardcoded GPIOs. The behaviour-side state (default `RESTORE_DEFAULT_OFF`; explicit emergency-stop script) is consistent with the relay-stays-off-at-boot operator observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decision #17. | [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) (whole file); [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) `MainRelaySwitchBindingTests`. | **No** — at the package-structure layer. The package is ready to compose against a future Relay product YAML the moment the hardware-evidence blockers above close. | n/a — package abstraction is already correct. |

### What 001A / 001C did **not** prove

- They did not prove the Relay load / contact / `K1` rating.
- They did not prove the module-side silkscreen pin-1 orientation.
- They did not prove the `J1` `NO` / `COM` / `NC` mapping.
- They did not prove the Core ↔ module harness identity.
- They did not promote `S360-310` `schematic_status` from
  `cataloged_unverified`.
- They did not edit
  [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml).
- They did not advance `PACKAGE-RELAY-001` /
  `PRODUCT-RELAY-001` / `WEBFLASH-RELAY-001` /
  `RELEASE-RELAY-001` / `WF-IMPORT-RELAY-001` past the
  substitution-layer precondition.

### Recommended next PR

Evidence is **not yet sufficient** for `PACKAGE-RELAY-001`
implementation. Six of the eleven enumerated blockers
(silkscreen / harness / `K1` BOM / contact rating / load-contact
proof / general `GPIO3` strap characterisation) remain unresolved
and each is owed against a separate evidence-bearing slice.

The conservative next PR therefore is **a bench-evidence-capture
slice for `S360-310`**: an `HW-ASSETS-S360-310-BENCH-001` /
`S360-310-BENCH-001` (or sibling) slice that commits the
operator-attributed silkscreen captures of module-side `J2` /
module-side `J1` (with `NO` / `COM` / `NC` labels where present) /
Core-side `J4`, the Core ↔ module harness inspection trace, the
`K1` BOM identity, the coil-drive waveform capture, and the
load-side continuity trace. Operator / reviewer / serial / date
must be recorded per
[`docs/hardware/hardware-artifact-policy.md`](hardware-artifact-policy.md).
The general ESP32-S3 `GPIO3` strap-pin boot-behaviour bench
characterisation may land in the same slice or as a sibling slice
against `S360-100-BENCH-001`.

The next PR is **not** `PACKAGE-RELAY-001` implementation. It is
**not** a Relay product YAML. It is **not** a WebFlash wrapper.
It is **not** a compile-only target. It is **not** a release
artifact. None of those become unblocked by this readiness refresh.

## S360-310-BENCH-001 — Relay bench evidence

**Status: pending — bench / silkscreen / harness / BOM / load-contact
evidence required.**

This section is the bench-side / silkscreen-side / harness-side /
BOM-side / load-contact-side companion to the schematic-side
[§Reconciliation findings](#reconciliation-findings),
[§Known unresolved issues](#known-unresolved-issues),
[§Required evidence before promotion](#required-evidence-before-promotion),
and
[§PACKAGE-RELAY-001 readiness refresh after CORE-ABSTRACT-BUS-001C / 001A](#package-relay-001-readiness-refresh-after-core-abstract-bus-001c--001a)
tables above, and to the curated artifact-side
[Open questions](artifacts/S360-310-R4.md#open-questions--verification-needed)
in [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md)
(HW-ASSETS-310). It is the canonical place where real-hardware
observations against a populated `S360-310-R4` Relay board paired
with a populated `S360-100-R4` Core board are recorded once they
exist, alongside `K1` BOM identity and the general ESP32-S3 `GPIO3`
strap-pin boot-behaviour bench characterisation gap recorded by
[`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md)
decisions #16 / #17.

**No physical bench, silkscreen, harness, BOM, load-contact, or
strap-pin boot-behaviour evidence has been supplied for this
record.** No operator, no reviewer, no review date, no observed
silkscreen pin-1 marks, no harness conductor-by-conductor trace,
no `K1` part-number reading, no coil-drive scope capture, no
contact-side continuity measurement, no oscilloscope-traced
ESP32-S3 `GPIO3` strap-state capture have been provided. The
evidence items below stay `pending — bench evidence required`;
this section does **not** close any of them and does **not**
advance `PACKAGE-RELAY-001` past the hardware-evidence gate.

### Evidence supplied

The fields below would be filled by an operator / reviewer after a
bench session against a populated `S360-310-R4` + `S360-100-R4`
pair. None has been filled. **Values are not invented.** Implicit
acceptance is not allowed.

| Field | Observed value | Evidence source |
|---|---|---|
| Review date | not recorded — pending bench evidence | — |
| Reviewer / operator initials | not recorded — pending bench evidence | — |
| Physical `S360-310-R4` board observed (serial / batch) | not recorded — pending bench evidence | — |
| Physical `S360-100-R4` Core board observed (serial / batch) | not recorded — pending bench evidence | — |
| Board revision marking (silkscreen) on `S360-310-R4` | not recorded — pending bench evidence | — |
| Core-side `J4` silkscreen pin-1 mark / dot / arrow / `1` legend | not recorded — pending bench evidence | — |
| Module-side `J2` silkscreen pin-1 mark / dot / arrow / `1` legend | not recorded — pending bench evidence | — |
| Module-side `J1` silkscreen pin-1 mark / dot / arrow / `1` legend | not recorded — pending bench evidence | — |
| Module-side `J1` `NO` / `COM` / `NC` silkscreen labels (if present) | not recorded — pending bench evidence | — |
| Core ↔ module 3-pin harness conductor count | not recorded — pending bench evidence | — |
| Core ↔ module 3-pin harness conductor-by-conductor pin mapping (Core `J4` pin N ↔ Module `J2` pin N) | not recorded — pending bench evidence | — |
| Core ↔ module 3-pin harness key / polarity at both ends (straight-through vs. crossover) | not recorded — pending bench evidence | — |
| `K1` part-number silkscreen reading / IC-top marking | not recorded — pending bench evidence | — |
| `K1` manufacturer | not recorded — pending bench evidence | — |
| `K1` coil voltage rating | not recorded — pending bench evidence | — |
| `K1` contact configuration (SPST-NO / SPDT) | not recorded — pending bench evidence | — |
| `K1` contact current rating (A) at AC / DC | not recorded — pending bench evidence | — |
| `K1` isolation rating (coil-to-contact, contact-to-contact) | not recorded — pending bench evidence | — |
| `K1` BOM line-item citation (manufacturer + part number + revision) | not recorded — pending BOM ingest | — |
| Coil-drive waveform capture at `Q1` collector (rise / fall / hold) | not recorded — pending bench evidence | — |
| Coil-drive current draw observation vs. `Q1` MMBT3904 SOA | not recorded — pending bench evidence | — |
| `K1` switching behaviour observation (energize / de-energize latency, contact bounce) | not recorded — pending bench evidence | — |
| Load-side continuity through `J1` (`NO` / `COM` / `NC` contacts) | not recorded — pending bench evidence | — |
| Relay boot state with `S360-100-R4` + `S360-310-R4` attached (de-energized vs. energized at power-on) | not recorded — pending bench evidence | — |
| Expected controlled load type (inline bathroom fan / SSR input / other) | not recorded — pending bench evidence | — |
| ESP32-S3 `GPIO3` strap-pin boot-behaviour capture (oscilloscope-traced reset / boot / strap-state) | not recorded — pending bench evidence | — |
| `GPIO3` strap-pin boot-behaviour generalisation status (pair-scoped vs. general claim across multiple units) | pair-scoped only — operator observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17; no general claim recorded | [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17 |
| Relay load / contact proof result | not recorded — pending bench evidence | — |
| Photos / silkscreen captures / scope traces / continuity measurements / serial logs | not supplied — pending bench evidence | — |
| Known failures or anomalies | not recorded — pending bench evidence | — |

### Per-item evidence table

Each row enumerates a `PACKAGE-RELAY-001` hardware-evidence
blocker, the artifact required to close it, its current status,
the captured value (if any), the source / note, and whether it
still blocks `PACKAGE-RELAY-001`. **`pending` = no committed
artifact in this repository.** Operator-attribution rules in
[`docs/hardware/hardware-artifact-policy.md`](hardware-artifact-policy.md)
apply to every artifact that would close one of these rows.

| Evidence item | Required artifact | Current status | Captured value | Source / note | Still blocks `PACKAGE-RELAY-001`? |
|---|---|---|---|---|---|
| S360-100 Core `J4` silkscreen / pin-1 orientation | Operator-attributed silkscreen photo of populated `S360-100-R4` Core showing `J4` pin-1 mark (dot / arrow / `1` legend) against the 3-pin connector; serial / batch recorded | `pending — bench evidence required` | not recorded | Gated on `S360-100-BENCH-001` closure ([`s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status)). No silkscreen reading on file. | **Yes.** |
| S360-310 Relay `J2` 1-to-3 pin order | Operator-attributed silkscreen photo of populated `S360-310-R4` module showing `J2` pin-1 mark and continuity-confirmed net order (`+5V` / `Relay` / `GND`) | `pending — bench evidence required` | not recorded | Schematic-only on the module side per [§Connector / pin-map findings](#connector--pin-map-findings); no silkscreen evidence on file per [`artifacts/S360-310-R4.md` Files NOT provided in this upload](artifacts/S360-310-R4.md#files-not-provided-in-this-upload). | **Yes.** |
| S360-310 Relay `J1` `NO` / `COM` / `NC` mapping | Operator-attributed silkscreen photo of populated `S360-310-R4` module showing `J1` pin-1 mark + `NO` / `COM` / `NC` labels (or continuity-traced mapping from `K1` contacts to `J1` pins 1 / 2 / 3 if labels are absent) | `pending — bench evidence required` | not recorded | Schematic does not label NO / COM / NC per [§Connector / pin-map findings](#connector--pin-map-findings); no silkscreen / continuity evidence on file. | **Yes.** |
| Core `J4` ↔ Relay `J2` harness identity, straight-through or keyed | Operator-attributed photo + conductor-by-conductor continuity trace of the 3-pin Core ↔ module cable, recording conductor count, length, connector key / polarity at both ends, and the Core `J4` pin N ↔ Module `J2` pin N mapping (verifying schematic-backed `+5V` ↔ `+5V`, `Relay` ↔ `Relay`, `GND` ↔ `GND`) | `pending — bench evidence required` | not recorded | No physical inspection of the harness has been recorded per [§Reconciliation findings](#reconciliation-findings) harness row. The operator-confirmed pair-scoped boot observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decision #19 does **not** substitute for harness identity evidence. | **Yes.** |
| `K1` BOM identity / manufacturer / part number | BOM line item committing `K1` manufacturer + part number + revision (paired with HW-BOM-ASSETS-S360-310 or sibling ingest); board-side silkscreen / IC-top photo confirming the part on a populated module | `pending — BOM ingest required` | not recorded | No `S360-310` BOM ingest has landed (the `HW-BOM-ASSETS-002` / PR #535 ingest covered `S360-400` / `S360-410`, not `S360-310`). Schematic shows `K1` reference only with no datasheet identity, no coil voltage, no contact rating. | **Yes.** |
| `K1` contact-current rating | `K1` vendor datasheet citation (paired with the BOM ingest above) + a contact-current AC / DC rating recorded against the populated part | `pending — BOM ingest + datasheet citation required` | not recorded | No rating on the schematic per [§Load / contact findings](#load--contact-findings). Description text `On/off relay for bathroom fans.` is consistent with a small mechanical relay but not a rating. The `Q1` MMBT3904 NPN SOA vs `K1` coil-current adequacy claim cannot be made until the rating is known. | **Yes.** |
| Expected controlled load type | Documented load class (inline bathroom fan, SSR input, low-voltage signalling, mains AC) recorded against the bench harness, with mains-side wiring guidance deferred to a later compliance slice if mains loads are within scope | `pending — bench evidence required` | not recorded | Hardware-catalog description `On/off relay for bathroom fans.` implies inline-fan use; not yet bench-confirmed. Any future mains-switching FanRelay product would also require a separate COMPLIANCE-001-equivalent slice. | **Yes.** |
| Relay boot state with `S360-100-R4` + `S360-310-R4` attached | Operator-attributed observation of relay coil state at power-on and during ESP32-S3 reset / boot (de-energized vs. energized), captured against a populated pair with serial / batch recorded | `pending — bench evidence required` | The operator-confirmed pair-scoped observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decision #17 records the relay stays de-energized at boot for the observed pair under operator review, but is **not** an artifact-committed capture | [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17 (pair-scoped only) | **Yes** — at the level of evidence required for `PACKAGE-RELAY-001` promotion. The pair-scoped boot observation is necessary but not sufficient. |
| `GPIO3` boot characterisation generalisation status | Oscilloscope-traced reset / boot / strap-state captures of ESP32-S3 `GPIO3` against **multiple** populated `S360-310-R4` + `S360-100-R4` units with operator / reviewer / serial / date recorded, sufficient to support a general claim about ESP32-S3 `GPIO3` JTAG-select strap behaviour with the Relay module attached | `pending — bench characterisation required (general claim, not pair-scoped)` | not recorded | Operator decisions #16 / #17 in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) record an observed-OK boot **scoped to the populated `S360-310-R4` + `S360-100-R4` pair under operator review**, **not** a generic claim about any future relay / expansion module attached to `J4`. The `CORE-ABSTRACT-BUS-001A` implementation acknowledges this gap explicitly in its outcome bullet per [`core-abstract-bus-reconciliation.md` §2026-05-21 — CORE-ABSTRACT-BUS-001A implementation](core-abstract-bus-reconciliation.md#2026-05-21--core-abstract-bus-001a-implementation). | **Yes.** |
| Relay load / contact proof | Oscilloscope or bench capture of (a) coil-drive waveform at `Q1` collector, (b) `K1` switching behaviour (energize / de-energize latency, contact bounce), (c) load-side continuity through `J1` `NO` / `COM` / `NC` contacts, optionally (d) `Q1` MMBT3904 SOA under the actual `K1` coil-current draw, against a populated `S360-310-R4` + `S360-100-R4` pair with operator / reviewer / serial / date recorded | `pending — bench evidence required` | not recorded | No coil-drive waveform, no `K1` switching capture, no load-side continuity trace on file. The pair-scoped operator observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17 does **not** include any of these captures. | **Yes.** |

### Status-language rules

The only status values this record may carry are:

- `pending — bench evidence required` (no committed artifact)
- `partial — some evidence recorded, but open items remain`
- `verified — all listed Relay bench / silkscreen / harness / BOM
  / load-contact items answered by evidence`
- `failed / blocked — evidence produced a blocking issue`

Today every row above is `pending`. Anything stronger is a future
bench / BOM / silkscreen / harness PR change and must cite real
evidence (operator-attributed photos / scope traces / continuity
measurements / harness photographs / BOM line items) per the
field table above and per
[`docs/hardware/hardware-artifact-policy.md`](hardware-artifact-policy.md).

### What this record does **not** do

S360-310-BENCH-001 is documentation only. It does not promote any
state on its own — even if it later flips to `verified`, the
downstream gates remain separate.

- It does **not** edit
  [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml).
- It does **not** implement `PACKAGE-RELAY-001`. The package edit
  (or the formal documentation that no package edit is needed) is
  owed to a separate evidence-bearing `PACKAGE-RELAY-001` PR after
  the rows above flip to `verified`.
- It does **not** add a Relay product YAML under
  [`products/`](../../products/), a WebFlash wrapper under
  [`products/webflash/`](../../products/webflash/), a build-matrix
  row in
  [`config/webflash-builds.json`](../../config/webflash-builds.json),
  or a release artifact / tag / checksum.
- It does **not** flip `webflash_build_matrix: true` for any
  FanRelay-bearing entry.
- It does **not** add an `artifact_name` for any FanRelay-bearing
  config string.
- It does **not** add a `FanRelay` token to any current or future
  Release-One config string.
- It does **not** promote `S360-310` `schematic_status` from
  `cataloged_unverified` in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json)
  to `verified`, and does **not** set `schematic_file`.
- It does **not** claim Relay load / contact / `K1` rating proof,
  WebFlash import readiness, or `RELEASE-RELAY-001` unblock.
- It does **not** advance `PRODUCT-RELAY-001`,
  `WEBFLASH-RELAY-001`, `RELEASE-RELAY-001`, or
  `WF-IMPORT-RELAY-001` past their existing blocked state behind
  `PACKAGE-RELAY-001`.
- It does **not** generalise the pair-scoped `GPIO3` boot-OK
  observation in
  [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md)
  decisions #16 / #17 to a general claim about ESP32-S3 `GPIO3`
  strap-pin boot behaviour.
- It does **not** change Release-One. Release-One stays
  `Ceiling-POE-VentIQ-RoomIQ`, version `1.0.0`, channel `stable`,
  artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`,
  tag `v1.0.0`.
- It does **not** change the LED preview path.
  `Ceiling-POE-VentIQ-RoomIQ-LED` stays `status: preview`,
  `channel: preview`.
- It does **not** unblock FanTRIAC. FanTRIAC stays `status:
  blocked`, `blocker: HW-005`, `webflash_build_matrix: false`.
- It does **not** change the mains-voltage compliance status of
  `S360-320` or `S360-400` (owned by COMPLIANCE-001).

### Summary verdict

**Evidence is incomplete.** Ten of the ten enumerated
hardware-evidence items above are `pending — bench evidence
required`; **none** has been captured against a populated
`S360-310-R4` + `S360-100-R4` pair, and no `K1` BOM ingest has
landed. The Core abstract-bus substitution-layer blockers are
**resolved** by `CORE-ABSTRACT-BUS-001C` / PR #557 and
`CORE-ABSTRACT-BUS-001A` / PR #558 (the readiness lens is
recorded in
[§PACKAGE-RELAY-001 readiness refresh after CORE-ABSTRACT-BUS-001C / 001A](#package-relay-001-readiness-refresh-after-core-abstract-bus-001c--001a)),
but the hardware-evidence blockers above stay open and continue
to block `PACKAGE-RELAY-001`. `PACKAGE-RELAY-001` is therefore
**not implementation-ready at the evidence layer**;
`PRODUCT-RELAY-001`, `WEBFLASH-RELAY-001`, `RELEASE-RELAY-001`,
and `WF-IMPORT-RELAY-001` stay blocked behind
`PACKAGE-RELAY-001`. No Relay package YAML edit, no Relay product
YAML, no WebFlash wrapper, no compile-only target, and no release
artifact / tag / checksum is made implementation-ready by this
PR.

## Follow-up PR sequence

Each entry is a separate PR with its own scope, review, and gate
evidence. None is committed to a calendar by this PR. Names follow
the existing style in
[`board-readiness-matrix.md` Follow-up PR sequence](board-readiness-matrix.md#follow-up-pr-sequence)
and
[`hardware-artifact-policy.md` Follow-up PR sequence](hardware-artifact-policy.md#follow-up-pr-sequence).

| PR | Purpose | Gated on |
|---|---|---|
| **`CORE-ABSTRACT-BUS-001`** | Systemic Core abstract-bus rebind that re-anchors [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) and [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) against the verified Core schematic. Resolves the `relay_pin: GPIO4` vs `GPIO10` vs `IO3` disagreement for `S360-310` and the parallel `relay_pin` / `expansion_gpio*` / `status_led_pin` / `pir_sensor_pin` / `halo_i2c` / `expansion_i2c` / `uart_bus` disagreements for the other fan-driver slices. | Per [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups). Independent of HW-PINMAP-310. |
| **`S360-310` `schematic_status` promotion** | Separate JSON-only PR that flips `schematic_status` for `S360-310` from `cataloged_unverified` to `verified` and sets `schematic_file: docs/hardware/schematics/S360-310-R4.pdf`. | Silkscreen / harness / BOM evidence landed (no outstanding flags). |
| **`PACKAGE-RELAY-001`** (alias: `PACKAGE-GAP-001` FanRelay slice) | Reconcile [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) against the schematic-backed reconciliation in this audit and the rebound Core abstract substitutions. | `CORE-ABSTRACT-BUS-001` landed (or paired with it); silkscreen / harness / BOM evidence landed. |
| **`PRODUCT-RELAY-001`** (alias: `PRODUCT-GAP-001` FanRelay slice) | Add a `FanRelay`-bearing product YAML (only if and when a customer-facing surface is decided). Goes through [`docs/product-onboarding.md`](../product-onboarding.md) gates. Subject to the fan-driver `max-one-of` rule. If the product switches mains loads, a separate COMPLIANCE-001-equivalent slice gates the product. | `PACKAGE-RELAY-001` landed. |
| **`WEBFLASH-RELAY-001`** | Add the WebFlash wrapper under [`products/webflash/`](../../products/webflash/) for the FanRelay-bearing product. | `PRODUCT-RELAY-001` landed. |
| **`RELEASE-RELAY-001`** (alias: `RELEASE-GAP-001` FanRelay slice) | Add the [`config/webflash-builds.json`](../../config/webflash-builds.json) entry, the GitHub Release artifact, and the build / release proof per [`docs/webflash-release-proof.md`](../webflash-release-proof.md). Stable promotion goes through the 17-row [`docs/preview-to-stable-promotion-gates.md`](../preview-to-stable-promotion-gates.md) gate. | `WEBFLASH-RELAY-001` landed. |
| **`WF-IMPORT-RELAY-001`** (alias: `WF-IMPORT-GAP-001` FanRelay slice) | WebFlash-side import per [`docs/webflash-release-handoff.md`](../webflash-release-handoff.md). | `RELEASE-RELAY-001` landed. |
| **HW-ASSETS-310-followup (manufacturing evidence)** | If KiCad source, KiCad PCB, BOM, CPL, gerbers, drill, STEP, silkscreen photos, bench photos, or board images later arrive for `S360-310-R4`. | Per-board decision recorded in [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md) updated by the new PR. |

A separate, later PR is required before any
retained-but-not-committed artifact class for `S360-310-R4` (KiCad
source, KiCad PCB, BOM, CPL, gerbers, drill files, STEP, images)
graduates to **may be committed**. That PR must choose a storage
backend per
[Future storage decision](hardware-artifact-policy.md#future-storage-decision)
in HW-ASSETS-001.

## Do-not-change guardrails

This PR does **not** edit:

- [`config/hardware-catalog.json`](../../config/hardware-catalog.json)
- [`config/product-catalog.json`](../../config/product-catalog.json)
- [`config/webflash-builds.json`](../../config/webflash-builds.json)
- [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json)
- any product YAML under [`products/`](../../products/)
- any WebFlash wrapper under
  [`products/webflash/`](../../products/webflash/)
- any package YAML under [`packages/`](../../packages/), including
  [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml),
  [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml),
  and [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml)
- any script under `scripts/`
- any test under `tests/`
- any workflow under `.github/workflows/`
- any component under `components/`
- any header under `include/`
- `.gitignore`
- the per-board reference docs
  ([`s360-100-r4-core.md`](s360-100-r4-core.md) and siblings) —
  the Core `J4` capture stays as it is; the
  `relay_pin` disagreement is recorded here and is **not**
  rewritten on the Core-side doc.
- the curated artifact index
  [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md) —
  its content is **consumed** by this audit, not rewritten by it.
- [`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf) —
  the schematic PDF stays byte-identical to the HW-ASSETS-310
  commit.

This PR does **not**:

- mark `S360-310` `verified`,
- add or set `schematic_file` for `S360-310` in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json),
- mark the pin map confirmed,
- change [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
  from `package-yaml-pending` / `needs-package-reconciliation` to
  `confirmed-ok`,
- promote `S360-310` to `preview` / `stable` / `production`,
- add a `FanRelay` token to any Release-One or preview config
  string,
- add a `FanRelay`-bearing entry to
  [`config/webflash-builds.json`](../../config/webflash-builds.json),
- regenerate firmware, create a GitHub Release or tag, or change
  any WebFlash manifest / import,
- resolve the `IO3` vs `GPIO4` vs `GPIO10` `relay_pin`
  disagreement,
- unblock `PACKAGE-RELAY-001`, `PRODUCT-RELAY-001`,
  `WEBFLASH-RELAY-001`, `RELEASE-RELAY-001`, or
  `WF-IMPORT-RELAY-001`,
- unblock FanTRIAC (HW-005 stays a separate gate),
- change the Sense360 LED Release-One exclusion,
- change the Core J10 vs RoomIQ J6 pin-order open question,
- change the mains-voltage compliance status for `S360-320` or
  `S360-400` (COMPLIANCE-001),
- claim that any bench / continuity / harness / silkscreen
  evidence exists,
- claim that any compliance evidence for a mains-switching
  FanRelay product exists,
- resolve the systemic Core abstract-bus mismatch enumerated in
  [`docs/release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups).

## See also

- [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md)
  — HW-ASSETS-310 curated artifact index; the schematic-side
  source of truth this audit consumes.
- [`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf)
  — committed module-side schematic PDF (HW-ASSETS-310).
- [`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md) — Core
  schematic-backed reference; §Pin assignments `IO3 → Relay`; §J4
  Relay module connector; §Fan / driver outputs `Relay` row;
  §S360-100-BENCH-001 status (bench / silkscreen gate; cross-linked
  from §[S360-310-BENCH-001 — Relay bench evidence](#s360-310-bench-001--relay-bench-evidence)
  Core-`J4` silkscreen row).
- [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md)
  — operator-decision evidence; decisions #16 / #17 record the
  pair-scoped boot-OK observation that the
  §[S360-310-BENCH-001 — Relay bench evidence](#s360-310-bench-001--relay-bench-evidence)
  `GPIO3` strap-pin boot characterisation row does **not** promote
  to a generic claim.
- [Hardware Artifact Policy (HW-ASSETS-001)](hardware-artifact-policy.md)
  — defines the per-board artifact-index schema and the
  retained-but-not-committed policy that HW-PINMAP-310-FOLLOWUP
  respects.
- [S360 Board Readiness Matrix (HW-GAP-001)](board-readiness-matrix.md)
  — per-board readiness across hardware-evidence and productization
  axes; the `S360-310` row and follow-up PR sequence row that this
  audit closes the documentation side of.
- [Remaining Board Documentation Audit](remaining-board-documentation-audit.md)
  — per-board documentation-state classification; the
  `Sense360 Relay (S360-310)` subsection.
- [Firmware Package Mapping Audit (HW-009 / HW-010)](firmware-package-mapping-audit.md)
  — six-label package classification taxonomy; the
  `relay_pin: GPIO4 — IO4 is SEN0609_RX; the Relay net is on IO3`
  finding inside the Release-One package-stack review (systemic
  Core abstract-bus mismatch, owned by
  [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)).
- [Package YAML Readiness Matrix (PACKAGE-GAP-001)](package-readiness-matrix.md)
  — `fan_relay.yaml` row; gating chain to `PACKAGE-RELAY-001`.
- [Product Availability Taxonomy (PRODUCT-AVAIL-001)](../product-availability-taxonomy.md)
  — cross-cutting availability ladder; `S360-310 Sense360 Relay
  partially-documented + design-pending` snapshot row.
- [Release-One Hardware Audit](../release-one-hardware-audit.md) —
  Required follow-ups #2 / #3, the owner of the systemic Core
  abstract-bus rebind that this audit defers to.
- [`docs/hardware/s360-311-r4-pwm.md`](s360-311-r4-pwm.md),
  [`docs/hardware/s360-312-r4-dac.md`](s360-312-r4-dac.md),
  [`docs/hardware/s360-320-r4-triac.md`](s360-320-r4-triac.md) —
  HW-PINMAP-31x sibling audits at the same
  `partial — schematic evidence available; package reconciliation
  pending` status.
- [`docs/preview-to-stable-promotion-gates.md`](../preview-to-stable-promotion-gates.md)
  — RELEASE-006 canonical 17-row preview-to-stable gate document;
  applies to any future FanRelay-bearing stable promotion.
- [`docs/product-onboarding.md`](../product-onboarding.md) —
  PRODUCT-004 ordered safe onboarding sequence; any future
  `PRODUCT-RELAY-001` slice goes through these gates.
- [`docs/webflash-contract.md`](../webflash-contract.md) — WebFlash
  artifact / grammar / token contract; `FanRelay` token mapping to
  `S360-310` (line 117).
- [`docs/cleanup-audit.md`](../cleanup-audit.md) — classification
  of stale / current / blocked-reference / legacy-compatible repo
  content; carries the HW-PINMAP-310 / HW-PINMAP-310-FOLLOWUP
  registration rows.
- [`config/hardware-catalog.json`](../../config/hardware-catalog.json)
  — machine-readable hardware catalog. `S360-310` row at lines
  62–70.
- [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json)
  — machine-readable WebFlash taxonomy; `FanRelay` reserved in
  `canonical_modules` (line 11), subject to fan-driver
  `max-one-of`.
- [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
  — single-channel FanRelay package (this audit's primary subject).

## HW-PINMAP-310-FOLLOWUP audit log

This section is the dated investigation log for
**HW-PINMAP-310-FOLLOWUP** — the schematic-backed reconciliation
work that promoted this audit from
`pending — schematic/design evidence required` to
`partial — schematic evidence available; package reconciliation
pending` (see [§Status](#status)) and, after promotion, the
ongoing docs-only re-checks of the silkscreen-side, harness-side,
BOM-side, bench-side, and Core abstract-bus-side evidence that
must land before this audit can move off `partial — schematic
evidence available; package reconciliation pending`. Each row
records a documentation-only action against the
[§Repository evidence](#repository-evidence),
[§Schematic summary](#schematic-summary),
[§Connector / pin-map findings](#connector--pin-map-findings),
[§Relay drive findings](#relay-drive-findings),
[§Load / contact findings](#load--contact-findings),
[§Existing package abstraction](#existing-package-abstraction),
[§Reconciliation findings](#reconciliation-findings),
[§Known unresolved issues](#known-unresolved-issues),
[§Package YAML status](#package-yaml-status),
[§Product / WebFlash status](#product--webflash-status),
[§Required evidence before promotion](#required-evidence-before-promotion),
and [§Follow-up PR sequence](#follow-up-pr-sequence) tables and
lists above. An audit-log entry **does not** by itself promote
the top-line status off `partial — schematic evidence available;
package reconciliation pending`, close any reconciliation row,
resolve the `IO3` / `GPIO4` / `GPIO10` `relay_pin` disagreement,
resolve the module-side `J2` silkscreen pin-1 location, resolve
the Core-side `J4` silkscreen pin-1 location, resolve the `J1`
NO / COM / NC mapping, resolve the `K1` BOM identity / coil
voltage / contact rating, resolve the Core-to-module harness
identity, change
[`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml)
from `package-yaml-pending` / `needs-package-reconciliation`,
edit any Core abstract package
([`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml),
[`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml)),
change the JSON `schematic_status` of `S360-310` from
`cataloged_unverified` in
[`config/hardware-catalog.json`](../../config/hardware-catalog.json),
set `schematic_file` for `S360-310`, add a `FanRelay`-bearing
entry to [`config/product-catalog.json`](../../config/product-catalog.json)
or [`config/webflash-builds.json`](../../config/webflash-builds.json),
add or modify any product YAML under
[`products/`](../../products/) or any WebFlash wrapper under
[`products/webflash/`](../../products/webflash/), regenerate
firmware, create a GitHub Release or tag, change any WebFlash
manifest / import, advance any of the named follow-up PRs
(`CORE-ABSTRACT-BUS-001`, `S360-310 schematic_status` promotion,
`PACKAGE-RELAY-001`, `PRODUCT-RELAY-001`, `WEBFLASH-RELAY-001`,
`RELEASE-RELAY-001`, `WF-IMPORT-RELAY-001`), close
`S360-100-BENCH-001` (
[`s360-100-r4-core.md` S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status)),
close `HW-PINMAP-311-FOLLOWUP` (
[`s360-311-r4-pwm.md` HW-PINMAP-311-FOLLOWUP audit log](s360-311-r4-pwm.md#hw-pinmap-311-followup-audit-log)),
close `HW-PINMAP-312-FOLLOWUP` (
[`s360-312-r4-dac.md` HW-PINMAP-312-FOLLOWUP audit log](s360-312-r4-dac.md#hw-pinmap-312-followup-audit-log)),
close `HW-PINMAP-320-FOLLOWUP` (
[`s360-320-r4-triac.md` HW-PINMAP-320-FOLLOWUP audit log](s360-320-r4-triac.md#hw-pinmap-320-followup-audit-log)),
close `COMPLIANCE-001` (
[`../compliance/mains-voltage-uk-eu-assessment.md` COMPLIANCE-001 audit log](../compliance/mains-voltage-uk-eu-assessment.md#compliance-001-audit-log)),
change Release-One, change the LED preview path, unblock
FanTRIAC (HW-005), or change the Sense360 LED Release-One
exclusion. The only way to move this record off `partial —
schematic evidence available; package reconciliation pending` is
to land the evidence enumerated in
[§Required evidence before promotion](#required-evidence-before-promotion)
under the named follow-up PRs in
[§Follow-up PR sequence](#follow-up-pr-sequence).

| Audit date | Scope | Files inspected | Outcome |
|---|---|---|---|
| 2026-05-18 | HW-PINMAP-310-FOLLOWUP schematic-backed reconciliation. Consume the HW-ASSETS-310 module-side schematic evidence committed at [`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf) and inventoried at [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md). Record the schematic-backed module-side `J2` 3-pin "From Core" capture (`+5V` / `Relay` / `GND`), the module-side relay coil-drive topology (`Q1` MMBT3904 NPN low-side; `R1` 1 kΩ base series; `R2` 10 kΩ pull-down; `D1` flyback diode; coil rail `+5V`; no opto; no indicator LED; no snubber), the module-side `J1` 3-pin "Inline Fan" load-side connector (3 contact-side pins on `K1`; NO / COM / NC unlabelled), and the logical module-`J2` ↔ Core-`J4` net-by-net match against [`s360-100-r4-core.md` §J4](s360-100-r4-core.md#j4--relay-module-connector-3-pin) (`+5V` ↔ `+5V`, `Relay` ↔ `Relay`, `GND` ↔ `GND`). Promote the top-line status from `pending — schematic/design evidence required` to `partial — schematic evidence available; package reconciliation pending`. Record the silkscreen-side, harness-side, BOM-side, and bench-side gaps that this PR does **not** close. Record the `IO3` (Core schematic) vs `GPIO4` ([`sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) line 61) vs `GPIO10` ([`sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) line 63) `relay_pin` disagreement as **unresolved** by this PR — resolution stays owned by `CORE-ABSTRACT-BUS-001` (alias for [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)). Refresh cross-links in [`docs/hardware/board-readiness-matrix.md`](board-readiness-matrix.md), [`docs/hardware/remaining-board-documentation-audit.md`](remaining-board-documentation-audit.md), [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md), [`docs/hardware/firmware-package-mapping-audit.md`](firmware-package-mapping-audit.md), [`docs/cleanup-audit.md`](../cleanup-audit.md), [`docs/product-readiness-matrix.md`](../product-readiness-matrix.md), [`docs/webflash-exposure-readiness-matrix.md`](../webflash-exposure-readiness-matrix.md), and [`docs/release-artifact-readiness-matrix.md`](../release-artifact-readiness-matrix.md). | [`docs/hardware/schematics/S360-310-R4.pdf`](schematics/S360-310-R4.pdf) (HW-ASSETS-310 module-side schematic, byte-identical to upload; SHA256 `441f0ccd681b5425fa9d276e6d7fb1bf289597674342dc8d9b8e656a23896c7d`; 38,967 bytes); [`docs/hardware/artifacts/S360-310-R4.md`](artifacts/S360-310-R4.md) (HW-ASSETS-310 curated artifact index — uploaded inventory, schematic content capture, retained-but-not-committed list, artifact-side Open Questions, [§Files NOT provided in this upload](artifacts/S360-310-R4.md#files-not-provided-in-this-upload)); [`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md) §[J4 — Relay module connector (3-pin)](s360-100-r4-core.md#j4--relay-module-connector-3-pin), §[Pin assignments](s360-100-r4-core.md#pin-assignments) (`IO3 = Relay`), §[Fan / driver outputs](s360-100-r4-core.md#fan--driver-outputs), and §[S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status) (bench / silkscreen / manufacturing-artifact gate); this document (§[Status](#status), §[Purpose and scope](#purpose-and-scope), §[Board identity](#board-identity), §[Repository evidence](#repository-evidence), §[Schematic summary](#schematic-summary), §[Connector / pin-map findings](#connector--pin-map-findings), §[Relay drive findings](#relay-drive-findings), §[Load / contact findings](#load--contact-findings), §[Existing package abstraction](#existing-package-abstraction), §[Reconciliation findings](#reconciliation-findings), §[Known unresolved issues](#known-unresolved-issues), §[Package YAML status](#package-yaml-status), §[Product / WebFlash status](#product--webflash-status), §[Required evidence before promotion](#required-evidence-before-promotion), §[Follow-up PR sequence](#follow-up-pr-sequence), §[Do-not-change guardrails](#do-not-change-guardrails)); [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) (`fan_relay_pin: ${relay_pin}` line 27; override-hook comment lines 22–25; `switch.platform: gpio` with `pin: ${fan_relay_pin}` line 38; `script: fan_emergency_stop` lines 58–65); [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) (`relay_pin: GPIO4` line 61 — disagrees with Core schematic `Relay = IO3`); [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) (`relay_pin: GPIO10` line 63 — disagrees with Core schematic); [`config/hardware-catalog.json`](../../config/hardware-catalog.json) `S360-310` row lines 62–70 (`group: Inline`, `type: Driver`, `friendly_name: Sense360 Relay`, `sku: S360-310`, `rev: R4`, `old_name: S360-Relay-C`, `description: On/off relay for bathroom fans.`, `schematic_status: cataloged_unverified`, no `schematic_file`); [`config/product-catalog.json`](../../config/product-catalog.json) (no FanRelay-bearing entry; the two shipping entries are `Ceiling-POE-VentIQ-RoomIQ` `status: production` / `channel: stable` and `Ceiling-POE-VentIQ-RoomIQ-LED` `status: preview` / `channel: preview`; `Ceiling-POE-VentIQ-FanTRIAC-RoomIQ` stays `status: blocked` / `blocker: HW-005` / `webflash_build_matrix: false`); [`config/webflash-builds.json`](../../config/webflash-builds.json) (no FanRelay-bearing build; only `Ceiling-POE-VentIQ-RoomIQ` stable and `Ceiling-POE-VentIQ-RoomIQ-LED` preview); [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) (`FanRelay` reserved in `canonical_modules` line 11, subject to the fan-driver `max-one-of` rule enforced by `FAN_DRIVER_TOKENS` in [`tests/validate_webflash_builds.py`](../../tests/validate_webflash_builds.py)); [`docs/hardware/board-readiness-matrix.md`](board-readiness-matrix.md) `S360-310` hardware-evidence and productization-axis rows + §[`S360-310` Sense360 Relay](board-readiness-matrix.md#s360-310-sense360-relay) board-by-board notes; [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md) `fan_relay.yaml` row + §[`fan_relay.yaml` / S360-310](package-readiness-matrix.md#fan_relayyaml--s360-310) detail; [`docs/hardware/firmware-package-mapping-audit.md`](firmware-package-mapping-audit.md) HW-009 Release-One package-stack `relay_pin` bullet (around line 405); [`docs/hardware/remaining-board-documentation-audit.md`](remaining-board-documentation-audit.md) §[Sense360 Relay (`S360-310`)](remaining-board-documentation-audit.md#sense360-relay-s360-310); [`docs/product-availability-taxonomy.md`](../product-availability-taxonomy.md) `S360-310` snapshot row (`partially-documented` + `design-pending`); [`docs/product-readiness-matrix.md`](../product-readiness-matrix.md) §[FanRelay / S360-310](../product-readiness-matrix.md#fanrelay--s360-310); [`docs/webflash-exposure-readiness-matrix.md`](../webflash-exposure-readiness-matrix.md) §[Relay / S360-310 WebFlash posture](../webflash-exposure-readiness-matrix.md#relay--s360-310-webflash-posture); [`docs/release-artifact-readiness-matrix.md`](../release-artifact-readiness-matrix.md) §[Relay / S360-310 release posture](../release-artifact-readiness-matrix.md#relay--s360-310-release-posture); [`docs/release-one-hardware-audit.md`](../release-one-hardware-audit.md) §[Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups) (Core abstract-bus rebind, alias `CORE-ABSTRACT-BUS-001`); [`docs/cleanup-audit.md`](../cleanup-audit.md) §[HW-PINMAP-310-FOLLOWUP update](../cleanup-audit.md#hw-pinmap-310-followup-update-2026-05-18-schematic-backed-reconciliation); [`docs/compliance/mains-voltage-uk-eu-assessment.md`](../compliance/mains-voltage-uk-eu-assessment.md) (COMPLIANCE-001 mains-voltage tracker — applies to `S360-320` / `S360-400`; does not gate FanRelay unless a future product switches mains loads, but tracked here as a sibling evidence record). | **Status promoted to `partial — schematic evidence available; package reconciliation pending`.** HW-PINMAP-310-FOLLOWUP consumed the HW-ASSETS-310 schematic and produced the schematic-backed reconciliation view above: module-`J2` ↔ Core-`J4` logical net match (`+5V` ↔ `+5V`, `Relay` ↔ `Relay`, `GND` ↔ `GND`) is schematic-backed; module-side relay coil-drive topology (`Q1` MMBT3904 NPN low-side; `R1` 1 kΩ; `R2` 10 kΩ; `D1` flyback; coil rail `+5V`; no opto; no indicator LED; no snubber) is schematic-backed; module-side `J1` 3-pin load-side connector identity is schematic-confirmed at the connector / pin-count layer. **The following stay unresolved:** module-side `J2` silkscreen pin-1 orientation; module-side `J1` silkscreen pin-1 orientation; module-side `J1` NO / COM / NC mapping; Core-side `J4` silkscreen pin-1 orientation (paired with the broader Core silkscreen gate in [`s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status)); Core-to-module 3-pin harness identity (conductor count, length, key / polarity at both ends, conductor-by-conductor pin mapping); `K1` BOM identity (part number, coil voltage, contact configuration, contact current / voltage / AC vs DC rating, isolation rating); whether the `Q1` MMBT3904 SOA is adequate for the chosen `K1` coil-current draw; any bench / continuity / waveform evidence against a populated `S360-310-R4` + `S360-100-R4` pair; and the systemic `IO3` (Core schematic) vs `GPIO4` ([`sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) line 61) vs `GPIO10` ([`sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) line 63) `relay_pin` disagreement (resolution belongs to `CORE-ABSTRACT-BUS-001`, alias for [`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups), **not** to HW-PINMAP-310-FOLLOWUP). [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) is **not** edited by this re-check: `fan_relay_pin: ${relay_pin}`, the override-hook comment block (lines 22–25), the `switch.platform: gpio` declaration with `pin: ${fan_relay_pin}` (line 38), and the `fan_emergency_stop` script (lines 58–65) are all preserved. [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) and [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) are also **not** edited. Downstream gates are unchanged: `S360-310` `schematic_status` stays `cataloged_unverified` with no `schematic_file` in [`config/hardware-catalog.json`](../../config/hardware-catalog.json) (the `S360-310 schematic_status` promotion remains gated on silkscreen / harness / BOM evidence + `CORE-ABSTRACT-BUS-001`); `PACKAGE-RELAY-001` (alias: `PACKAGE-GAP-001` FanRelay slice) stays **blocked** and the `fan_relay.yaml` row in [`package-readiness-matrix.md`](package-readiness-matrix.md) stays `schematic-evidence-pending` + `needs-package-reconciliation`; `PRODUCT-RELAY-001`, `WEBFLASH-RELAY-001`, `RELEASE-RELAY-001` (alias: `RELEASE-GAP-001` FanRelay slice), and `WF-IMPORT-RELAY-001` (alias: `WF-IMPORT-GAP-001` FanRelay slice) stay **blocked** behind `PACKAGE-RELAY-001`; no FanRelay-bearing entry exists in [`config/product-catalog.json`](../../config/product-catalog.json), [`config/webflash-builds.json`](../../config/webflash-builds.json), [`products/`](../../products/), or [`products/webflash/`](../../products/webflash/); `FanRelay` stays reserved in [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) `canonical_modules` (line 11) subject to the fan-driver `max-one-of` rule; `CORE-ABSTRACT-BUS-001` is not advanced; `S360-100-BENCH-001` is not closed; `HW-PINMAP-311-FOLLOWUP`, `HW-PINMAP-312-FOLLOWUP`, and `HW-PINMAP-320-FOLLOWUP` are not closed; `COMPLIANCE-001` is not closed. Release-One is unchanged: stays `Ceiling-POE-VentIQ-RoomIQ`, version `1.0.0`, channel `stable`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`, tag `v1.0.0`. LED preview is unchanged: stays `Ceiling-POE-VentIQ-RoomIQ-LED`, `status: preview`, `channel: preview`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-LED-v1.0.0-preview.bin`. The Sense360 LED Release-One exclusion is unchanged. FanTRIAC stays `status: blocked`, `blocker: HW-005`, `webflash_build_matrix: false`. The Core J10 vs RoomIQ J6 pin-order open question is not resolved. No FanRelay-bearing product / WebFlash / release / import is made ready by this re-check. |

| 2026-05-21 | CORE-ABSTRACT-BUS-001A landed (separate atomic slice). The schematic-correct `relay_pin: GPIO3` value (per Core schematic [`s360-100-r4-core.md` §Pin assignments](s360-100-r4-core.md#pin-assignments) `IO3 = Relay`) is now bound in the five affected Core abstract packages ([`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml), [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml), [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml), [`packages/hardware/sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml), [`packages/hardware/sense360_core_wall.yaml`](../../packages/hardware/sense360_core_wall.yaml)). The `GPIO3` collision that previously blocked the rebind was resolved by `CORE-ABSTRACT-BUS-001C-IMPLEMENT-001` / **PR #557** which moved ALS_INT off `GPIO3` to `GPIO47` and the SX1509 / expander interrupt off `GPIO3` to `GPIO17`. The `relay_pin` substitution in [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) (`fan_relay_pin: ${relay_pin}` line 27) now resolves to `GPIO3` against the affected Core abstract packages. The systemic `IO3` (Core schematic) vs `GPIO4` ([`sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) pre-001A) vs `GPIO10` ([`sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) pre-001A) `relay_pin` disagreement is **resolved at the Core abstract-bus substitution layer**. | [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) (`relay_pin: GPIO10 → GPIO3`); [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) (`relay_pin: GPIO4 → GPIO3`); [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml) (`relay_pin: GPIO10 → GPIO3`); [`packages/hardware/sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml) (`relay_pin: GPIO10 → GPIO3`); [`packages/hardware/sense360_core_wall.yaml`](../../packages/hardware/sense360_core_wall.yaml) (`relay_pin: GPIO4 → GPIO3`); [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) (`RelayPinRebindTests` + `MainRelaySwitchBindingTests` added; `RELAY_REBIND_PACKAGES` list pins the schematic-correct value); [`docs/hardware/core-abstract-bus-reconciliation.md`](core-abstract-bus-reconciliation.md) (new `### 2026-05-21 — CORE-ABSTRACT-BUS-001A implementation` audit-log entry); [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) (CORE-ABSTRACT-BUS-001A status update addendum). Voice-variant Core packages [`packages/hardware/sense360_core_voice_ceiling.yaml`](../../packages/hardware/sense360_core_voice_ceiling.yaml) and [`packages/hardware/sense360_core_voice_wall.yaml`](../../packages/hardware/sense360_core_voice_wall.yaml) remain at pre-001A `relay_pin: GPIO4` (out of scope for this slice). | **Status stays `partial — schematic evidence available; package reconciliation pending`.** This audit log entry records the Core abstract-bus substitution layer fix only. The following stay **unresolved** and continue to block `PACKAGE-RELAY-001` promotion: module-side `J2` silkscreen pin-1 orientation; module-side `J1` silkscreen pin-1 orientation; module-side `J1` NO / COM / NC mapping; Core-side `J4` silkscreen pin-1 orientation (paired with the broader Core silkscreen gate in [`s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status)); Core-to-module 3-pin harness identity (conductor count, length, key / polarity at both ends, conductor-by-conductor pin mapping); `K1` BOM identity (part number, coil voltage, contact configuration, contact current / voltage / AC vs DC rating, isolation rating); whether the `Q1` MMBT3904 SOA is adequate for the chosen `K1` coil-current draw; the general ESP32-S3 `GPIO3` strap-pin boot-behaviour bench characterisation against a populated `S360-310-R4` + `S360-100-R4` pair (operator decisions #16 / #17 in the 001C rebind plan record only the pair-scoped observed-OK, not the general claim); and any bench / continuity / waveform evidence against a populated `S360-310-R4` + `S360-100-R4` pair. [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) is **not** edited by this slice — the override-hook comment block (lines 22–25), the `switch.platform: gpio` declaration with `pin: ${fan_relay_pin}` (line 38), and the `fan_emergency_stop` script (lines 58–65) are preserved verbatim; only the resolved value of `${relay_pin}` changes (now `GPIO3`). Downstream gates are unchanged at the JSON / product / WebFlash / release layer: `S360-310` `schematic_status` stays `cataloged_unverified` with no `schematic_file` in [`config/hardware-catalog.json`](../../config/hardware-catalog.json); `PACKAGE-RELAY-001` stays **blocked** behind the silkscreen / harness / `K1` BOM / general boot-strap-behaviour preconditions above and the `fan_relay.yaml` row in [`package-readiness-matrix.md`](package-readiness-matrix.md) stays `schematic-evidence-pending` + `needs-package-reconciliation`; `PRODUCT-RELAY-001`, `WEBFLASH-RELAY-001`, `RELEASE-RELAY-001`, and `WF-IMPORT-RELAY-001` stay **blocked** behind `PACKAGE-RELAY-001`; no FanRelay-bearing entry exists in [`config/product-catalog.json`](../../config/product-catalog.json), [`config/webflash-builds.json`](../../config/webflash-builds.json), [`products/`](../../products/), or [`products/webflash/`](../../products/webflash/); `FanRelay` stays reserved in [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) `canonical_modules` (line 11) subject to the fan-driver `max-one-of` rule; `S360-100-BENCH-001` is not closed; `HW-PINMAP-311-FOLLOWUP`, `HW-PINMAP-312-FOLLOWUP`, and `HW-PINMAP-320-FOLLOWUP` are not closed; `COMPLIANCE-001` is not closed. Release-One stays `Ceiling-POE-VentIQ-RoomIQ`, version `1.0.0`, channel `stable`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`, tag `v1.0.0`. LED preview stays `Ceiling-POE-VentIQ-RoomIQ-LED`, `status: preview`, `channel: preview`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-LED-v1.0.0-preview.bin`. The Sense360 LED Release-One exclusion is unchanged. FanTRIAC stays `status: blocked`, `blocker: HW-005`, `webflash_build_matrix: false`. No claim of Relay load / contact / `K1` rating proof is made by this slice. No WebFlash import-readiness claim. No hardware release-readiness claim. No `RELEASE-RELAY-001` unblock claim. |

| 2026-05-21 | PACKAGE-RELAY-001 readiness refresh after 001C / 001A. Re-evaluate the `PACKAGE-RELAY-001` blocker set against the post-`CORE-ABSTRACT-BUS-001C` (PR #557) and post-`CORE-ABSTRACT-BUS-001A` (PR #558) repo state. Record which blockers are now resolved at the Core abstract-bus substitution layer (the `GPIO3` collision; the `relay_pin: IO3` vs `GPIO4` vs `GPIO10` disagreement; the absence of a pin-pinning regression for `relay_pin`; the question of whether `fan_relay.yaml` needs behaviour / package cleanup beyond inheriting `${relay_pin}`), and separate them from the remaining hardware / evidence blockers (S360-100 Core `J4` silkscreen / pin-1 orientation; S360-310 module-side `J2` / `J1` silkscreen / pin-1 orientation; `J1` `NO` / `COM` / `NC` mapping; Core ↔ module 3-pin harness identity; `K1` BOM identity; `K1` contact-current rating; Relay load / contact proof; general ESP32-S3 `GPIO3` strap-pin boot-behaviour characterisation against multiple populated `S360-310-R4` + `S360-100-R4` units — the operator-confirmed pair-scoped boot-OK observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17 is **not** a generic claim). Add a new top-line readiness section §[PACKAGE-RELAY-001 readiness refresh after CORE-ABSTRACT-BUS-001C / 001A](#package-relay-001-readiness-refresh-after-core-abstract-bus-001c--001a) containing the readiness table (blocker × previous state × current state × evidence source × still blocks PACKAGE-RELAY-001? × what unblocks it) and a conservative next-PR recommendation (bench-evidence-capture slice for `S360-310`; **not** `PACKAGE-RELAY-001` implementation). Refresh [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md) `fan_relay.yaml` row and the §[`fan_relay.yaml` / S360-310](package-readiness-matrix.md#fan_relayyaml--s360-310) detail to record the post-001A / 001C substitution-layer resolution while keeping the status `schematic-evidence-pending` + `needs-package-reconciliation`. Refresh [`docs/hardware/firmware-package-mapping-audit.md`](firmware-package-mapping-audit.md) Release-One package-stack `relay_pin` bullet to record `relay_pin: GPIO3` post-001A. Refresh [`UPCOMING_PR.md`](../../UPCOMING_PR.md) Current queue summary, Completed / merged PRs, and active queue (PRODUCT-RELAY-001 / WEBFLASH-RELAY-001 / RELEASE-RELAY-001 blockers refreshed to distinguish the resolved substitution-layer precondition from the still-open hardware-evidence blockers; Recently uploaded evidence bullet added). | [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) (whole file; structurally correct post-001A; `fan_relay_pin: ${relay_pin}` line 27 now resolves to `GPIO3`); [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) (`relay_pin: GPIO3` line 49); [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) (`relay_pin: GPIO3` line 78); [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml) (`relay_pin: GPIO3` line 51); [`packages/hardware/sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml) (`relay_pin: GPIO3` line 64); [`packages/hardware/sense360_core_wall.yaml`](../../packages/hardware/sense360_core_wall.yaml) (`relay_pin: GPIO3` line 71); [`packages/expansions/comfort_ceiling.yaml`](../../packages/expansions/comfort_ceiling.yaml) (`comfort_ceiling_als_int_pin: GPIO47` post-001C); [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml) (`sx1509_interrupt_pin: GPIO17` post-001C); [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) (`RelayPinRebindTests` / `MainRelaySwitchBindingTests` / `NoSubstitutionCollisionTests` / `ComfortCeilingAlsIntTests` / `ExpanderIntPinTests` / `SX1509InterruptPinTests` / `PirSensorPinTests` / `RoomIQHiLinkUartTests` / `RoomIQSen0609UartTests`); [`docs/hardware/core-abstract-bus-reconciliation.md`](core-abstract-bus-reconciliation.md) (§[2026-05-21 — CORE-ABSTRACT-BUS-001C implementation](core-abstract-bus-reconciliation.md#2026-05-21--core-abstract-bus-001c-implementation), §[2026-05-21 — CORE-ABSTRACT-BUS-001A implementation](core-abstract-bus-reconciliation.md#2026-05-21--core-abstract-bus-001a-implementation)); [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) (decisions #16 / #17 — pair-scoped boot OK; decision #19 — connector / harness accepted for planning record); [`docs/hardware/s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status) (still `pending — bench/manufacturing evidence required`); [`docs/hardware/artifacts/S360-310-R4.md` Files NOT provided in this upload](artifacts/S360-310-R4.md#files-not-provided-in-this-upload) (curated artifact-side gaps still owed); [`config/hardware-catalog.json`](../../config/hardware-catalog.json) `S360-310` row (`schematic_status: cataloged_unverified`, no `schematic_file`); [`config/product-catalog.json`](../../config/product-catalog.json) (no FanRelay-bearing entry); [`config/webflash-builds.json`](../../config/webflash-builds.json) (no FanRelay-bearing build); [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) (`FanRelay` token reservation unchanged); this document (new §[PACKAGE-RELAY-001 readiness refresh after CORE-ABSTRACT-BUS-001C / 001A](#package-relay-001-readiness-refresh-after-core-abstract-bus-001c--001a)). | **Status stays `partial — schematic evidence available; package reconciliation pending`.** This readiness refresh consolidates the post-`CORE-ABSTRACT-BUS-001C` / post-`CORE-ABSTRACT-BUS-001A` state into a readiness table. **Substitution-layer blockers resolved by PR #557 / PR #558:** the `GPIO3` collision; the `relay_pin: IO3` vs `GPIO4` vs `GPIO10` disagreement; the pin-pinning regression scaffold for `relay_pin`; the structural correctness check on `fan_relay.yaml` (the package was already correctly abstracted; no behaviour or package cleanup is owed beyond inheriting `${relay_pin}`). **Hardware / evidence blockers that still block `PACKAGE-RELAY-001`:** S360-100 Core `J4` silkscreen / pin-1 orientation (gated on `S360-100-BENCH-001` closure); S360-310 module-side `J2` / `J1` silkscreen / pin-1 orientation; `J1` `NO` / `COM` / `NC` mapping; Core ↔ module 3-pin harness identity; `K1` BOM identity (part number, coil voltage, contact configuration, isolation rating); `K1` contact-current rating; Relay load / contact proof (coil-drive waveform, `K1` switching behaviour, load-side continuity through `J1` `NO` / `COM` / `NC` contacts, optionally `Q1` MMBT3904 SOA characterisation under actual `K1` coil-current draw); and the general ESP32-S3 `GPIO3` strap-pin boot-behaviour bench characterisation against multiple populated `S360-310-R4` + `S360-100-R4` units (the operator-confirmed boot OK in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17 is **pair-scoped**, not a generic claim). The conservative recommended next PR is a bench-evidence-capture slice for `S360-310` (`HW-ASSETS-S360-310-BENCH-001` / `S360-310-BENCH-001` or sibling) committing operator-attributed silkscreen captures of module-side `J2` / module-side `J1` (with `NO` / `COM` / `NC` labels where present) / Core-side `J4`, the Core ↔ module harness inspection trace, the `K1` BOM identity, the coil-drive waveform capture, and the load-side continuity trace — **not** `PACKAGE-RELAY-001` implementation, **not** a Relay product YAML, **not** a WebFlash wrapper, **not** a compile-only target, and **not** a release artifact. [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) is **not** edited by this readiness refresh. Downstream gates are unchanged: `S360-310` `schematic_status` stays `cataloged_unverified` with no `schematic_file` in [`config/hardware-catalog.json`](../../config/hardware-catalog.json); `PACKAGE-RELAY-001` stays **blocked** on the silkscreen / harness / `K1` BOM / load-contact / general boot-strap-behaviour preconditions above and the `fan_relay.yaml` row in [`package-readiness-matrix.md`](package-readiness-matrix.md) stays `schematic-evidence-pending` + `needs-package-reconciliation` (the row notes are refreshed to record the post-001A / 001C substitution-layer resolution); `PRODUCT-RELAY-001`, `WEBFLASH-RELAY-001`, `RELEASE-RELAY-001`, and `WF-IMPORT-RELAY-001` stay **blocked** behind `PACKAGE-RELAY-001`; no FanRelay-bearing entry exists in [`config/product-catalog.json`](../../config/product-catalog.json), [`config/webflash-builds.json`](../../config/webflash-builds.json), [`products/`](../../products/), or [`products/webflash/`](../../products/webflash/); `FanRelay` stays reserved in [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) `canonical_modules` subject to the fan-driver `max-one-of` rule; `S360-100-BENCH-001` is not closed; `HW-PINMAP-311-FOLLOWUP`, `HW-PINMAP-312-FOLLOWUP`, and `HW-PINMAP-320-FOLLOWUP` are not closed; `COMPLIANCE-001` is not closed. Release-One stays `Ceiling-POE-VentIQ-RoomIQ`, version `1.0.0`, channel `stable`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`, tag `v1.0.0`. LED preview stays `Ceiling-POE-VentIQ-RoomIQ-LED`, `status: preview`, `channel: preview`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-LED-v1.0.0-preview.bin`. The Sense360 LED Release-One exclusion is unchanged. FanTRIAC stays `status: blocked`, `blocker: HW-005`, `webflash_build_matrix: false`. **No claim of Relay load / contact / `K1` rating proof is made by this readiness refresh.** No WebFlash import-readiness claim. No hardware release-readiness claim. No `RELEASE-RELAY-001` unblock claim. No `PACKAGE-RELAY-001` implementation-readiness claim. The voice-variant Core packages [`packages/hardware/sense360_core_voice_ceiling.yaml`](../../packages/hardware/sense360_core_voice_ceiling.yaml) and [`packages/hardware/sense360_core_voice_wall.yaml`](../../packages/hardware/sense360_core_voice_wall.yaml) remain at pre-001A `relay_pin: GPIO4` (out of scope for the 001A slice and for this readiness refresh). |

| 2026-05-21 | S360-310-BENCH-001 evidence-capture-checklist slice. Add the new top-line §[S360-310-BENCH-001 — Relay bench evidence](#s360-310-bench-001--relay-bench-evidence) section as the canonical bench-side / silkscreen-side / harness-side / BOM-side / load-contact-side companion to the schematic-side reconciliation tables above. Record the bench-evidence-capture **checklist** for the ten enumerated `PACKAGE-RELAY-001` hardware-evidence blockers: S360-100 Core `J4` silkscreen / pin-1 orientation; S360-310 Relay `J2` 1-to-3 pin order; S360-310 Relay `J1` `NO` / `COM` / `NC` mapping; Core `J4` ↔ Relay `J2` harness identity (straight-through or keyed); `K1` BOM identity / manufacturer / part number; `K1` contact-current rating; expected controlled load type; relay boot state with `S360-100-R4` + `S360-310-R4` attached; ESP32-S3 `GPIO3` strap-pin boot characterisation generalisation status; Relay load / contact proof result. **No physical bench, silkscreen, harness, BOM, load-contact, or strap-pin boot-behaviour evidence has been supplied.** Every row stays `pending — bench evidence required`. Refresh [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md) `fan_relay.yaml` row + §[`fan_relay.yaml` / S360-310](package-readiness-matrix.md#fan_relayyaml--s360-310) detail to add the S360-310-BENCH-001 evidence-capture-checklist link to the Follow-up owner chain while keeping the package status at `schematic-evidence-pending` + `needs-package-reconciliation`. Refresh [`UPCOMING_PR.md`](../../UPCOMING_PR.md) Current queue summary, Completed / merged PRs, and Recently uploaded evidence to record this PR's docs-only scope. | [`docs/hardware/s360-310-r4-relay.md`](s360-310-r4-relay.md) (new §[S360-310-BENCH-001 — Relay bench evidence](#s360-310-bench-001--relay-bench-evidence) section + this audit-log row; §[PACKAGE-RELAY-001 readiness refresh after CORE-ABSTRACT-BUS-001C / 001A](#package-relay-001-readiness-refresh-after-core-abstract-bus-001c--001a) preserved verbatim; §[Status](#status), §[Repository evidence](#repository-evidence), §[Schematic summary](#schematic-summary), §[Connector / pin-map findings](#connector--pin-map-findings), §[Relay drive findings](#relay-drive-findings), §[Load / contact findings](#load--contact-findings), §[Existing package abstraction](#existing-package-abstraction), §[Reconciliation findings](#reconciliation-findings), §[Known unresolved issues](#known-unresolved-issues), §[Package YAML status](#package-yaml-status), §[Product / WebFlash status](#product--webflash-status), §[Required evidence before promotion](#required-evidence-before-promotion), §[Follow-up PR sequence](#follow-up-pr-sequence), §[Do-not-change guardrails](#do-not-change-guardrails), §[See also](#see-also) preserved verbatim); [`docs/hardware/package-readiness-matrix.md`](package-readiness-matrix.md) §[`fan_relay.yaml` / S360-310](package-readiness-matrix.md#fan_relayyaml--s360-310) (Follow-up owner chain refreshed to link S360-310-BENCH-001 evidence-capture-checklist as this PR); [`UPCOMING_PR.md`](../../UPCOMING_PR.md) Current queue summary + Completed / merged PRs + Recently uploaded evidence (new entries for this PR); [`docs/hardware/s360-100-r4-core.md` §S360-100-BENCH-001 status](s360-100-r4-core.md#s360-100-bench-001-status) (still `pending — bench/manufacturing evidence required`; cross-linked from the new bench section's Core-`J4` row); [`docs/hardware/artifacts/S360-310-R4.md` Files NOT provided in this upload](artifacts/S360-310-R4.md#files-not-provided-in-this-upload) (curated artifact-side gaps still owed; cross-linked from the new bench section); [`docs/hardware/core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17 (pair-scoped boot OK; cross-linked from the new bench section's `GPIO3` boot-characterisation row); [`docs/hardware/hardware-artifact-policy.md`](hardware-artifact-policy.md) (operator-attribution rules for the bench-evidence rows); [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) (NOT edited; `fan_relay_pin: ${relay_pin}` line 27 unchanged); [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml) (NOT edited; `relay_pin: GPIO3` line 78 unchanged post-001A); [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml) (NOT edited; `relay_pin: GPIO3` line 49 unchanged post-001A); [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) (NOT edited; `RelayPinRebindTests` / `MainRelaySwitchBindingTests` / `NoSubstitutionCollisionTests` preserved); [`config/hardware-catalog.json`](../../config/hardware-catalog.json) `S360-310` row (NOT edited; `schematic_status: cataloged_unverified`, no `schematic_file`); [`config/product-catalog.json`](../../config/product-catalog.json) (NOT edited; no FanRelay-bearing entry); [`config/webflash-builds.json`](../../config/webflash-builds.json) (NOT edited; no FanRelay-bearing build); [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) (NOT edited; `FanRelay` token reservation unchanged). | **Status stays `partial — schematic evidence available; package reconciliation pending`.** This audit-log entry records the addition of the S360-310-BENCH-001 evidence-capture **checklist** to this document. **No physical evidence has been supplied.** Every one of the ten enumerated hardware-evidence rows (S360-100 Core `J4` silkscreen; S360-310 Relay `J2` pin-1; S360-310 Relay `J1` `NO` / `COM` / `NC`; Core ↔ module harness identity; `K1` BOM identity; `K1` contact-current rating; expected controlled load type; relay boot state with the populated pair attached; `GPIO3` strap-pin boot characterisation generalisation status; Relay load / contact proof) stays `pending — bench evidence required`. The pair-scoped operator boot-OK observation in [`core-abstract-bus-001c-rebind-plan.md`](core-abstract-bus-001c-rebind-plan.md) decisions #16 / #17 is cross-referenced for completeness and is **not** promoted to a general claim about ESP32-S3 `GPIO3` strap-pin boot behaviour. [`packages/expansions/fan_relay.yaml`](../../packages/expansions/fan_relay.yaml) is **not** edited; the override-hook comment block (lines 22–25), the `switch.platform: gpio` declaration with `pin: ${fan_relay_pin}` (line 38), the `restore_mode: RESTORE_DEFAULT_OFF` posture, the `fan_auto_mode` global, and the `fan_emergency_stop` script are all preserved. [`packages/hardware/sense360_core_ceiling.yaml`](../../packages/hardware/sense360_core_ceiling.yaml), [`packages/hardware/sense360_core.yaml`](../../packages/hardware/sense360_core.yaml), [`packages/hardware/sense360_core_mapping.yaml`](../../packages/hardware/sense360_core_mapping.yaml), [`packages/hardware/sense360_core_poe.yaml`](../../packages/hardware/sense360_core_poe.yaml), [`packages/hardware/sense360_core_wall.yaml`](../../packages/hardware/sense360_core_wall.yaml) all stay at the post-001A `relay_pin: GPIO3` value. [`packages/expansions/comfort_ceiling.yaml`](../../packages/expansions/comfort_ceiling.yaml) stays at the post-001C `comfort_ceiling_als_int_pin: GPIO47`. [`packages/expansions/gpio_expander_sx1509.yaml`](../../packages/expansions/gpio_expander_sx1509.yaml) stays at the post-001C `sx1509_interrupt_pin: GPIO17`. [`tests/test_core_abstract_bus.py`](../../tests/test_core_abstract_bus.py) is **not** edited; the `RelayPinRebindTests` / `MainRelaySwitchBindingTests` / `NoSubstitutionCollisionTests` scaffold from 001A / 001C is preserved verbatim. Downstream gates are unchanged at the JSON / product / WebFlash / release layer: `S360-310` `schematic_status` stays `cataloged_unverified` with no `schematic_file` in [`config/hardware-catalog.json`](../../config/hardware-catalog.json); `PACKAGE-RELAY-001` stays **blocked** at the evidence layer (silkscreen / harness / `K1` BOM / contact-current rating / load-contact proof / general `GPIO3` strap-pin boot characterisation all still owed) and the `fan_relay.yaml` row in [`package-readiness-matrix.md`](package-readiness-matrix.md) stays `schematic-evidence-pending` + `needs-package-reconciliation`; `PRODUCT-RELAY-001`, `WEBFLASH-RELAY-001`, `RELEASE-RELAY-001`, and `WF-IMPORT-RELAY-001` stay **blocked** behind `PACKAGE-RELAY-001`; no FanRelay-bearing entry exists in [`config/product-catalog.json`](../../config/product-catalog.json), [`config/webflash-builds.json`](../../config/webflash-builds.json), [`products/`](../../products/), or [`products/webflash/`](../../products/webflash/); `FanRelay` stays reserved in [`config/webflash-compatibility.json`](../../config/webflash-compatibility.json) `canonical_modules` (line 11) subject to the fan-driver `max-one-of` rule; `S360-100-BENCH-001` is not closed; `HW-PINMAP-311-FOLLOWUP`, `HW-PINMAP-312-FOLLOWUP`, and `HW-PINMAP-320-FOLLOWUP` are not closed; `COMPLIANCE-001` is not closed. Release-One stays `Ceiling-POE-VentIQ-RoomIQ`, version `1.0.0`, channel `stable`, artifact `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`, tag `v1.0.0`. LED preview stays `Ceiling-POE-VentIQ-RoomIQ-LED`, `status: preview`, `channel: preview`. FanTRIAC stays `status: blocked`, `blocker: HW-005`, `webflash_build_matrix: false`. No `packages/**`, `products/**`, `products/webflash/**`, `config/**`, `scripts/**`, `.github/workflows/**`, `components/**`, `include/**`, `firmware/**`, `manifest.json`, `firmware/sources.json`, `tests/**` edit. No `webflash_build_matrix` flip. No `artifact_name` / `webflash_wrapper` / `config_string` / `release_one_required_configs` / `lifecycle_statuses` / `canonical_modules` / `canonical_power` / `forbidden_tokens` / `REQUIRED_CONFIGS` / kit change. No `schematic_status` / `schematic_file` promotion. No COMPLIANCE-001 movement. **No claim of Relay load / contact / `K1` rating proof** — the evidence-capture checklist remains pending. **No WebFlash import-readiness claim.** **No hardware release-readiness claim.** **No `RELEASE-RELAY-001` unblock claim.** **No `PACKAGE-RELAY-001` implementation-readiness claim** — the package stays blocked at the evidence layer. No edit to `fan_relay.yaml`. No Relay product YAML. No WebFlash wrapper. No compile-only target. No release artifact / tag / checksum / build-info manifest. |

The next audit-log entry against HW-PINMAP-310-FOLLOWUP should
appear when committed evidence is added to this repository that
the field-evidence rules above can cite: operator-attributed
silkscreen captures of the module-side `J2` 1-to-3 pin order, the
module-side `J1` 1-to-3 pin order (with NO / COM / NC labels if
present), and the Core-side `J4` 1-to-3 pin order; physical 3-pin
Core ↔ module harness inspection with conductor-by-conductor pin
mapping recorded; `K1` BOM identity (part number, manufacturer,
coil voltage, contact configuration, contact current / voltage /
AC vs DC rating, isolation rating); oscilloscope or bench
captures of the relay coil-drive waveform, the `K1` switching
behaviour, the load-side continuity through `J1` NO / COM / NC
contacts, or the `Q1` MMBT3904 SOA under the actual coil-current
draw, against a populated `S360-310-R4` + `S360-100-R4` pair,
with operator / reviewer identity and review date recorded; a
standalone schematic-backed reference doc (e.g. a rewritten
`docs/hardware/s360-310-r4-relay.md` or a companion
`docs/hardware/s360-310-r4-relay-reference.md` in the per-board
reference pattern of
[`s360-200-r4-roomiq.md`](s360-200-r4-roomiq.md) /
[`s360-211-r4-ventiq.md`](s360-211-r4-ventiq.md) /
[`s360-300-r4-led.md`](s360-300-r4-led.md)); KiCad schematic
source (`S360-310-R4.kicad_sch`) / KiCad PCB source
(`S360-310-R4.kicad_pcb`) / KiCad project metadata
(`*.kicad_pro` / `*.kicad_prl` / `fp-lib-table` /
`sym-lib-table`) / BOM / CPL / Gerbers / drill files / STEP / or
board images for `S360-310-R4` that the audit can cite by repo
path; or progress on the systemic Core abstract-bus rebind
(`CORE-ABSTRACT-BUS-001`, alias for
[`release-one-hardware-audit.md` Required follow-ups #2 / #3](../release-one-hardware-audit.md#required-follow-ups)).
Until any of those land, the next audit-log entry should report
the same `partial — schematic evidence available; package
reconciliation pending` outcome with the new inspection date.

# S360-300-R4 — Sense360 LED Hardware Reference

## Purpose

This document is the schematic-backed reference for the **Sense360 LED**
board, revision **R4** (`S360-300-R4`). It exists so that future audits and
any future LED-bearing config string have a single citable source of truth
for what the LED board itself carries.

This document is hardware reference only. It does **not** by itself prove
that any ESPHome YAML in this repository is correct. The HW-007 PR commits
the module-side schematic PDF and this standalone reference doc.

The machine-readable `schematic_status` field for `S360-300` in
[`config/hardware-catalog.json`](../../config/hardware-catalog.json) is
**not** changed by this PR — the status promotion from `cataloged_unverified`
is deferred to HW-008.

> **Sense360 LED is not in Release-One.** The Release-One config string is
> `Ceiling-POE-VentIQ-RoomIQ` and carries **no** `LED` token. The
> Release-One YAML
> [`products/sense360-ceiling-poe-ventiq-roomiq.yaml`](../../products/sense360-ceiling-poe-ventiq-roomiq.yaml)
> omits LED package includes on purpose. HW-007 commits LED schematic
> evidence; it does **not** add `LED` to the Release-One config string,
> the Release-One artifact name, the WebFlash build matrix, or the product
> catalog. A future LED-bearing config (e.g. `Ceiling-POE-VentIQ-RoomIQ-LED`)
> is a separate product PR.

## Board identity

| Field         | Value                                              |
| ------------- | -------------------------------------------------- |
| Friendly name | Sense360 LED                                       |
| SKU           | S360-300                                           |
| Rev           | R4                                                 |
| Old name      | LED Ring                                           |
| Role          | WS2812B-based addressable LED ring (indicator)     |

This row matches the entries in
[`docs/hardware-catalog.md`](../hardware-catalog.md) and
[`config/hardware-catalog.json`](../../config/hardware-catalog.json). Do
not use the legacy `LED Ring` name in new user-facing material.

## Schematic source

| Field             | Value                                              |
| ----------------- | -------------------------------------------------- |
| Schematic file    | [`schematics/S360-300-R4.pdf`](schematics/S360-300-R4.pdf) |
| PDF committed     | Yes, under HW-007                                  |
| KiCad source      | Not committed                                      |

The schematic PDF is the authoritative reference for the sections below.
Where a label in the PDF was ambiguous or hard to read, the affected row
is marked **verify**. No pin is inferred or invented.

## Main components (visible on this sheet)

- **Connector `J1`** — 3-pin module connector. Pins (per the schematic):
  - `LED_DATA` — WS2812B data input
  - `+5V` — LED power
  - `GND` — ground
- **WS2812B LED chain** — addressable RGB LEDs driven from the connector's
  `LED_DATA` line. Exact LED count and chain order are **verify** against
  the silkscreen.

The Core side of `LED_DATA` is the `74LVC1G07` (`U2A`) open-drain buffer
plus the 330 Ω series resistor (`R8`) on `S360-100-R4`, terminating at the
Core's `J3` 3-pin LED connector — see
[`s360-100-r4-core.md` LED output](s360-100-r4-core.md#led-output).
The Sense360 LED board's `J1` mates with the Core's `J3` either directly
or via a short harness. The harness identity (direct mate vs cable) is
**verify**.

## Connector `J1` — module connector (3-pin)

| Pin | Net        | Notes                                                           |
| --- | ---------- | --------------------------------------------------------------- |
| 1   | `LED_DATA` | WS2812B data input. Mates with the Core `J3` `LED_DATA_3V3` pin (after the `U2A` buffer + `R8` 330 Ω on Core). |
| 2   | `+5V`      | LED power. Mates with the Core `J3` `+3.3V` or `+5V` rail — **verify** against the Core J3 capture (`s360-100-r4-core.md` records Core J3 pin 1 as `+3.3V`; a `+5V` mismatch on the silkscreen would be a separate Open Question for HW-008 / silkscreen verification). |
| 3   | `GND`      | Common ground.                                                  |

The exact 1-to-3 pin order on `J1` is **verify** against the silkscreen.
The Core `J3` table in
[`s360-100-r4-core.md` §J3](s360-100-r4-core.md#j3--ws2812b-led-connector-3-pin)
records the mating side; both tables should agree on a real board, and
any disagreement is a verification gap, not an HW-007 finding.

## LED chain

The schematic shows a chain of WS2812B addressable LEDs driven from the
`LED_DATA` pin of `J1`. Each WS2812B integrates an RGB LED + serial-data
controller, so the chain is cascaded `DOUT → DIN` through successive LEDs.
Exact LED count on the production S360-300-R4 board is **verify** against
the silkscreen and the PCB layout — this doc does not state a count.

## What this schematic proves

- The Sense360 LED board has its own module-side schematic, now committed
  to this repo at
  [`schematics/S360-300-R4.pdf`](schematics/S360-300-R4.pdf).
- The board exposes a 3-pin module connector (`J1`) carrying `LED_DATA`,
  `+5V`, and `GND`.
- The on-board LED implementation is a WS2812B chain driven from
  `LED_DATA`.

## What this schematic does **not** prove

- It does **not** prove that Sense360 LED is part of Release-One. The
  Release-One config string `Ceiling-POE-VentIQ-RoomIQ` carries no `LED`
  token. The Release-One YAML omits LED package includes on purpose.
- HW-007 itself did **not** resolve the `LED_DATA` source-pin
  discrepancy at the package level. That reconciliation has since been
  landed by **HW-010**:
  - [`packages/hardware/led_ring_ceiling.yaml`](../../packages/hardware/led_ring_ceiling.yaml)
    now drives WS2812B on `GPIO38` (Core `IO38 = LED_DATA` per the
    `S360-100-R4` schematic).
  - The Core schematic (`S360-100-R4`) shows `LED_DATA` originating at
    ESP32-S3 `IO38`, with `IO14 = SCS` (peripheral SPI chip-select).
  - HW-010 is a one-package edit; it does **not** add `LED` to the
    Release-One config string, does **not** add a WebFlash LED build,
    and does **not** change the FanTRIAC HW-005 blocker. Sense360 LED
    remains excluded from Release-One. The wall LED package
    (`packages/hardware/led_ring_wall.yaml`)
    and the legacy S3 Core package
    ([`packages/hardware/sense360_core_ceiling_s3.yaml`](../../packages/hardware/sense360_core_ceiling_s3.yaml))
    were intentionally not changed by HW-010 — neither has Core-side
    schematic evidence proving the same `LED_DATA` path, and both
    remain documented as unresolved.
- It does **not** add `LED` to the Release-One config string.
- It does **not** change the WebFlash build matrix or the product
  catalog.
- It does **not** flip the JSON `schematic_status` field for `S360-300`.

## Open questions / verification needed

These items are explicitly out of scope for HW-007.

1. **`J1` pin 2 rail identity (`+5V` vs `+3.3V`).** The Core `J3` table
   records the rail as `+3.3V`; the Sense360 LED board may operate
   WS2812B from `+5V` instead. Confirm against the silkscreen on both
   boards.
2. **LED count on the chain.** Not stated in this doc; **verify** against
   the silkscreen.
3. ~~**`LED_DATA` source-pin reconciliation at the package level.**~~
   **Resolved by HW-010.** The ceiling LED package
   ([`packages/hardware/led_ring_ceiling.yaml`](../../packages/hardware/led_ring_ceiling.yaml))
   now binds `led_data_pin: GPIO38`, matching `IO38 = LED_DATA` on the
   `S360-100-R4` schematic. HW-010 does not change anything about
   Release-One: LED stays excluded from the Release-One config string,
   no WebFlash LED build was added, and no product-catalog LED entry
   was added. The wall LED and legacy S3 Core packages remain
   unresolved at the package level — see HW-010 in
   [`firmware-package-mapping-audit.md` (archived)](../archive-index.md).
4. **Harness between Core `J3` and LED `J1`.** Direct mate vs short
   cable / pigtail is **verify**.
5. **LED protection components on the LED board.** Series resistors,
   capacitor across `+5V`/`GND`, or ESD protection local to the LED
   board are not exhaustively captured here. The Core side carries the
   330 Ω series on `LED_DATA` (`R8`) and the 74LVC1G07 (`U2A`) buffer;
   any LED-side additions are **verify**.

The harness-rail (Open Question 1), LED-count (Open Question 2), and
harness-identity (Open Question 4) items above are tracked as **row 10
(hardware bench verification)** of the canonical preview-to-stable
gate table in
[`../preview-to-stable-promotion-gates.md` (archived)](../archive-index.md);
each must close (silkscreen / scope / measurement evidence committed)
or be explicitly accepted in writing before the LED preview build is
eligible for `status: production` / `channel: stable`. The bench-side
companion record where any future real-hardware observation against
these questions is captured is **S360-300-BENCH-001** below.

## S360-300-BENCH-001 status

**Status: pending — bench hardware evidence required.**

This section is the bench-side companion to the schematic-side
[Open questions](#open-questions--verification-needed) above and to
**row 10 (hardware bench verification)** of
[`../preview-to-stable-promotion-gates.md` (archived)](../archive-index.md).
It is the canonical place where real-hardware observations of the
Sense360 LED (`S360-300-R4`) board are recorded once they exist.

**No physical bench evidence has been supplied for this record.** No
operator, no date, no observed board / harness / rail / count /
behaviour values have been provided. The Open Questions above remain
`verify`; the RELEASE-006 row-10 gate remains `pending`; this section
does **not** close it.

### Evidence supplied

The fields below would be filled by an operator after a bench session.
None has been filled. **Values are not invented.** Implicit acceptance
is not allowed.

| Field | Observed value | Evidence source |
|---|---|---|
| Bench date | not recorded — pending bench evidence | — |
| Tester / operator initials | not recorded — pending bench evidence | — |
| Physical S360-300 board (serial / batch) | not recorded — pending bench evidence | — |
| S360-300 revision | not recorded — pending bench evidence | — |
| Sense360 Core board (serial / batch) | not recorded — pending bench evidence | — |
| Core revision | not recorded — pending bench evidence | — |
| Harness used (direct mate vs cable / pigtail) | not recorded — pending bench evidence | — |
| Core connector exercised | not recorded — pending bench evidence (expected `J3` per [`s360-100-r4-core.md#j3--ws2812b-led-connector-3-pin`](s360-100-r4-core.md#j3--ws2812b-led-connector-3-pin)) | — |
| LED connector exercised | not recorded — pending bench evidence (expected `J1` per [Connector `J1`](#connector-j1--module-connector-3-pin) above) | — |
| Rail measurement on `J1` pin 2 (`+5V` vs `+3.3V`) | not recorded — pending bench evidence | — |
| Data-pin / signal-path observation (Core `IO38` → `U2A` 74LVC1G07 → `R8` 330 Ω → `J3` → LED `J1`) | not recorded — pending bench evidence | — |
| Ground / power continuity observation | not recorded — pending bench evidence | — |
| LED count on the production board | not recorded — pending bench evidence | — |
| LED type (WS2812B variant, if observable from silkscreen / package marking) | not recorded — pending bench evidence | — |
| Test firmware or preview firmware used | not recorded — pending bench evidence | — |
| Flash / source used | not recorded — pending bench evidence | — |
| Observed LED ring behaviour (boot pattern, brightness control, colour order) | not recorded — pending bench evidence | — |
| Photos / screenshots / serial logs / scope captures / measurements | not supplied — pending bench evidence | — |
| Known failures or anomalies | not recorded — pending bench evidence | — |

### Per-question status

| Bench question | Status | Source-of-truth Open Question |
|---|---|---|
| Harness rail (`J1` pin 2 `+5V` vs `+3.3V`) | `pending — bench hardware evidence required` | [Open Question 1](#open-questions--verification-needed) |
| LED count on the chain | `pending — bench hardware evidence required` | [Open Question 2](#open-questions--verification-needed) |
| Harness identity (Core `J3` to LED `J1`, direct mate vs cable) | `pending — bench hardware evidence required` | [Open Question 4](#open-questions--verification-needed) |
| Observed LED ring behaviour | `pending — bench hardware evidence required` | tracked in this section + [`board-readiness-matrix.md` (archived)](../archive-index.md) S360-300 "Open work / stable gate" |

A field can only be marked `verified` here when committed evidence
(silkscreen photo, scope capture, harness photograph, measurement,
serial log) is supplied with operator identity and date. A bench
session that closes some but not all four questions yields `partial`,
not `verified`. A bench session that produces a blocking failure
yields `failed / blocked` with the anomaly recorded under
"Known failures or anomalies" above.

### Status-language rules

The only status values this record may carry are:

- `pending — bench hardware evidence required` (no evidence supplied)
- `partial — some bench evidence recorded, but open questions remain`
- `verified — all listed bench questions answered by evidence`
- `failed / blocked — bench test produced a blocking issue`

Today the record is `pending`. Anything stronger is a future bench-PR
change and must cite real evidence per the field table above.

### What this record does **not** do

S360-300-BENCH-001 is documentation only. It does not promote any
state on its own — even if it later flips to `verified`, the
downstream gates remain separate.

- It does **not** promote Sense360 LED to stable / `status: production`
  / `channel: stable`. RELEASE-006 rows 9 and 11–17 stay independent.
- It does **not** add `LED` to the Release-One config string. The
  Release-One config string remains `Ceiling-POE-VentIQ-RoomIQ` with
  no `LED` token, artifact
  `Sense360-Ceiling-POE-VentIQ-RoomIQ-v1.0.0-stable.bin`, tag
  [`v1.0.0`](https://github.com/sense360store/esphome-public/releases/tag/v1.0.0).
- It does **not** add Sense360 LED to WebFlash-side `REQUIRED_CONFIGS`
  (the baseline-health invariant list); the
  [REQUIRED_CONFIGS policy (archived)](../archive-index.md)
  default is **do not add**, and that default is unchanged.
- It does **not** create an LED kit entry in WebFlash's
  `scripts/data/kits.json`. The
  [Kit policy (archived)](../archive-index.md)
  default is **do not add**, and that default is unchanged.
- It does **not** substitute for the WebFlash operator flash proof
  (`WF-HW-TEST-001`). Row 9 of RELEASE-006 is a separate,
  independently-owned gate in the
  [`sense360store/WebFlash`](https://github.com/sense360store/WebFlash)
  repo and remains `pending` regardless of any state recorded here.
- It does **not** flip `S360-300` in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json)
  (already `schematic_status: verified` under HW-008 — that flip is
  schematic-side evidence, not bench-side evidence).
- It does **not** change FanTRIAC. `Ceiling-POE-VentIQ-FanTRIAC-RoomIQ`
  stays `status: blocked`, `blocker: HW-005`,
  `webflash_build_matrix: false`. Bench-verifying LED does not
  advance the FanTRIAC unblock.
- It does **not** change the mains-voltage compliance status for
  `S360-320` or `S360-400` (COMPLIANCE-001).
- It does **not** edit any file under
  [`config/`](../../config/), [`products/`](../../products/),
  [`products/webflash/`](../../products/webflash/),
  [`packages/`](../../packages/), [`tests/`](../../tests/),
  [`scripts/`](../../scripts/), [`components/`](../../components/),
  [`include/`](../../include/), or `.github/workflows/`.
- It does **not** advance any future "RELEASE-007"-style LED stable
  promotion. Any such promotion would require closing all four
  bench questions here **and** clearing every other RELEASE-006 row
  (operator flash proof, stable release notes, production catalog
  promotion, stable build artifact, stable WebFlash import,
  REQUIRED_CONFIGS / kit decisions, human approval).

### Cross-links

- [Open questions / verification needed](#open-questions--verification-needed)
  — the schematic-side companion list this record mirrors.
- [`preview-to-stable-promotion-gates.md` row 10 (archived)](../archive-index.md)
  — the RELEASE-006 gate this record reports against.
- [`preview-to-stable-promotion-gates.md` Required hardware evidence (archived)](../archive-index.md)
  — the LED-specific list of Open Questions that must close or be
  accepted before stable promotion is considered.
- [`board-readiness-matrix.md` S360-300 row (archived)](../archive-index.md)
  — the per-board readiness view; bench / operator proof cell stays
  `pending`.
- [`firmware-package-mapping-audit.md` LED_DATA / Sense360 LED (archived)](../archive-index.md)
  — HW-009 / HW-010 firmware-package-vs-schematic row; `confirmed-ok`
  is package-vs-schematic agreement and is not a bench result.

## Firmware mapping notes

This document is hardware reference only.

The Sense360 LED package today is
[`packages/hardware/led_ring_ceiling.yaml`](../../packages/hardware/led_ring_ceiling.yaml),
which drives WS2812B on `GPIO38` as of **HW-010**, matching the Core
schematic (`IO38 = LED_DATA`). HW-007 / HW-008 committed the schematic
evidence; HW-010 landed the matching package-level edit. HW-010 does
**not** add `LED` to the Release-One config string and does **not**
make Sense360 LED WebFlash-shippable — that remains a separate future
product PR.

The Release-One YAML
[`products/sense360-ceiling-poe-ventiq-roomiq.yaml`](../../products/sense360-ceiling-poe-ventiq-roomiq.yaml)
does **not** include the LED package — the WebFlash config string
`Ceiling-POE-VentIQ-RoomIQ` carries no `LED` token, so the binary built
from that YAML does not include LED firmware.

## HW-007 scope and non-scope

What this doc does:

- Records that the `S360-300-R4` schematic PDF is now committed under
  [`schematics/S360-300-R4.pdf`](schematics/S360-300-R4.pdf).
- Captures the visible main-component evidence: 3-pin `J1` carrying
  `LED_DATA` / `+5V` / `GND`; WS2812B LED chain.
- Confirms that schematic evidence exists for the LED board.

What this doc does **not** do:

- It does **not** add Sense360 LED to Release-One. The Release-One
  config string remains `Ceiling-POE-VentIQ-RoomIQ`, with no `LED` token.
- It does **not** change the Release-One artifact name, the WebFlash
  build matrix, the product catalog, or the WebFlash compatibility
  snapshot.
- It does **not** flip `S360-300` in
  [`config/hardware-catalog.json`](../../config/hardware-catalog.json)
  (`schematic_status` stays `cataloged_unverified`); the JSON status
  upgrade is deferred to HW-008.
- HW-007 itself did **not** resolve the `GPIO14` (package) vs `IO38`
  (schematic) `LED_DATA` discrepancy; that reconciliation was landed
  later by **HW-010** as a one-package edit (`GPIO14` → `GPIO38` in
  [`packages/hardware/led_ring_ceiling.yaml`](../../packages/hardware/led_ring_ceiling.yaml)).
  HW-010 did not add `LED` to the Release-One config string and did
  not promote Sense360 LED to WebFlash-shippable.
- It does **not** rebind, audit, or rename
  `packages/hardware/led_ring_ceiling.yaml` or any LED package.
- It does **not** change the LED-related reconciliation flags in
  [`docs/hardware/s360-100-r4-core.md`](s360-100-r4-core.md).

## See also

- [Sense360 Hardware Catalog](../hardware-catalog.md) — canonical board /
  module names, SKUs, revisions, and legacy names. Do not reuse
  `LED Ring` in new user-facing material.
- [`config/hardware-catalog.json`](../../config/hardware-catalog.json) —
  machine-readable mirror of the catalog. LED's row remains
  `schematic_status: cataloged_unverified` after HW-007; HW-008 owns the
  status flip.
- [S360-100-R4 Core Hardware Reference](s360-100-r4-core.md) — the
  Sense360 Core board, including the `LED_DATA → U2A 74LVC1G07 → R8 → J3`
  path that drives the LED board's `J1` input.
- [Remaining-board documentation audit (HW-004 / HW-006) (archived)](../archive-index.md)
  — classifies Sense360 LED's current documentation state and explicitly
  records that LED is excluded from Release-One.
- [Release-One Hardware Audit (archived)](../archive-index.md) — LED
  policy (Option A: LED packages omitted from Release-One YAML).
- [Release-One Configuration (archived)](../archive-index.md) — Release-One config
  string `Ceiling-POE-VentIQ-RoomIQ`; explicitly notes Sense360 LED is
  not bundled.
- [WebFlash compatibility taxonomy audit (COMPAT-001) (archived)](../archive-index.md)
  — records `LED` as a future-token in the taxonomy and explicitly keeps
  it excluded from Release-One.
- [Hardware Source and Manufacturing Artifact Policy (HW-ASSETS-001) (archived)](../archive-index.md)
  — canonical policy for hardware source / manufacturing artifacts.
  Records Sense360 LED (`S360-300-R4`) as a finished /
  hardware-evidenced board, and defines the per-board artifact index
  workflow that will record any future KiCad / BOM / CPL / Gerbers /
  STEP material for this board. Hardware evidence alone does not
  promote Sense360 LED to WebFlash-shippable.

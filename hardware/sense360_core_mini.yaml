# Core "mini" hardware: ESP-IDF, board, I2C bus.

esp32:
  board: ${board_variant}
  framework:
    type: esp-idf

# Single, stable I²C bus id
i2c:
  id: i2c0
  sda: ${i2c_sda_pin}
  scl: ${i2c_scl_pin}
  scan: true

